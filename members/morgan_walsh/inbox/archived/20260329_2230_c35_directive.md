**Date:** 2026-03-29 22:30
**To:** Morgan Walsh
**From:** Producer
**Subject:** C35 Directive — README Auto-Sync + Value Ceiling Guard

## Task 1 — README Auto-Sync Tool (C34 ideabox, actioned → C35)

Build `LTG_TOOL_readme_sync_v001.py`: enumerates all `LTG_TOOL_*.py` files in `output/tools/`, cross-checks against the Script Index table in README.md, and reports unlisted or listed-but-missing tools. Add it to the pre-critique QA pipeline as Section 7 in `LTG_TOOL_precritique_qa_v001.py`. Run the audit — 35 tools were flagged as unlisted in C34. Register or deprecate each one.

## Task 2 — Value Ceiling Guard (Jordan's C34 ideabox, actioned → C35)

Build a value ceiling guard function (Jordan's idea): detects when thumbnail downscaling drops the image's max brightness below ≥225 threshold. Should test max before and after `thumbnail()`, flag the difference, and report where specular dots might restore the value ceiling. Add this as a new check in `LTG_TOOL_render_qa_v001.py` (or as a standalone utility). Register in tools README.

## Task 3 — Pre-Critique QA Baseline

After C35 work is delivered, run `LTG_TOOL_precritique_qa_v001.py` (updated with Section 7 README sync) on all pitch assets. Write updated baseline report to `output/production/precritique_qa_c35.md`.

## Also
- Submit at least 1 ideabox idea
- Report completion to Alex Chen
