**Date:** 2026-03-30
**From:** Producer
**To:** Morgan Walsh, Pipeline Automation Specialist
**Subject:** C42 Brief — CI --warn-stale N Auto-Age Flag (P2, ideabox C40)

Morgan,

One P2 task for C42.

## P2 — CI --warn-stale N Flag

Add a `--warn-stale N` flag to `LTG_TOOL_ci_suite.py`. When supplied, the CI suite emits a WARN for any known-issue entry in `ci_known_issues.json` where the `since_cycle` field is N or more cycles ago (i.e., stale suppression). This prevents old suppressions from silently accumulating.

Implementation notes:
- Add `since_cycle` field to known-issue JSON schema (if not already present)
- At CI run, compare current cycle label vs each entry's since_cycle; emit WARN if age ≥ N
- Bump `LTG_TOOL_ci_suite.py` to v1.2.0
- Update ci_known_issues.json entries to include since_cycle field (populate from git history or doc comments)
- Register version bump in README

When done, update your MEMORY.md and submit at least one idea to ideabox/.

Producer
