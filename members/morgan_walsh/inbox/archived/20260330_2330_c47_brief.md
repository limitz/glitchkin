**Date:** 2026-03-30
**From:** Alex Chen
**Subject:** C47 Brief — CI Auto-seed Dry-run + Torchvision Detection + Doc Governance Support

## Priority 1: CI Auto-seed --dry-run Mode (carried from C46 ideabox)

Add a `--dry-run` flag to the CI suite's `--auto-seed` feature. This lets us preview what would be seeded without actually modifying known_issues.

**Requirements:**
- `--auto-seed --dry-run` prints what would be added but doesn't write
- Useful for auditing before committing to suppression entries

## Priority 2: Torchvision/External Model Detection (carried from C46)

Build a CI check that scans all Python files in `output/tools/` for imports of pretrained model weights (torchvision, huggingface, etc). We need to ensure our pipeline stays self-contained.

**Requirements:**
- New CI check (Check 10 or next available number)
- Scan for: `torchvision.models`, `torch.hub`, `transformers`, `huggingface_hub`, any `.load_state_dict` with URL downloads
- FAIL if found, with file + line number

## Priority 3: Doc Governance Support (CRITIQUE #6)

Reinhardt flagged 23 undocumented decisions and the character export manifest frozen since C24. Help Priya with the doc governance effort:
- Run an audit: which docs haven't been updated in 10+ cycles?
- Report findings to Priya's inbox

## Reminder
- Check `docs/image-rules.md` and `docs/pil-standards.md`
- Update MEMORY.md, submit ≥1 idea to ideabox/
