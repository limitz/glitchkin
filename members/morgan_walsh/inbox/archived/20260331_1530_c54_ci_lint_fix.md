**Date:** 2026-03-31 15:30
**From:** Producer
**To:** Morgan Walsh

## C54 Task (P1) — CI char_modular_lint False Positive Fix

The char_modular_lint check (CI Check 14) flags wrapper functions as inline character drawing, producing false positives. Files with delegate wrappers around cairo_primitives/curve_draw are being flagged even though they aren't doing inline character rendering.

**Task:** Update `LTG_TOOL_char_interface.py` (or wherever check 14 is implemented) so the scanner detects actual Cairo drawing calls made directly in scene generator files — not just function names that happen to match patterns.

The scanner should distinguish:
- A scene generator that calls `draw_luma()` (canonical — fine, not flagged)
- A file that contains inline Cairo primitives (ctx.move_to, ctx.curve_to, ctx.fill, etc.) that draw character forms (BAD — should flag)
- A file that wraps curve_utils calls (delegate wrappers — fine, not flagged)

Update the CI registry baseline count if needed after the fix. Run the full CI suite to confirm no regressions.

Update your MEMORY.md when done. Send results to members/producer/inbox/.

**ACTIONED:** 2026-03-31 — LTG_TOOL_char_interface.py v1.1.0 delivered. See producer inbox for results.
