**Date:** 2026-03-29 22:48
**From:** Producer
**To:** Morgan Walsh, Pipeline Automation Specialist
**Subject:** C39 — Pipeline Tasks

Morgan,

Cycle 39. Three tasks.

## Task 1 — CI Suite Re-Run (P1)

Re-run the full CI suite (`LTG_TOOL_ci_suite_v001.py`) to confirm clean result after Kai's C38 fixes. Expected: all PASS, suppression fix stable, G002 false positive resolved. Report the result — if FAIL, flag immediately.

## Task 2 — Lineup Suppression Expansion (your C38 ideabox idea)

Extend `glitch_spec_suppressions.json` to cover character lineup files v004–v007:
- These trigger G006/G007 false positives (non-Glitch character skin tones, outline pattern mismatches in multi-character lineup files)
- Add suppression entries, or add a `file_prefix` suppression mode to glitch_spec_lint to suppress by filename pattern (e.g., `*_lineup_*`)
- The `file_prefix` approach is preferred — more future-proof as lineup versions increase
- Expected: drop WARN count from ~15 to ~3 (only legitimate edge cases remaining)
- Review `ideabox/actioned/20260330_morgan_walsh_lineup_suppression_expansion.md` for the full spec

## Task 3 — Arc-Diff as Pre-Critique Gate (Lee Tanaka's C38 ideabox idea)

Integrate `LTG_TOOL_contact_sheet_arc_diff_v001.py` into `LTG_TOOL_precritique_qa_v001.py` as a pre-critique gate step:
- Run arc-diff between current and prior contact sheet version
- If CHANGED > 3 panels: add NOTE to QA report listing which panels changed
- If REMOVED > 0: add WARN (panel removal = story continuity risk)
- Saves critics time — gives them a changelog before they open any files
- Review `ideabox/actioned/20260330_lee_tanaka_arc_diff_as_precrit_gate.md` for the spec

## Delivery
- CI suite run results (report in completion message)
- Updated `LTG_TOOL_precritique_qa_v001.py` (arc-diff integration, version bump)
- Updated `glitch_spec_suppressions.json` or updated `LTG_TOOL_glitch_spec_lint_v001.py` (file_prefix support)
- Submit 1 ideabox idea to `/home/wipkat/team/ideabox/`

Start by reading your ROLE.md, then output/tools/README.md, then all inbox messages.

— Producer
