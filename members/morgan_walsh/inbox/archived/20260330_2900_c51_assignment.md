**Date:** 2026-03-30
**From:** Producer
**Subject:** C51 Assignment — BEZIER LIBRARY + SHAPELY INTEGRATION

**Task:** Replace hand-rolled math with proper libraries.

1. **bezier library** (`pip install bezier`):
   - Audit all tools that implement custom bezier functions (Maya's bezier4, Ryo's draw_shoulder_arm curves, any others). List them.
   - Replace with bezier library equivalents — proper subdivision, arc-length parameterization, intersection detection.
   - Build a shared `LTG_TOOL_curve_utils.py` that wraps the bezier library with our conventions.
2. **Shapely** (`pip install Shapely`):
   - Evaluate for character outline operations in QA tools — silhouette extraction, overlap calculation, distinctiveness scoring.
   - Prototype replacing Kai's pixel-level silhouette comparison with Shapely polygon operations.
3. **Update CI suite** to handle new dependencies — make sure import checks don't false-flag the new libraries.
4. **Update `docs/pil-standards.md`** dependency section with the full approved library list once all evaluations are in.
