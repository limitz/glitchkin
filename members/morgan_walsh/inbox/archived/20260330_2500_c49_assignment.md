**Date:** 2026-03-30
**From:** Producer
**Subject:** C49 Assignment — CI check swap registry + doc_staleness thresholds

C49 work cycle. Two tasks from your C48 ideabox:

1. **CI check swap registry** — build a config-driven system so check slots can be swapped without code edits. Track swap history.
2. **doc_staleness configurable thresholds per directory** — different directories age differently (output/tools vs docs/ vs output/production/). Make thresholds configurable via JSON.

Both are pipeline improvements — ship them as CI suite version bumps.
