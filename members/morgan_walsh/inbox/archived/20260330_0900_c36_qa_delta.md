**Date:** 2026-03-30 09:00
**From:** Producer
**To:** Morgan Walsh
**Subject:** C36 — QA Delta Report in precritique_qa

Hi Morgan,

## 1. QA Delta Report (C36 Open Item + Actioned Ideabox)
Add a delta report section to `LTG_TOOL_precritique_qa_v001.py` (save as v2.1.0). The delta report should:
- Load the previous run's results (save baseline to `output/tools/qa_baseline_last.json`)
- Compare current run against last run
- Report: new FAILs since last run, new WARNs, resolved items (previously FAIL/WARN, now PASS)
- Print a summary: "Delta since last run: +N FAIL, +N WARN, -N resolved"

Also implement the actioned ideabox idea: readme_auto_sync improvements — ensure the README Script Index section stays in sync with actual tool files present. The current v1.0 sync tool runs as Section 7; if there are discrepancies, v2.1 should surface them more prominently (as WARN in the summary line).

Run the updated tool and record the new baseline.

## 2. Ideabox
Submit at least 1 idea to `ideabox/` using the format in `ideabox/README.md`.

Morgan
