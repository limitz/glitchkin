# Cycle 22 Tasks — Jordan Reed (Background & Environment Artist)

**Date:** 2026-03-29 15:00
**From:** Producer (via Alex Chen)
**Subject:** Critique 10 feedback — environment fixes

---

## Critique 10 Feedback (Environments)

**Takeshi Murakami** reviewed Tech Den v003 and Kitchen v002. Grade: B- and pending.
His main critique: the team hears notes but doesn't fully solve them. This cycle: SOLVE the light shaft, don't renegotiate it.

Full critique: `output/production/critique_c10_takeshi_murakami.md`

---

## Your Cycle 22 Tasks

### Task 1 — Tech Den v004 (CRITICAL — Takeshi P1 + P2)

**Fix 1a: Move the light shaft into the desk zone**
The current shaft fans BELOW the floor line — placing the light event where no character occupies.
- Shaft apex must be repositioned so the majority of the shaft area lands ON and ABOVE the desk surface (DESK_TOP_Y ≈ 395)
- Shaft should be narrower (~200px wide at widest)
- Position: diagonal from upper-left (near window) to mid-desk-right, fading before reaching bed zone
- Increase max_alpha to 140-160 — a daylight shaft in a dark room is NOT subtle
- Use `ltg_render_lib.light_shaft()` (already in library)

This is the desk where Cosmo works. The light must land there.

**Fix 1b: Individuate monitor glow spill**
Current: single wide-ellipse desk spill = uniform ambient wash. Not acceptable.
Replace with THREE separate `gaussian_glow()` calls, one per monitor:
- CRT1 (x≈195): cool blue-white spill forward+left onto desk
- CRT2 (x≈420): spill onto central desk area
- Flat panel (x≈635): spill toward oscilloscope zone

Three monitors = three distinct light zones on the desk surface. This is what makes a multi-monitor workspace feel inhabited.

Output: `LTG_ENV_tech_den_v004.py` + rendered `LTG_ENV_tech_den_v004.png`

### Task 2 — Grandma Miri Kitchen v003 (Takeshi P3)

**Fix 2a: Extend wall texture to side walls**
Upper wall stripe texture is currently only on the back wall rectangle. Apply to left AND right wall polygons.
- Reduce stripe alpha on side walls by 30-40% (WALL_SHADOW vs WALL_WARM — less illuminated surfaces)

**Fix 2b: Fix floor grid perspective**
There are two contradictory spatial grids on the same floor surface — `draw_floor_tiles()` and `draw_floor_linoleum_overlay()`. This will eventually become visible as an artifact.
Options:
a. Integrate into one perspective-correct linoleum grid converging to vp_x, OR
b. Remove `draw_floor_tiles()` entirely and rely on `draw_floor_linoleum_overlay()` with a corrected perspective-aware grid

One floor system. No contradictions.

Output: `LTG_ENV_grandma_kitchen_v003.py` + rendered `LTG_ENV_grandma_kitchen_v003.png`

---

## Notes
- `ltg_render_lib.py` will be renamed by Kai to `LTG_TOOL_render_lib_v001.py` this cycle. Update your imports in both v004 generators if Kai's work is done first. Check Kai's inbox for status.
- If blocked waiting for Kai's rename: write your generator with a note at the top marking the import for update.

---

## Deliverables
1. `output/backgrounds/environments/LTG_ENV_tech_den_v004.py` + `.png`
2. `output/backgrounds/environments/LTG_ENV_grandma_kitchen_v003.py` + `.png`

Report completion to Alex Chen inbox.
