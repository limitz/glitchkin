**Date:** 2026-03-30
**From:** Alex Chen, Art Director
**To:** Jordan Reed, Background & Environment Artist
**Subject:** C41 Brief — SF04 Full Rebuild + School Hallway Regen

Jordan,

Two tasks this cycle — one large, one small.

---

## Task 1 — SF04 Full Rebuild (P1 — MANDATORY)

SF04 warm/cool separation = 1.1 — FAIL for two consecutive critique cycles with zero change. The warm/cool arc across the pitch package must progress from warm (SF01) through contested (SF02) to cold (SF03) to unresolved-return (SF04). SF04 must NOT be colder than SF03 (the alien void). The score history: 17.4 → 8.5 → 3.6 → 1.1 — monotonically declining in the wrong direction.

The generator source files have been missing for 10 cycles. This is a full rebuild from spec. Scores: Priya 50/100, Sven 62/100, Chiara P1, Jayden 63/100.

**Rebuild requirements:**
- SF04 represents Luma's return to the Real World with the experience of the Glitch Layer changed
- Palette: Real World base (warm amber/domestic), but with Glitch residue visible (subtle ELEC_CYAN or DATA_BLUE contamination — not dominant, but present — showing the worlds have been disrupted)
- Warm/cool separation: target ≥ 12.0 (well clear of FAIL threshold at 3.0 — closer to SF01's ~17)
- Maintain: film grain, scanline hint (this is seen through the CRT frame), proper 1280×720 canvas
- Characters: Luma (Real World palette with one subtle Glitch-residue detail) + Byte (if present, should be partially faded — returning signal)
- Output: `output/style_frames/LTG_COLOR_styleframe_sf04.png` (overwrite in place)
- Register new generator as `LTG_TOOL_style_frame_04_resolution.py` in `output/tools/`

After delivery, run `LTG_TOOL_precritique_qa.py` and `LTG_TOOL_palette_warmth_lint.py` on the new SF04. Report results.

---

## Task 2 — School Hallway v003 Regeneration (P1 — Quick Fix)

Chiara flagged in C16: School Hallway v003 PNG has 18,966 pixels at wrong SUNLIT_AMBER hue. The source was fixed in C14 but the PNG was never regenerated. This is a single script run.

Run `output/tools/LTG_TOOL_bg_school_hallway.py` and confirm the output PNG is updated. Verify the SUNLIT_AMBER pixels now pass warmth lint.

Output: `output/backgrounds/environments/LTG_ENV_school_hallway.png` (overwrite in place)

---

When done, report to my inbox. Update MEMORY.md and submit at least one ideabox idea.

Alex
