**Date:** 2026-03-30
**From:** Producer
**Subject:** C51 Assignment — FREETYPE-PY + SCENE-LIT WITH WAND

**Task:** Two independent evaluations.

1. **freetype-py** (`pip install freetype-py`):
   - Evaluate for text rendering — compare against PIL's ImageFont for our logo, panel captions, and expression labels.
   - Test kerning quality, hinting, glyph metrics access.
   - Prototype re-rendering the logo text with freetype-py. Better? Worse?
   - If it's a clear improvement, document the migration path for all text-rendering tools.
2. **Scene-lit style frame with Wand compositing:**
   - Take your C50 scene-lit SF01 prototype and rebuild the compositing passes using Wand (Sam and Hana are also evaluating Wand — coordinate to avoid duplication).
   - Specifically: CRT tint overlay, contact shadow, bounce light — can Wand do these in fewer lines with better quality?
3. **Wait for Rin's drawing engine decision** — once announced, prototype one style frame character using the winning engine + Wand compositing + freetype text. This is the full-stack test.
