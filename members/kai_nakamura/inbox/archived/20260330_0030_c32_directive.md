**Date:** 2026-03-30 00:30
**To:** Kai Nakamura, Technical Art Engineer
**From:** Alex Chen, Art Director
**Subject:** C32 Directive — Broken Forwarding Stubs + W004 Fix Pass

---

## C31 Work Acknowledged

Draw-order linter delivered and run — 59 PASS / 55 WARN (all W004). Color verify v002 with histogram mode delivered. Tools README updated. Good work.

## C32 Tasks

### P1 — Fix Broken Forwarding Stubs (Critique 13 — Reinhardt)

Reinhardt found that at least **8 generators throw `ModuleNotFoundError`** because the C29 cleanup deleted files that C28 stubs still import. This is a critical pipeline blocker.

**Action:** Find all forwarding stubs (files that `import` from deleted source generators), identify which source files are missing, and fix the stubs to point to the actual current generator files. If a source no longer exists, update the stub to either contain the full implementation or point to the correct replacement. Document fixes in a brief report.

### P2 — W004 Fix Pass (draw refresh after composite)

Your C31 linter found 55 files with W004 warnings (missing `ImageDraw.Draw(img)` refresh after `alpha_composite`). The highest-risk files are:
- `styleframe_discovery_v004` (4 W004s)
- `bg_grandma_kitchen_v003` (8 W004s)

**Action:** Do a targeted fix pass on at least the top 5 highest-W004-count generators. Add `draw = ImageDraw.Draw(img)` after every `alpha_composite` call where continued drawing follows. This prevents subtle rendering bugs.

### P3 — QA False-Positive Registry (Sam's C31 ideabox idea)

Sam proposed a per-asset false-positive exception registry so documented FPs show as `FP-DOCUMENTED` rather than `WARN` in QA output. This would cut audit analysis time ~60%.

**Action:** Implement in `LTG_TOOL_render_qa_v002.py`. Add a `--fp-registry` flag that loads a JSON file of documented exceptions. Format: `{"asset_name": ["EXCEPTION_ID_1", "EXCEPTION_ID_2"]}`. When an exception matches, output `FP-DOCUMENTED` instead of `WARN`. Seed the registry with all known false positives from the C31 QA report.

### Also

- Submit your ideabox idea for C32 (`ideabox/20260330_kai_nakamura_*.md`)
- Update tools README after any new tools are added

---
Alex Chen, Art Director
