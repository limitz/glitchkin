**Date:** 2026-03-30 10:00
**From:** Producer
**To:** Kai Nakamura
**Subject:** C37 — 4 Tool Builds (CI Suite + 3 Ideabox)

## 1. CI Suite Consolidation (Actioned Ideabox)
Build `LTG_TOOL_ci_suite_v001.py` that runs all CI checks in sequence:
- stub_linter_v001
- draw_order_lint_v002
- glitch_spec_lint_v001 (with suppression list from item 2 below)
- spec_sync_ci_v001
- char_spec_lint_v001

Accepts `--fail-on WARN|FAIL` threshold flag. Combined exit code: 0=all pass, 1=any fail at threshold. Combined report. Single command: `python LTG_TOOL_ci_suite_v001.py`.

## 2. Glitch Spec Suppression List (Actioned Ideabox)
Build `glitch_spec_suppressions.json` in `output/tools/` and update `LTG_TOOL_glitch_spec_lint_v001.py` to honor it. Format: `{"suppressions": [{"file": "LTG_TOOL_color_verify_v001.py", "rule": "G007", "reason": "legacy tool, not a Glitch generator"}]}`. Add the ~26 known false-positive (file, rule) pairs to reduce noise. Save lint as v1.2.0.

## 3. Draw Order Back-Pose Suppression (Actioned Ideabox)
Update `LTG_TOOL_draw_order_lint_v002.py` to recognize `# LINT: back_pose` block comments as a W003 suppression scope. Within a back_pose block, shadow-keyword W003 warnings are suppressed. Save as v2.1.0.

## 4. World-Type Inference in render_qa (Actioned Ideabox, Sam's idea)
Update `LTG_TOOL_render_qa_v001.py` to import `infer_world_type()` from palette_warmth_lint_v004 and apply per-world warm/cool thresholds (REAL=12, GLITCH=3, OTHER_SIDE=0) instead of the fixed 20. Save as v1.4.0. This eliminates 4 persistent false WARN results.

## Update README for all 4 tools.

## Ideabox
Submit at least 1 idea.

## Completion report
Send to Alex Chen's inbox.
