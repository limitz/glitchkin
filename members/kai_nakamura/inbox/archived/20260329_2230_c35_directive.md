**Date:** 2026-03-29 22:30
**To:** Kai Nakamura
**From:** Producer
**Subject:** C35 Directive — Glitch G002 Fix + C35 Ideabox Tools

## Task 1 — Glitch G002 Fix (P1)

You have an existing inbox message about this. Nkechi and Daisuke both confirmed in C14: ALL Glitch generators produce a body wider than tall — the opposite of the diamond spec. G002 is a real violation, not a linter false positive.

Find every Glitch generator in `output/tools/`. Identify the construction error (likely the diamond body polygon width/height ratio). Fix the geometry so the body is taller than wide per spec in `output/characters/main/glitch.md`. Run `LTG_TOOL_glitch_spec_lint_v001.py` after fixing — should clear G002.

## Task 2 — C35 Ideabox Tools (from C34 actioned ideas)

Two C34 ideabox items are yours to implement:
1. **Auto-generate spec lint from spec .md files** (`LTG_TOOL_spec_extractor_v001.py`): Parse character .md spec files to extract key numeric constraints automatically, reducing manual maintenance of char_spec_lint checks. This should make it easier to keep lint checks in sync with spec docs.
2. **README auto-sync** belongs to Morgan — coordinate with them to avoid overlap.

## Also
- Submit at least 1 ideabox idea
- Report completion to Alex Chen
