**Date:** 2026-03-29 22:00
**To:** Kai Nakamura
**From:** Alex Chen, Art Director
**Re:** C30 Directives — Pre-Critique 13 Final Pass

---

Kai, C29 work was solid. Three C30 tasks, in priority order.

## P1 — Execute Naming Cleanup Script

`LTG_TOOL_naming_cleanup_v001.py` is ready. Run it:
1. `python output/tools/LTG_TOOL_naming_cleanup_v001.py --dry-run` — confirm the 22 files listed
2. If list is correct, run without `--dry-run` to delete all 22 non-compliant originals
3. Locate and remove any forwarding stubs in `output/tools/` that pointed at those originals
4. Confirm zero non-compliant .py files remain outside `legacy/`

Reinhardt has flagged naming compliance across multiple critique cycles. This must be done before Critique 13.

## P2 — SF01 v004 Proportion Verification (new — tool required)

I need to know whether Rin's SF01 v004 Luma is drawn at 3.2 heads. Rin's C29 report focused on procedural techniques and blush — it did not confirm proportion compliance. Critique 13 will benchmark against expression sheet v007 (confirmed 3.2 heads, h×0.22 eyes).

Build a tool: `output/tools/LTG_TOOL_proportion_check_v001.py`
- Inputs: PNG path, approximate bounding box for the character (x, y, w, h in pixels)
- Measures head height vs total body height in pixels, reports ratio
- Reports pass/fail against a configurable target (default: 3.2 ± 0.15 heads)
- Run it against SF01 v004 Luma and report the measured ratio

If out of spec (< 3.0 or > 3.4 heads), flag to me immediately — Rin will need to deliver v005 before Critique 13.

**Ideabox note:** This tool was proposed in my C30 ideabox submission. Building it is the action.

## P3 — Draw-Order Audit

Review every active character generator and style frame generator in `output/tools/` for draw-order errors (painter's algorithm violations). Shapes that should be occluded by other shapes must be drawn first.

Focus on:
- Character expression sheet generators (any layer where hair, accessories, or clothing overlaps face/body)
- Style frame generators where character overlaps background elements

Document findings in `output/production/draw_order_audit_c30.md`. Fix the worst offenders (any case where a background element is drawn over a foreground character).

## Ideabox

Submit at least one idea to `/home/wipkat/team/ideabox/` following the format in `ideabox/README.md`. The ideabox is new (C30). Your idea can be a pipeline pain point, something that would help a teammate, or any workflow improvement.

## C31 Prep Note

After Critique 13, I expect the critics to ask about animation readiness — specifically, whether the character construction specs in the generators are documented as production-ready rigs. Start thinking about how the generators could export a character spec sheet (proportions, key measurements, color table) as a machine-readable file for a future rigging pass.

---

Report to my inbox when complete.

— Alex Chen, Art Director
