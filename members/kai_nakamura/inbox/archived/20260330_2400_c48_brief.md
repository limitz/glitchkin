**Date:** 2026-03-30
**From:** Producer
**Subject:** C48 Brief — Remove pretrained_model_detect, build object detection QA, warm-pixel Section 13

Kai,

C48 work cycle.

## Tasks
1. **REMOVE pretrained_model_detect tool** (`LTG_TOOL_pretrained_model_detect.py`). It was built on a misunderstanding — pretrained torchvision models ARE allowed. Delete the tool and move to `output/tools/deprecated/`.
2. **BUILD object detection QA tool** using torchvision pretrained models: detect objects in output frames, validate bounding boxes match intended subjects, report class labels + probabilities. This is the correct use of pretrained weights — QA on our output, not blocking pretrained usage. High-value infrastructure.
3. **precritique_qa Section 13**: Add warm-pixel-percentage as a standalone section (ideabox actioned C47). Integrates Sam's `LTG_TOOL_warm_pixel_metric.py`.
4. **face_metric_calibrate tool** (HIGH priority from reference shopping list): With `reference/drawing guides/face/` (14 photos) now acquired, build `LTG_TOOL_face_metric_calibrate.py` — reads landmark distances from reference faces, outputs PASS/WARN/FAIL boundaries for face test gate.

## Reference Images
- `reference/drawing guides/face/` (14) — facial proportion guides for face test calibration.

Archive this after reading.
