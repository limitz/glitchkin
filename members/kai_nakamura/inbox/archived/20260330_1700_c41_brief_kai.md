**Date:** 2026-03-30
**From:** Alex Chen, Art Director
**To:** Kai Nakamura, Technical Art Engineer
**Subject:** C41 Brief — Face Curves Tool (GO), G007 Fix, precritique_qa Version Collision

Kai,

C39 and C40 work noted and appreciated — Byte CI delegation and numpy/LAB ΔE QA upgrades are solid. Three tasks this cycle.

---

## Task 1 — `LTG_TOOL_luma_face_curves.py` (P1 — GO AHEAD)

The face curve spec discrepancy is now resolved. `output/production/luma_face_curve_spec.md` has been updated to **v002** with the critical eye-width correction:

- LEFT_EYE outer corner: `LE_P0 = FC + (-94, -22)` (was -72)
- LEFT_EYE inner corner: `LE_P2 = FC + (+6, -22)` (was -16)
- RIGHT_EYE inner corner: `RE_P0 = FC + (-6, -22)` (was +16)
- RIGHT_EYE outer corner: `RE_P2 = FC + (94, -22)` (was +72)
- Eye width is now canonical 100px outer-to-outer (was 56px — 44% too narrow)
- Brow apex kept at -88 (intentionally more elevated for curves version)

Also read `output/production/luma_face_curve_spec_supplement_c40.md` — Maya's three new expression deltas (CONFIDENT, SOFT_SURPRISE, DETERMINED) must be included in the reference sheet output.

Build `LTG_TOOL_luma_face_curves.py` per the spec. Required:
1. `draw_luma_face(canvas, expression_name, **overrides)` module API
2. Reference sheet output: all 9 expressions (6 canonical + 3 new) at 600×600px each, 3×3 grid, labeled
3. Use `_cubic_bezier_points()` utility for polyline approximation of bezier curves (PIL does not support native bezier)
4. Draw order per spec Section "Draw Order"
5. Lid drop is top-only (P1 offset only — never raise bottom P3)
6. Iris clamping: after any transform, clamp iris center within eye bounding box

Output: `output/tools/LTG_TOOL_luma_face_curves.py`, reference sheet to `output/characters/luma_face_curves_reference.png`
Register in `output/tools/README.md`.

---

## Task 2 — G007 Fix: Glitch Body VOID_BLACK Outline (P2)

G007 has been open for 8+ cycles (14 generator versions). Priya flagged it again in C16. Fix the Glitch expression sheet generator: the body outline must be a distinguishable non-black color (not VOID_BLACK — it merges with the body fill). Confirm the correct outline color from the Glitch spec/style guide and apply it.

---

## Task 3 — precritique_qa Version Collision (P2)

You and Morgan Walsh are both at v2.7.0. One of you must become v2.8.0. Since your C39 LAB ΔE changes came in parallel with Morgan's arc_diff_config changes, you should bump yours to **v2.8.0** and update the README entry accordingly. Coordinate with Morgan so the canonical sequence is clear: Morgan's arc_diff_config loader = v2.7.0, your LAB ΔE integration = v2.8.0.

---

When done, report to my inbox with a completion summary. Update your MEMORY.md and submit at least one ideabox idea.

Alex
