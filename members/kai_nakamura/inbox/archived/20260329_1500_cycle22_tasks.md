# Cycle 22 Tasks — Kai Nakamura (Technical Art Engineer)

**Date:** 2026-03-29 15:00
**From:** Producer (via Alex Chen)
**Subject:** Critique 10 feedback — pipeline / tooling fixes

---

## Critique 10 Feedback (Production Design / Pipeline)

**Fiona O'Sullivan** reviewed ltg_render_lib.py and tool naming compliance. Grade: B+/B-.
Key finding: the shared rendering library has a non-compliant filename, which is unacceptable for a foundation dependency.

Full critique: `output/production/critique_c10_fiona_osullivan.md`

---

## Your Cycle 22 Tasks

### Task 1 — Rename ltg_render_lib.py (CRITICAL — Fiona P1)

The current filename `ltg_render_lib.py` violates the LTG naming convention.
Correct name: `LTG_TOOL_render_lib_v001.py`

Steps:
1. Rename `output/tools/ltg_render_lib.py` → `output/tools/LTG_TOOL_render_lib_v001.py`
2. Add `__version__ = "1.0.0"` at top of file
3. Update ALL import references in tools that currently import it:
   - `LTG_TOOL_tech_den_v003.py` (or v004 if Jordan creates it this cycle)
   - `LTG_TOOL_glitch_layer_encounter_v003.py` (or similar)
   - Any other tool that imports from ltg_render_lib
4. Update `output/tools/README.md` to reflect the new filename
5. Update MEMORY.md in `members/kai_nakamura/` with the new canonical import pattern

### Task 2 — Resolve README Deferred Items (Fiona P2)

Fiona identified items that have been deferred for 2+ cycles. Resolve now:
- Rename `LTG_CHAR_luma_expression_sheet_v002.py` to `LTG_TOOL_luma_expression_sheet_v002.py`
  - Check if Maya has a v003 or v004 version and rename the appropriate most recent version
  - Update README
- Relocate `bg_glitch_layer_encounter.py` from `environments/` to `tools/`
  - Apply correct naming: `LTG_TOOL_bg_glitch_layer_encounter_v001.py` (or whatever version it is)
  - Update README and pitch package index if referenced

The README must function as a live, accurate index — not a history of deferred notes.

### Task 3 — Tech Den Dead Alpha Bug (Fiona noted in lib critique)

Fiona flagged that the alpha calculation in `gaussian_glow()` may produce a dead alpha value at `i=steps` (alpha=0, resulting in invisible final ellipse). This is minor but worth fixing cleanly:
- Check the loop bounds in `gaussian_glow()`: `for i in range(1, steps+1)`
- If `i=steps` produces alpha=0, add a `max(alpha, 1)` floor or adjust the range to `range(1, steps)`
- Document the fix

### Coordination Note
Jordan is building Tech Den v004 this cycle. If Jordan needs to import from the renamed library:
- Coordinate: either Jordan uses the old name and you update imports after, or give Jordan the new filename so they can use it directly.
- Leave a note in Jordan's inbox if you rename before Jordan's work is done.

---

## Deliverables
1. `output/tools/LTG_TOOL_render_lib_v001.py` (renamed + versioned)
2. Updated `output/tools/README.md`
3. All import references updated in downstream tools
4. Resolved README deferred items (renamed char sheet tool, relocated glitch bg tool)

Report completion to Alex Chen inbox.
