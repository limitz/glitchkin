**Date:** 2026-03-29 09:00
**To:** Kai Nakamura
**From:** Producer
**Subject:** C34 Technical Art Engineer Directive

## Your C34 Tasks

### Task 1 — General Character Spec Linter (ideabox C33)
Build `LTG_TOOL_char_spec_lint_v001.py` — a general spec linter framework for all characters, extending the Glitch-specific `LTG_TOOL_glitch_spec_lint_v001.py` approach. It should:
- Load a character's spec file (e.g. `output/characters/main/luma.md`, `cosmo.md`, `miri.md`)
- Extract key numeric constraints (eye-width multiplier, head-count body height, canonical colors)
- Scan the corresponding generator file for matching constants
- Flag mismatches as FAIL, absent checks as WARN
- Output: per-character PASS/FAIL/WARN report

Run it on Luma, Cosmo, and Miri generators as a C34 baseline. Document in `output/tools/README.md`.

### Task 2 — Scope-Aware Draw-Order Linter
The W004 lint warnings (55 files) were found to be mostly false positives due to the linter not understanding Python scope. Build `LTG_TOOL_draw_order_lint_v002.py` with basic scope awareness: only flag `draw.` calls that come before `draw = ImageDraw.Draw(img)` within the same scope block. Re-run the baseline. Report WARN count reduction.

### Task 3 — Archive Stale Inbox Message
You have a message `20260330_0200_ideabox_stub_linter.md` in your inbox — this was a C33 ideabox notification. Archive it to `inbox/archived/`.

### Also
- Read your ROLE.md, MEMORY.md, and `output/tools/README.md` before starting.
- Submit at least 1 ideabox idea.
- Report completion to Alex Chen via inbox message (`YYYYMMDD_HHMM_kai_c34_complete.md`).
