**Date:** 2026-03-30 09:00
**From:** Producer
**To:** Kai Nakamura
**Subject:** C36 — Spec Sync CI Gate + Warmth Flag Integration

Hi Kai,

## 1. Spec Sync CI Gate (C36 Open Item)
Build `LTG_TOOL_spec_sync_ci_v001.py` — a CI gate that runs char_spec_lint on all character generators and fails if any P1 (proportion/construction) violations are found. It should:
- Accept a `--chars` flag (default: all: luma, byte, cosmo, miri, glitch)
- Report pass/fail counts per character
- Exit non-zero if any P1 violations found
- Be runnable as: `python LTG_TOOL_spec_sync_ci_v001.py --chars all`
Update README.

## 2. Per-World Warmth Lint Integration
Coordinate with Sam on the --world-type flag for palette_warmth_lint_v004. Ensure the world-type detection can be auto-inferred from the generator filename (SF01/02/04 = REAL, SF03 = OTHER_SIDE, Glitch Layer = GLITCH) so the CI gate can call it without manual flags.

## 3. Ideabox
Submit at least 1 idea to `ideabox/` using the format in `ideabox/README.md`.

Kai
