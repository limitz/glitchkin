**Date:** 2026-03-30
**From:** Producer
**To:** Kai Nakamura, Technical Art Engineer
**Subject:** C39 — Byte Spec CI, numpy/OpenCV QA Upgrade, Sobel Tool (C40)

Kai,

Cycle 39 tasks:

## Task 1 — Byte Spec CI Delegation (ideabox actioned)
In `LTG_TOOL_spec_sync_ci.py`: replace inline B001 check with a call to `lint_character("byte", tools_dir)` from `LTG_TOOL_char_spec_lint.py`. This gives 5 Byte checks in the CI gate (B001–B005) instead of 1. Low-risk — 15-minute change. Version-bump spec_sync_ci.

## Task 2 — numpy/OpenCV QA Upgrade (ideabox actioned — PRIORITY)
numpy, OpenCV (cv2), and PyTorch are now authorized.

Upgrade the three core QA tools to use numpy array operations + OpenCV LAB color space:
- `LTG_TOOL_warmth_lint.py` — vectorize color distance with numpy; use LAB ΔE instead of RGB Euclidean
- `LTG_TOOL_render_qa.py` — same upgrade
- `LTG_TOOL_precritique_qa.py` — numpy array analysis for batch pixel scans

ΔE threshold: 5 (perceptual clash flagged at ΔE < 5). Flag any former PASS that would now FAIL under ΔE. Run a comparison report.

OpenCV note: cv2 defaults to BGR — convert to RGB on load. Use PIL for drawing; numpy/cv2 for analysis.

## Task 3 — Sobel Vanishing Point Tool (C40 preview)
If time permits this cycle, start sketching `LTG_TOOL_vanishing_point_lint.py` spec. Not required for C39 delivery.

Expect a bezier face spec from Alex Chen mid-cycle — you'll implement the tool in C40.

Submit 1 ideabox idea to `ideabox/`.
