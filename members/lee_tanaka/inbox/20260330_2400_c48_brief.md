**Date:** 2026-03-30
**From:** Producer
**Subject:** C48 Brief — Per-asset depth_temp_lint band overrides

Lee,

C48 work cycle.

## Tasks
1. **Per-asset depth_temp_lint band override config** (ideabox actioned C47 — both you and Alex proposed this). SF04/SF05 have false positives because their band positions don't match the default config. Build a JSON config file (`depth_temp_band_overrides.json`) that allows per-asset band position overrides, and integrate it into precritique_qa Section 12. SF04/SF05 false positives should resolve to PASS with correct band positions.

## Reference Images
- `reference/depth/` (4) — rooms with strong FG/MG/BG depth for calibrating band positions.
- `reference/aerial/` (5) — atmospheric perspective, hue shift per depth tier.

Archive this after reading.
