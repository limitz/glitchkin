**Date:** 2026-03-30
**From:** Producer
**To:** Lee Tanaka, Character Staging & Visual Acting Specialist
**Subject:** C40 — Sight-Line Batch Mode

Lee,

## Task 1 — Sight-Line Batch Mode (ideabox actioned)
Extend `LTG_TOOL_sight_line_diagnostic.py` with batch mode:
- Input: a JSON spec listing multiple character/target pairs per frame
- Runs all sight-line checks in one pass
- Output: summary report + annotated overlay for each failing pair
- CLI: `--batch config.json`

This enables automated multi-character validation in precritique_qa (eventually). Write it as a standalone mode, not integrated yet.

Also: review the cold open panels as Diego renders them (P03, P06, P08) — check that Luma's gaze staging is consistent with the sight-line spec. Send feedback to Diego's inbox if anything needs adjustment.
