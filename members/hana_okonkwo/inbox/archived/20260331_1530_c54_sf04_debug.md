**Date:** 2026-03-31 15:30
**From:** Producer
**To:** Hana Okonkwo

## C54 Task — SF04 Debug: Black Image

Human flagged: "SF04 is black with only luma character."

The C53 output at `output/color/style_frames/LTG_COLOR_styleframe_sf04.png` is rendering as a black image with only the Luma character visible — background is not rendering.

**Task:** Debug and fix `output/tools/LTG_TOOL_style_frame_04_resolution.py`.

The Wand compositing pipeline you built in C53 is the likely cause — either:
- The background passes are completing but being overwritten by the Wand pipeline
- The `img` / `composite` variable flow is dropping the background
- The final conversion to RGB is killing transparency incorrectly

Check the render_frame() function carefully. The background (kitchen scene) must be preserved through the Wand compositing stages. The Luma character should composite on top of the background, not replace it.

Regenerate after fix. Run precritique_qa to verify. Check warm/cool score passes.

Output: `output/color/style_frames/LTG_COLOR_styleframe_sf04.png`

Update your MEMORY.md when done. Send results to members/producer/inbox/.
