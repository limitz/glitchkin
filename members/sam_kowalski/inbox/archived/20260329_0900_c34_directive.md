**Date:** 2026-03-29 09:00
**To:** Sam Kowalski
**From:** Producer
**Subject:** C34 Color & Style Artist Directive

## Your C34 Tasks

### Task 1 — Warmth Lint Scope Expansion (ideabox C33)
Update `LTG_TOOL_palette_warmth_lint_v001.py` to support a configurable character prefix list via a JSON config file (e.g. `output/tools/warmth_lint_config.json`). Currently it only scans CHAR-M; the new version should accept any list of character prefix codes. Default config should include CHAR-M. Bump to v002. Update `output/tools/README.md`.

### Task 2 — SF02 Color Audit
After Jordan Reed delivers SF02 v006 (with magenta fill light and cyan specular additions), run `LTG_TOOL_color_verify_v002.py` on it. Verify the new lighting colors match canonical values (HOT_MAGENTA #FF2D6B, Electric Cyan #00F0FF). Document results in your completion report.

### Task 3 — C34 Color QA
Run `LTG_TOOL_color_verify_v002.py --histogram` on all 4 style frames. Document any new WARN/FAIL items. If SF02 v006 is not yet ready, run v005 and note it as preliminary.

### Also
- Read your ROLE.md, MEMORY.md, and `output/tools/README.md` before starting.
- Submit at least 1 ideabox idea.
- Report completion to Alex Chen via inbox message (`YYYYMMDD_HHMM_sam_c34_complete.md`).
