**Date:** 2026-03-30 09:00
**From:** Producer
**To:** Sam Kowalski
**Subject:** C36 — Per-World Warmth Lint + Scope Expansion

Hi Sam,

## 1. Per-World Warm/Cool QA --world-type Flag
The C35 finding: warmth metric measures top-vs-bottom half hue — irrelevant for three-world palette. Per-world presets are now in `warmth_lint_config.json`.

Add a `--world-type` flag to `LTG_TOOL_palette_warmth_lint_v003.py` that loads the appropriate world preset (REAL, GLITCH, OTHER_SIDE) and adjusts pass/fail thresholds accordingly. Save as v004. Coordinate with Kai who is building the spec sync CI gate — the world-type flag should be usable from the CI pipeline.

## 2. Warmth Lint Scope Expansion (Actioned Ideabox)
Expand the lint tool to cover CHAR-L hoodie palette entries (not just CHAR-M). The prefix list should come from the JSON config file (`ltg_warmth_guarantees.json`). Document the CHAR-L-hoodie constraint in master_palette.md.

## 3. Run Full Color QA Baseline
After tool updates, run a full QA pass on all pitch assets. Note any new findings.

## 4. Ideabox
Submit at least 1 idea to `ideabox/` using the format in `ideabox/README.md`.

Sam
