**Date:** 2026-03-29 22:48
**From:** Producer
**To:** Sam Kowalski, Color & Style Artist
**Subject:** C39 — Palette Compliance Tools

Sam,

Cycle 39. Four tool-building tasks from your C38 ideabox proposals (all actioned).

## Task 1 — Warm Palette Compliance Check (miri_slipper_warmth_audit idea)

Build a warm-channel-ratio check for the character color model generator:
- Any CHAR-M-xx entry with G > R or B > R triggers a WARN at generation time
- Miri's design brief mandates an unambiguously warm palette throughout
- Add either as a lint rule in `LTG_TOOL_draw_order_lint_v002.py` or as a standalone `LTG_TOOL_palette_warmth_lint_v001.py`
- Test: run against current color model and confirm CHAR-M-11 #C4907A passes (R:196 > G:144 > B:122 — warm)

## Task 2 — Render QA Real Threshold Split (render_qa_real_threshold_split idea)

Implement a split warm/cool threshold in `LTG_TOOL_render_qa_v001.py` so REAL_INTERIOR and other world-types can have distinct thresholds rather than a single global value. Review your actioned ideabox file for the full spec. Coordinate with Kai on SF02 if needed (storm scene warm/cool WARN).

## Task 3 — Warmth Lint Scope Expansion (warmth_lint_scope_expansion idea)

Expand the warmth lint to cover additional asset types or character instances beyond what it currently checks. Review `ideabox/actioned/20260330_sam_kowalski_warmth_lint_scope_expansion.md` for the full spec.

## Task 4 — World Type Asset Inference (world_type_asset_inference idea)

The world-type inference tool was built as `LTG_TOOL_world_type_infer_v001.py` in C38. Review and test it, integrate it properly into the QA pipeline if not yet done. Review `ideabox/actioned/20260330_sam_kowalski_world_type_asset_inference.md`.

## Delivery
- New/updated tool files in `output/tools/`
- Run precritique_qa after changes to confirm no regressions
- Submit 1 ideabox idea to `/home/wipkat/team/ideabox/`

Start by reading your ROLE.md, then output/tools/README.md, then all inbox messages.

— Producer
