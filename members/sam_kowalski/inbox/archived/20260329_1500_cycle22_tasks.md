# Cycle 22 Tasks — Sam Kowalski (Color & Style Artist)

**Date:** 2026-03-29 15:00
**From:** Producer (via Alex Chen)
**Subject:** Critique 10 feedback — color fixes

---

## Critique 10 Feedback (Color)

**Victoria Ashford** + **Naomi Bridges** both flagged SF02 window pane alpha as Priority 1.
Naomi also found a factual error in the color story document (CORRUPT_AMBER value) and a JEANS_BASE documentation gap in SF03.

Naomi grade on color story doc: **B+** (narrative is strongest yet; errors prevent A).

Full critiques: `output/production/critique_c10_victoria_ashford.md`, `output/production/critique_c10_naomi_bridges.md`

---

## Your Cycle 22 Tasks

### Task 1 — SF02 Glitch Storm v005: Window Pane Alpha + CORRUPT_AMBER Fix (CRITICAL)

**Fix 1a: Reduce window pane alpha**
Both Victoria (→ 90-120) and Naomi (→ 110-130) independently flagged this. Use 110-120 as the consensus range.
- Current: win_colors at alpha 160-180
- Fix: `win_colors = [(*SOFT_GOLD, 115), (*WARM_CREAM, 110)]` (or similar in that range)
- DO NOT change glow cone alpha (already correct at 90-110 max)
- Naomi's reasoning: at 160-180, window warm is more saturated than Luma's hoodie, inverting character-warmth hierarchy. Windows support the narrative; they don't compete with the protagonist.

**Fix 1b: Reconcile CORRUPT_AMBER to GL-07**
Color story doc says `#FF8C00` but SF02 generator uses `#C87A20` (200, 122, 32). These are NOT the same.
- `#FF8C00` is the genuine warm-cold complement to Electric Cyan. Produces correct contrast in storm scene.
- `#C87A20` is murky brownish amber that loses contrast. The narrative argument about Byte's outline being "load-bearing" ONLY works with the high-contrast version.
- Fix: update `CORRUPT_AMBER` or equivalent variable in the SF02 generator to `(255, 140, 0)` (#FF8C00)
- Also update `ltg_style_frame_color_story.md` to confirm GL-07 = #FF8C00 as canonical

Output: `LTG_TOOL_style_frame_02_glitch_storm_v005.py` + rendered `LTG_COLOR_styleframe_glitch_storm_v005.png`

### Task 2 — JEANS_BASE Documentation (Naomi P3)

In SF03, JEANS_BASE (38, 61, 90) = #263D5A is 50 points darker on blue channel than canonical CHAR-L-05 #3A5A8C.
Naomi's concern: future Glitch Layer scenes will reference SF03 as spec, and painters will use the wrong jeans value.

Fix: Open `output/color/palettes/master_palette.md` and add an entry that either:
- a. Confirms `#263D5A` as `CHAR-L-05a` (SF03 UV-ambient shadow variant) with explicit physics note, OR
- b. Documents that SF03 uses a UV-ambient jeans shadow value and the canonical jeans base remains `#3A5A8C`

Either path is acceptable — the point is to document the intent so future scenes don't use the wrong value by accident.

### Task 3 — Color Story Document Update

After fixing CORRUPT_AMBER, update `output/color/style_frames/ltg_style_frame_color_story.md`:
- Fix GL-07 value to `#FF8C00` in all references
- Add the three-sentence pitch-deck summary as a standalone callout box:
  *"SF01: This is Luma's world. SF02: Neither world owns this frame. SF03: This is not Luma's world."*
  (Victoria elevated this to pitch-package quality writing — make it prominent)

---

## Deliverables
1. `output/color/style_frames/LTG_TOOL_style_frame_02_glitch_storm_v005.py` + rendered `LTG_COLOR_styleframe_glitch_storm_v005.png`
2. Updated `output/color/palettes/master_palette.md` (JEANS_BASE documentation)
3. Updated `output/color/style_frames/ltg_style_frame_color_story.md`

Report completion to Alex Chen inbox.
