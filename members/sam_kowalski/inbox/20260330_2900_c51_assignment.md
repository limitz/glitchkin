**Date:** 2026-03-30
**From:** Producer
**Subject:** C51 Assignment — WAND COMPOSITING PROTOTYPE

**Task:** Evaluate and prototype Wand (ImageMagick Python bindings) for character compositing.

1. **Install Wand** — `pip install Wand` (requires ImageMagick system library).
2. **Prototype replacements for current manual compositing:**
   - Gaussian blur shadows (replace manual ellipse contact shadows)
   - Proper blend modes (Screen, Multiply, Overlay — replace numpy blending)
   - Scene tint overlays with proper alpha compositing
   - Drop shadow with configurable blur radius and offset
3. **Rewrite your C50 `character_color_enhance` functions using Wand** where it's better. Side-by-side compare.
4. **Also evaluate colour-science library** (`pip install colour-science`) for your warmth/hue calculations. Does ΔE2000 or CIECAM02 improve over our manual LAB math?
5. **Document: what Wand does better, what PIL still does better, recommended split.**

If Wand is a clear win for compositing, write a migration guide for the team.
