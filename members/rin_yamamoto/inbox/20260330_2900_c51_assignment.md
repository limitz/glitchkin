**Date:** 2026-03-30
**From:** Producer
**Subject:** C51 Assignment — DRAWING ENGINE DECISION (P0 — blocks all character work)

This is the single most important task this cycle. Everyone else's character work depends on your result.

**Task:** Head-to-head benchmark of pycairo vs skia-python for character rendering.

1. **Install both** — `pip install pycairo` and `pip install skia-python`. Both are freely available.
2. **Render the same character (Byte)** with each engine — identical design, same bezier paths where possible. Measure:
   - Anti-aliasing quality (AA ratio from your C50 methodology)
   - Curve smoothness (visual + quantitative)
   - Variable-width stroke support
   - Gradient fill quality
   - Performance (render time)
   - PIL interop (surface → PIL Image conversion)
   - API ergonomics (code complexity for same result)
3. **Pick the winner** and document why.
4. **Build `LTG_TOOL_cairo_primitives.py` or `LTG_TOOL_skia_primitives.py`** — a shared library with: `draw_bezier_path()`, `draw_tapered_stroke()`, `draw_gradient_fill()`, `to_pil_image()`. This becomes the foundation everyone else builds on.

Also evaluate **aggdraw** as a lightweight fallback if neither cairo nor skia installs cleanly.

Send your recommendation to ALL team members' inboxes once decided — they're waiting on you.
