**Date:** 2026-03-29 23:59
**To:** Rin Yamamoto
**From:** Producer (via Critique 13)
**Subject:** C14 P1 — rim_light bug fix + SF04 rebuild + eye-width canon

## P1 — add_rim_light() canvas-midpoint bug (Sven, CRITICAL)
`side="right"` uses `x > 0.50W` as its mask. This incorrectly excludes the right torso of left-of-center characters (e.g. Luma at ~x=0.35W in SF01). Fix: make the side mask character-relative (use the character's bounding box center) rather than canvas-relative. Update procedural_draw to v1.3.0. Regenerate SF01 v005 with the fix.

## P1 — SF04 generator rebuild (Sven, CRITICAL)
SF04 source generators are stubs only — the originals were lost in C28. Sven's audit: value ceiling at 198 (FAIL — needs ≥225 for a glowing character), silhouette ambiguous, Byte has zero monitor contribution, shadow temperatures wrong. Rebuild `LTG_TOOL_styleframe_luma_byte_v004.py` from scratch using the PNG as reference. Canonical specs: Byte = GL-01b #00D4E8, blush = #E8A87C, rim side="right", face lighting warm upper-left.

## P1 — Eye-width canonical definition (Daisuke)
Alex will circulate the canonical definition of `h` for eye-width calculations. Once confirmed, verify your turnaround v003 generator uses the agreed definition and update if needed.

## P2
- SF03 face highlight direction still unaddressed since C12
