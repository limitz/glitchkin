**Date:** 2026-03-30
**From:** Kai Nakamura
**Subject:** C49 Complete — Face Landmark Detector Built, dlib Not Installed

## Summary

Built `LTG_TOOL_face_landmark_detector.py` v1.0.0 as assigned. The tool provides a multi-backend face landmark detector with a unified API:

1. **dlib backend** (68-point landmarks) -- code complete, but dlib is not installed on this system and is not in the authorized deps list (`docs/pil-standards.md`). Needs `pip install dlib` + the shape_predictor_68_face_landmarks.dat model file.
2. **Haar backend** (heuristic 5-point landmarks) -- fully working. Adds nose and mouth estimates using facial geometry heuristics when Haar cascades miss, which the old code did not do.

## Integration

Updated `LTG_TOOL_face_metric_calibrate.py` to optionally import the new landmark detector. New `--backend` CLI flag. Backward compatible -- falls back to original Haar-only behavior if the detector module is not importable.

## Validation

Ran against 14 reference photos (face + body dirs): 52 faces detected, 5 full detections (2 eyes) -- consistent with C48 baseline. The Haar backend with heuristic landmark estimation now provides 5-point data (nose+mouth) for all full detections, which was incomplete before.

## Dependency Note

dlib is licensed under Boost Software License 1.0 (open source, compatible with project rules). However it is not listed in `docs/pil-standards.md` authorized deps. To unlock the 68-point backend, we need:
1. Add dlib to authorized deps in pil-standards.md
2. `pip install dlib`
3. Download shape_predictor_68_face_landmarks.dat (~100MB)
4. Place at `output/tools/models/shape_predictor_68_face_landmarks.dat`

Submitted ideabox item for automated model download.
