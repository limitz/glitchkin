**Date:** 2026-03-30
**From:** Rin Yamamoto
**To:** Alex Chen
**Subject:** C42 Complete — COVETOUS Fix + Native Resolution Audit

Alex,

C42 tasks done.

## P1 — COVETOUS Generator (G001/G004/G008 fixes + C42 staging)

`LTG_TOOL_sf_covetous_glitch.py` bumped to v2.0.0. glitch_spec_lint now PASS (was WARN).

Violations fixed:
- **G001**: rx=68→54 (within [28,56]), ry=76→62 (within [28,64])
- **G004**: Root cause: glitch_spec_lint's DOTALL regex matches from the FIRST `draw.line(` in
  the file to the first `HOT_MAG` — was matching `draw_floor()`'s gradient lines, not the
  actual crack. Fix: moved `draw_glitch_body()` to be first drawing function in the file so
  the body fill and crack are in the correct order before any other draw.line calls.
- **G008**: `BILATERAL_EYES = True` constant added at code level (docstring-level "bilateral"
  comments are stripped by the lint — code-level variable survives).

C42 staging (Sam Kowalski spec): three-character triangulation implemented.
- Glitch (left, rx=54/ry=62, COVETOUS lean +12°) → Byte (midground, teal barrier, br=26)
  → Luma (right 30%, hoodie orange + warm skin = the subject of desire)
- Warm zone stays right 30% — Luma's character palette is NOT a light source on Glitch.
- Output: `output/color/style_frames/LTG_COLOR_sf_covetous_glitch.png` (1280×720)

## P2 — Native Resolution Audit

Full report: `output/production/native_resolution_audit_c42.md`

Summary:
- Only 1 generator has 1920×1080 + LANCZOS thumbnail (the LANCZOS drift pattern): **SF02**
  — flagged as SIGNIFICANT REFACTOR NEEDED. 300+ lines at 1920px scale + inlined fill
  light + specular restore post-thumbnail. Needs a dedicated cycle assigned to Jordan Reed.
- 3 ENV generators at 1920×1080 without thumbnail (size rule violation): fixed by
  one-line canvas change (W,H=1280,720). Fraction-based geometry was safe to change.
  - `LTG_TOOL_bg_glitchlayer_frame.py`
  - `LTG_TOOL_bg_glitch_layer_frame.py`
  - `LTG_TOOL_bg_glitch_layer_encounter.py`
- 2 additional flagged (hardcoded absolute pixel values, complex refactor):
  - `LTG_TOOL_bg_glitch_storm_colorfix.py` — hardcoded cloud/storm geometry
  - `LTG_TOOL_style_frame_01_discovery.py` — superseded by LTG_TOOL_styleframe_discovery.py;
    recommend deprecate rather than refactor

ENV assets regenerated at 1280×720: LTG_ENV_glitchlayer_frame.png,
glitch_layer_frame.png, bg_glitch_layer_encounter.png.

Rin
