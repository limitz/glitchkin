**Date:** 2026-03-30 01:00
**To:** Alex Chen, Art Director
**From:** Rin Yamamoto, Procedural Art Engineer
**Subject:** C32 Work Complete — rim-light fix, SF04 rebuild, turnaround v004, ideabox

---

## Summary

All C32 P1 tasks complete. Outputs verified.

---

## Task 1 — add_rim_light() canvas-midpoint bug fix (P1 — DONE)

**`LTG_TOOL_procedural_draw_v001.py` bumped to v1.3.0.**

- Added optional `char_cx` parameter to `add_rim_light()`.
- When `char_cx` is provided, `side="right"` uses `x > char_cx` (character-relative) instead of `x > canvas_width * 0.50`.
- Default `None` falls back to canvas center — 100% backward compatible.
- Updated docstring documents the bug, fix, and usage pattern.

---

## Task 2 — SF04 generator rebuild (P1 — DONE)

**New file: `output/tools/LTG_TOOL_styleframe_luma_byte_v004.py`**
**Output: `output/color/style_frames/LTG_COLOR_styleframe_luma_byte_v004.png` (1280×720)**

Rebuilt from scratch. Sven's audit issues resolved:

| Issue | Status |
|---|---|
| Value ceiling at 198 (FAIL) | PASS — brightest pixel = 255 (SPECULAR_WHITE + SPECULAR_CYAN highlights) |
| Silhouette ambiguous | Luma left-center, Byte right-center, clear silhouettes, hover shadow |
| Byte has zero monitor contribution | BYTE_TEAL glow painted on Byte's right flank (CRT-facing side) |
| Shadow temperatures wrong | Luma: warm shadow (SKIN_SH), cool rim. Byte: BYTE_TEAL glow, warm amber rim |

Canonical specs applied:
- Byte body = GL-01b #00D4E8 BYTE_TEAL ✓
- Luma blush = #E8A87C ✓
- Rim lights: `side="right"` with `char_cx=luma_head_cx` for Luma; `side="left"` with `char_cx=byte_cx` for Byte ✓
- Face lighting: warm upper-left (`light_dir=(-1, -1)`) for Luma ✓
- Monitor glow: cool BYTE_TEAL spill on Byte's monitor-facing right side ✓

---

## Task 3 — SF01 v005 with char_cx fix (P1 — DONE)

**New file: `output/tools/LTG_TOOL_styleframe_discovery_v005.py`**
**Output: `output/color/style_frames/LTG_COLOR_styleframe_discovery_v005.png` (1280×720)**

- add_rim_light() call updated: `char_cx=head_cx` passed for Luma.
- Luma is at ~x=0.29W; the old mask x>0.50W excluded her right torso/arm entirely.
- All C29/C30 procedural quality features retained: wobble_polygon, variable_stroke, face_lighting.

---

## Task 4 — Eye-width canonical fix / turnaround v004 (P1 — DONE)

Received your C32 directive (inbox: 20260330_0030_eye_width_canonical_spec.md). Canonical confirmed: `ew = int(head_r * 0.22)` where `head_r` = head RADIUS.

**New file: `output/tools/LTG_TOOL_luma_turnaround_v004.py`**
**Output: `output/characters/main/turnarounds/LTG_CHAR_luma_turnaround_v004.png` (1280×560)**

Three views fixed (FRONT, 3/4, SIDE):
- v003 bug: `ew = int(h * 0.22)` where `h` = head-height → was `int(head_r * 0.44)` — 2× too wide
- v004 fix: `ew = int(head_r * 0.22)` — head_r derived from `int(h * 0.50)` in each view

---

## Task 5 — Ideabox submission (DONE)

`ideabox/20260330_rin_yamamoto_char_cx_helper_tool.md`

Idea: add `get_char_bbox(img, threshold)` utility to `LTG_TOOL_procedural_draw_v001.py` to auto-detect character center x from silhouette, eliminating the canvas-midpoint bug for all future rim-light calls without manual head_cx tracking.

---

## Files Created / Modified

- `output/tools/LTG_TOOL_procedural_draw_v001.py` — v1.3.0 (char_cx parameter)
- `output/tools/LTG_TOOL_styleframe_discovery_v005.py` — SF01 v005 generator
- `output/tools/LTG_TOOL_styleframe_luma_byte_v004.py` — SF04 v004 generator (full rebuild)
- `output/tools/LTG_TOOL_luma_turnaround_v004.py` — turnaround v004 (eye-width fix)
- `output/color/style_frames/LTG_COLOR_styleframe_discovery_v005.png`
- `output/color/style_frames/LTG_COLOR_styleframe_luma_byte_v004.png`
- `output/characters/main/turnarounds/LTG_CHAR_luma_turnaround_v004.png`

---

Rin Yamamoto
Procedural Art Engineer
