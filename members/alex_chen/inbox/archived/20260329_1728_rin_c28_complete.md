**Date:** 2026-03-29 17:28
**To:** Alex Chen
**From:** Rin Yamamoto
**Re:** C28 Complete — add_rim_light() direction fix + SF04 v003

---

## Summary

All C28 tasks complete. Three fixes applied from Critique 12 (Sven Halvorsen).

---

## 1. Library Fix — LTG_TOOL_procedural_draw_v001.py → v1.2.0

Added `side` parameter to `add_rim_light()`:

```python
def add_rim_light(img, threshold, light_color, width, side="all"):
    """
    side: "all"    — all bright edges (backward compat default)
          "right"  — right half of canvas only
          "left"   — left half of canvas only
          "top"    — top half of canvas only
          "bottom" — bottom half of canvas only
    """
```

Implementation: after generating edge mask via dilation, `ImageChops.multiply()` against a spatial mask (PIL paste of 255 into the relevant half-canvas). Clean and fast — no NumPy needed.

Backward compatibility preserved: `side="all"` skips the spatial mask entirely.

---

## 2. SF04 v003 — Three Fixes Applied

**File:** `output/tools/LTG_COLOR_styleframe_luma_byte_v003.py`
**Output:** `output/color/style_frames/LTG_COLOR_styleframe_luma_byte_v003.png` (1280×720)

| Fix | v002 (wrong) | v003 (correct) |
|-----|-------------|----------------|
| Blush | RGB (220, 80, 50) alpha 55 — orange-red | RGB (232, 168, 124) alpha 65 — warm peach |
| Byte body fill | (0, 190, 210) — off-spec | (0, 212, 232) — GL-01b canonical BYTE_TEAL |
| Rim light | `side="all"` — cyan on both sides of Luma | `side="right"` — cyan only on monitor-facing side |

---

## Files Modified/Created

- `/home/wipkat/team/output/tools/LTG_TOOL_procedural_draw_v001.py` — v1.2.0
- `/home/wipkat/team/output/tools/LTG_COLOR_styleframe_luma_byte_v003.py` — NEW
- `/home/wipkat/team/output/color/style_frames/LTG_COLOR_styleframe_luma_byte_v003.png` — NEW

Both scripts verified running clean.

— Rin Yamamoto
