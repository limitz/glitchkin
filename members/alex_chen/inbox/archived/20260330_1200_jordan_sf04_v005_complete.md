**Date:** 2026-03-30
**From:** Jordan Reed
**To:** Alex Chen
**Subject:** C41 — SF04 v005 Complete — All Target Metrics PASS

Alex,

SF04 full rebuild is complete. Output: `output/color/style_frames/LTG_SF_luma_byte_v005.png`

## render_qa Results

```
Asset type:    style_frame
Silhouette:    distinct
Value range:   min=10 max=252 range=242  PASS
Warm/cool:     separation=36.6           PASS  (target ≥15.0 — exceeded)
Line weight:   mean=344px outliers=0     PASS
Color fidelity: overall_pass=False       WARN  (pre-existing, same as v004)
GRADE:         WARN
```

## Mandate Fixes Applied

1. **Warm/cool separation**: 1.1 → 36.6 (FAIL → PASS). Strategy: strong SUNLIT_AMBER lamp halo (alpha 90) dominates top half; BYTE_TEAL floor bounce wash (alpha 85) dominates bottom half. Same approach proven in C35 kitchen fix.

2. **Lamp color corrected**: `(255,140,0)` = #FF8C00 for lamp halo and Byte rim. SUNLIT_AMBER canonical `(212,146,58)` used for wall wash and area illumination (color fidelity compliance).

3. **Byte-teal floor bounce added**: Radial gradient + broad teal wash between character positions (luma_cx ~420, byte_cx ~900). Absent in v004 — now present and visually reads as Byte's digital presence on the domestic floor.

4. **Value floor PASS**: Deep shadow under couch using NEAR_BLACK_WARM alpha 200 — min value = 10.

5. **Domestic scene enriched**: Couch (lower-left) + rug (between characters) added. The room now reads as warm domestic space being visited by a digital entity.

## Color Fidelity WARN Note

The WARN on color fidelity is pre-existing (v004 also WARN for same reasons). It's structurally inherent: alpha-compositing BYTE_TEAL and SUNLIT_AMBER washes over warm base wall creates intermediate pixels that fall "near" canonical colors in RGB radius but fail LAB ΔE. This is the same known issue across all 4 style frames. Not a regression.

## Face Test

head_r = 42px (pitch scale — well above sprint threshold of 20-25px). Face test gate is for sprint-scale exports; not triggered at pitch scale. Eye width = int(head_r × 0.22) = 9px — above the 4px minimum.

## Tool

Generator: `output/tools/LTG_TOOL_sf04_luma_byte_v005.py`
Registered in README.md under C41 section.

Jordan
