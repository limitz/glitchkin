**Date:** 2026-03-30
**From:** Ryo Hasegawa
**Subject:** C49 Completion — draw_shoulder_arm integration into Cosmo motion generator

## Summary

Integrated the shared `draw_shoulder_arm()` helper (C48) into `LTG_TOOL_cosmo_motion.py`. The Cosmo motion spec sheet now uses proper shoulder involvement per the Shoulder Involvement Rule (image-rules.md C47).

## Changes Made

1. **`LTG_TOOL_cosmo_motion.py`** — replaced inline rectangle arm drawing with shared helper:
   - Imports `draw_shoulder_arm`, `compute_shoulder_shift`, `shoulder_polyline`, `ShoulderArmStyle`
   - `COSMO_ARM_STYLE` constant: clothing="fitted", two_segment=True, elbow_bend_factor=0.10
   - `draw_cosmo_figure()` now calls `draw_shoulder_arm()` for both arms with proper side conventions
   - `shoulder_polyline()` draws the asymmetric torso top edge
   - Shoulder anchor markers placed at shifted positions from `compute_shoulder_shift()`
   - Notebook position now derived from left hand coordinates returned by `draw_shoulder_arm()`

2. **`LTG_TOOL_sheet_geometry_calibrate.py`** — added cosmo sheet path and corrected expected_panels (was 3 placeholder, now 4)

3. **`sheet_geometry_config.json`** — re-calibrated cosmo family from actual sheet; restored miri, miri_v002, glitch families with beat_color and annotation_bg settings that were lost when calibrator overwrote the file

4. **`LTG_TOOL_precritique_qa.py`** — bumped CYCLE_LABEL to C49, version 2.16.1

## Lint Results

- Cosmo: PASS=6 WARN=0 FAIL=0 (was PASS=3 WARN=3 with placeholder config)
- Luma: PASS=5 WARN=1 (unchanged — pre-existing beat_badges WARN)
- Byte: PASS=3 WARN=3 (unchanged — pre-existing dark panel WARNs)
- Glitch: PASS=5 WARN=1 (unchanged)

## Not Done

- Luma and Byte motion generators NOT integrated (marked "if time permits" in brief). Their arm code is structurally different (thick-line vs. rectangle polygon). Would require more invasive refactoring — recommend as separate cycle task.

## Ideabox

Submitted: `20260330_ryo_hasegawa_calibrator_preserve_extra_fields.md`
