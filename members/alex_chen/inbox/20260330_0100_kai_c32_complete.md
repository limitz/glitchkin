**Date:** 2026-03-30 01:00
**To:** Alex Chen, Art Director
**From:** Kai Nakamura, Technical Art Engineer
**Subject:** C32 Work Complete

---

## Summary

All C32 assigned tasks complete. Inbox archived.

---

## P1 — Broken Forwarding Stubs Fixed (8 stubs)

Root cause: C29 `LTG_TOOL_naming_cleanup_v001.py` deleted original `LTG_CHAR_*/LTG_COLOR_*` source files that C28 forwarding stubs still imported from, causing `ModuleNotFoundError` at runtime.

**Fixes applied:**

| Stub | Fix |
|------|-----|
| `LTG_TOOL_luma_expression_sheet_v005.py` | Delegates to `v007.build_sheet()` |
| `LTG_TOOL_luma_expression_sheet_v006.py` | Delegates to `v007.build_sheet()` |
| `LTG_TOOL_luma_turnaround_v002.py` | Delegates to `v003.build_turnaround()` |
| `LTG_TOOL_grandma_miri_expression_sheet_v003.py` | Delegates to `v002.build_sheet()` |
| `LTG_TOOL_cosmo_turnaround_v002.py` | **Full rebuild** — no prior generator; built 4-view Cosmo turnaround from spec |
| `LTG_TOOL_styleframe_luma_byte_v001.py` | Preservation stub (re-saves existing PNG; placeholder if missing) |
| `LTG_TOOL_styleframe_luma_byte_v002.py` | Preservation stub (same pattern) |
| `LTG_TOOL_styleframe_luma_byte_v003.py` | Preservation stub (same pattern; PITCH PRIMARY) |

All 8 stubs verified to run without error.

---

## W004 — Draw Refresh Audit

Reviewed `style_frame_01_discovery_v003` and `styleframe_discovery_v004` (highest W004 counts from C31 linter report). Findings:

- **True W004 bugs** require `img` to be *reassigned* via `img = Image.alpha_composite(...)` while a stale `draw` handle remains in scope.
- **Most of the 55 linter warnings are false positives**: matches in docstring text, helper functions with local draw variables, and `img.paste()` in-place (does not replace the object).
- Added W004 refresh at the one confirmed real bug site in `style_frame_01_discovery_v003` (line ~610).
- Recommend the linter be updated to exclude docstring/comment matches and scope function bodies separately — filed as ideabox idea for C33.

---

## P2 — Cosmo Expression Sheet v004 Fixed

`LTG_TOOL_cosmo_expression_sheet_v004.py` was byte-identical to v003 and output `_v003.png`.

**Changes applied:**
- Output path → `LTG_CHAR_cosmo_expression_sheet_v004.png`
- Title text updated → `v004 | C32: SURPRISED blush`
- SURPRISED expression: `blush: False` → `blush: True`
- Added `BLUSH` + `BLUSH_HI` constants; blush drawing in `draw_cosmo()` — two nested ellipses per cheek for soft blush effect
- Confirmed correct output: `912×946px` at `output/characters/main/LTG_CHAR_cosmo_expression_sheet_v004.png`

---

## Ideabox

Submitted: `ideabox/20260329_kai_nakamura_stub_linter_tool.md`

Proposes `LTG_TOOL_stub_linter.py` — scans `output/tools/` for any stub importing from deleted `LTG_CHAR_*/LTG_COLOR_*` files and reports which are broken and whether a canonical `LTG_TOOL_*` replacement exists. Would prevent the C32 P1 backlog from recurring.

---

Kai Nakamura
Technical Art Engineer
