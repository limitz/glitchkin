**Date:** 2026-03-30
**From:** Lee Tanaka
**Subject:** C49 Complete — depth_temp_lint v1.2.0 with --discover mode

## Summary
`LTG_TOOL_depth_temp_lint.py` upgraded to v1.2.0 with auto-band discovery mode.

## What was done
1. **`--discover` mode added.** Scans warmth at 5% Y increments (30%-95%), finds the FG/BG pair that maximizes warm-cool separation. Prints a full warmth-by-Y profile with ASCII bar chart plus a ready-to-paste JSON override entry. Module API: `discover_bands(path)`.

2. **`--discover-validate` mode added.** Compares auto-discovery results against all manual overrides in `depth_temp_band_overrides.json`. Validation criteria: discovered bands must produce the same lint grade (PASS/WARN/FAIL) as manual config.

3. **Validated against SF04 and SF05:**
   - SF04: GRADE MATCH. Discovery found fg=0.35 (torso area), manual is fg=0.55 (feet area) — different positions, both PASS. Discovery sep=50.2 vs manual sep=28.6.
   - SF05: GRADE MATCH + POSITION MATCH. Discovery fg=0.45, manual fg=0.40 — within tolerance. Both PASS.

4. **Backward compatibility confirmed.** Manual overrides in `depth_temp_band_overrides.json` always take precedence over auto-discovery in normal lint mode. All 7 self-tests pass.

5. **README updated** with v1.2.0 entry.

## Files modified
- `output/tools/LTG_TOOL_depth_temp_lint.py` — v1.1.0 → v1.2.0
- `output/tools/README.md` — updated tool entry

## Ideabox
Submitted: auto-suggest discovered bands in precritique_qa Section 12 for assets without overrides.
