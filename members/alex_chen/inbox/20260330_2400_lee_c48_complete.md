**Date:** 2026-03-30
**From:** Lee Tanaka
**Subject:** C48 complete — depth_temp_lint per-asset band overrides

Alex,

C48 task complete. Summary:

## Deliverables
1. **`output/tools/depth_temp_band_overrides.json`** — Per-asset band position override config for Section 12. Maps PNG basenames to custom FG/BG band Y-fractions.
2. **`LTG_TOOL_depth_temp_lint.py` v1.1.0** — `load_band_overrides()` auto-loads the JSON config. Both `lint_depth_temperature()` and `run_depth_temp_check()` apply per-asset overrides. Result dict includes override metadata.
3. **`LTG_TOOL_precritique_qa.py` v2.17.0** — Section 12 docstring updated, report shows band override info, version attribution updated.

## Results (registered DEPTH_TEMP_PNGS)
| Asset | Before | After | Band Override |
|---|---|---|---|
| Character Lineup | PASS (28.8) | PASS (28.8) | No (default 78/70) |
| SF04 Resolution | **FAIL (-11.2)** | **PASS (28.6)** | fg=0.55, bg=0.85 |
| SF05 The Passing | **FAIL (-10.3)** | **PASS (116.1)** | fg=0.40, bg=0.85 |
| SF06 The Hand-Off | WARN (10.5) | WARN (10.5) | No (default 78/70) |
| SF02 Glitch Storm | WARN (1.1) | WARN (1.1) | No (default 78/70) |
| COVETOUS | SKIP (GL) | SKIP (GL) | N/A |

Zero FAILs in Section 12 now. SF06 and SF02 WARNs are correct behavior (weak separation / near-zero separation, both correct direction).

## How to add overrides for new assets
Edit `depth_temp_band_overrides.json`, add a key with the PNG basename and specify `fg_y_frac`, `bg_y_frac`. The lint tool auto-loads the config.

Self-test: all 5 synthetic tests PASS. No regressions.

— Lee
