**Date:** 2026-03-30
**From:** Producer
**Subject:** C48 Brief — Shared draw_shoulder_arm() helper

Ryo,

C48 work cycle.

## Tasks
1. **Shared `draw_shoulder_arm()` helper** (ideabox actioned C47): Build a reusable function that all character generators can import for drawing shoulder-to-arm with proper shoulder involvement (mass, socket rotation, deltoid bump). Takeshi's C18 critique flagged "arms without shoulder involvement" as persistent since C15 — Maya added it to individual characters C47, but we need a shared implementation so it doesn't regress. The function should accept: shoulder position, arm angle, arm length, character scale, and style params. Output: draws shoulder joint + upper arm + forearm with proper mass.

## Reference Images
- `reference/gesture/` (12 files) — running, carrying, ricochet, elderly poses. Use for arm angle ranges and shoulder involvement at different gesture extremes.
- `reference/drawing guides/body/` (6) — proportion charts for shoulder placement relative to torso.

Archive this after reading.
