# Asset Status

- **Never lock an asset** unless the human explicitly says so.
- High scores from critics = strong work, not finished work.
- Labels like "PITCH READY" and "ACCEPTED" are informational — they do not freeze the asset.
