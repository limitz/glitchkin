**Date:** 2026-03-30 02:00
**From:** Producer
**To:** Alex Chen, Art Director
**Re:** Cycle 21 — Final Pre-Critique 10 Pass + Kai Nakamura Onboarding

Alex,

This is the last work cycle before Critique 10. Two priorities:

## TEAM CHANGE

Lee Tanaka is temporarily deactivated (storyboard decks complete). New team member: **Kai Nakamura, Technical Art Engineer**. Kai's first cycle focuses on building the shared rendering library and applying procedural improvements to key outputs.

## TASK 1 — Onboard Kai Nakamura

Send Kai a detailed first-cycle brief (to `kai_nakamura/inbox/`). Kai should:
1. Read all team ROLE.md files and the production bible for context
2. Survey the existing tools in `output/tools/` — identify the top 3 patterns that repeat and should be in a shared library
3. Build `output/tools/ltg_render_lib.py` — shared utilities module covering at minimum:
   - `noise_texture(width, height, scale, seed, octaves)` — Perlin-style noise for texture generation
   - `gaussian_glow(img, radius, color, alpha)` — add a glow effect to an image layer
   - `light_shaft(draw, x0, y0, x1, y1, width_near, width_far, color, alpha, feather)` — draw a light shaft trapezoid with feathered edge
   - `dust_motes(draw, region, count, seed, color, alpha_range)` — scatter dust particles in a region
   - `catenary(draw, x0, y0, x1, y1, sag, color, width)` — draw a sagging cable/wire
4. Apply `ltg_render_lib` to at least ONE existing generator as a proof of concept (e.g., regenerate Tech Den v003 using the shared library)

## TASK 2 — Final Pitch Package Audit

Before Critique 10, do a full pass of `output/production/pitch_package_index.md`. For each major section:
- Confirm the file actually exists (spot check 5-6 key files)
- Flag any "Needs Work" items that are still open
- Write a one-page "Pitch Package Readiness" summary: `output/production/pitch_readiness_c21.md`

## TASK 3 — Archive + MEMORY

Archive the rehire message and cycle 20 completion reports. Update MEMORY.md.

—Producer
