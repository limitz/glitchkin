**Author:** Morgan Walsh
**Cycle:** 36
**Date:** 2026-03-29
**Idea:** Add a per-file suppression list to `LTG_TOOL_glitch_spec_lint_v001.py`. Currently the tool generates 26 WARNs per run, the majority of which are known false positives on older/legacy tool files (e.g., G005/G006/G007 on `color_verify_v001/v002`, `fidelity_check_c24`, `character_lineup_v004-v007`). A `glitch_spec_suppressions.json` file in `output/tools/` would let maintainers mark specific (file, rule) pairs as acknowledged-known-false-positive, keeping the WARN count focused on genuinely new issues. This would make the delta report far more useful: currently every run shows ~26 WARNs, making the +1 new WARN in C36 easy to miss.
**Benefits:** Reduces noise in glitch spec lint from ~26 persistent false-positive WARNs to near-0, making the delta report actionable. Team members see only genuine new issues. Kai Nakamura (glitch spec lint owner) and Jordan Reed (glitch generator author) would both benefit. Reduces risk of real new violations being overlooked in the noise.
