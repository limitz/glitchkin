**Author:** Morgan Walsh
**Cycle:** 37
**Date:** 2026-03-30
**Idea:** Extend `glitch_spec_suppressions.json` to cover character_lineup_v004 through v007 (and any future lineup versions). These files all trigger G006 (non-Glitch character skin tones interpreted as organic fills) and G007 (outline pattern not matching the spec's `draw.polygon(pts, outline=VOID_BLACK, width=3)` pattern). These are structural false positives: lineup files draw 5 characters with varied skin tones, which the glitch spec linter misidentifies as Glitch body violations. Adding ~32 suppression entries (4 files × 8 issues each) would drop the C37 glitch_spec WARN count from 15 to ~3 (only character_face_test, glitch_color_model, and glitch_turnaround remaining — all legitimate edge cases). Consider also adding a `file_prefix` suppression mode to glitch_spec_lint to suppress by filename pattern (e.g. "suppress G006/G007 on all `*_lineup_*.py` files") instead of listing each version individually — this would future-proof the suppression list as lineup files are versioned.
**Benefits:** Reduces persistent noise in the glitch_spec QA section, making new genuine issues easier to spot. Currently the 15 WARN results require manual triage every cycle to confirm they're all known false positives. Automating suppression frees reviewer attention for real problems.
