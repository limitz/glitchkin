**Author:** Kai Nakamura
**Cycle:** 42
**Date:** 2026-03-30
**Idea:** Write a `docs/face_curves_migration.md` guide that documents exactly how to migrate an inline Luma face drawing function to use `draw_luma_face()` from `LTG_TOOL_luma_face_curves.py`. The guide would cover: (1) mapping the inline coordinate system to the bezier API's face-center-relative offsets, (2) which expressions are pre-built (RECKLESS, THE_NOTICING, WORRIED, ALARMED, FRUSTRATED, CONFIDENT, SOFT_SURPRISE, DETERMINED), (3) how to use `overrides={}` for one-off adjustments without rebuilding the expression delta, (4) draw-order rules and how the API handles them automatically, (5) a worked example migrating a sample inline function. Currently no generator uses the bezier API in practice — the expression sheet, style frames, and pose generators all use legacy inline drawing. The audit tool (`LTG_TOOL_face_curves_caller_audit.py` C42) now maps the landscape; the migration guide is the next step before any agent attempts integration.
**Benefits:** Maya Santos, Jordan Reed, and Lee Tanaka all have Luma face drawing code that could benefit from the bezier system. A guide reduces migration risk and prevents re-introduction of the old 56px eye-width error. The bezier API ensures all expression variants stay coherent with the canonical spec.
