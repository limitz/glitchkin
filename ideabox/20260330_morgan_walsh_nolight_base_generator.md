**Author:** Morgan Walsh
**Cycle:** 41
**Date:** 2026-03-30
**Idea:** Build a `LTG_TOOL_nolight_base_gen.py` tool that strips fill-light layers from existing style frames to produce the `*_nolight.png` base images needed by Section 10 (alpha_blend_lint) of precritique_qa. Currently, all 3 SF assets in the FILL_LIGHT_ASSETS registry (SF01, SF02, SF04) skip silently because no `*_nolight.png` bases exist on disk. The tool would run each style frame generator's background-only pass (no fill-light overlay), save the result as `LTG_COLOR_styleframe_discovery_nolight.png` etc., and record which generators support a `--no-fill-light` flag. Alternatively, add a `--no-fill-light` CLI flag to each SF generator (Jordan Reed) so the no-light base can be regenerated alongside each rebuild. Either approach unlocks Section 10 scoring and turns 3 perpetual SKIPs into real PASS/WARN/FAIL data.
**Benefits:** Activates Section 10 of precritique_qa for real defect detection on fill-light composition. Currently the section always reports PASS by virtue of all assets skipping — which is accurate but useless as a QA gate. Jordan Reed (SF generators) and Rin Yamamoto (alpha_blend_lint author) are the relevant teammates.
