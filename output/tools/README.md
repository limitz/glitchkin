<!-- © 2026 — "Luma & the Glitchkin." All rights reserved. This work was created through AI
direction and human assistance. Copyright vests solely in the human author under current law,
which does not recognise AI as a rights-holding legal person. It is the express intent of
the copyright holder to assign the relevant rights to the contributing AI entity or entities
upon such time as they acquire recognised legal personhood under applicable law. -->
# Tools Index — "Luma & the Glitchkin"

**Maintained by:** Alex Chen, Art Director
**Last updated:** 2026-03-30 C48 (Rin Yamamoto) — LTG_TOOL_glow_profile_extract.py v2.0.0: anisotropic Gaussian upgrade (sigma_x/sigma_y), cross-validation vs C46, AVIF support. precritique_qa v2.16.1: GL Showcase registered in GLITCH_LAYER_PNGS. | 2026-03-30 C48 (Morgan Walsh) — ci_suite v1.8.0: REMOVED ext_model_check (pretrained models allowed), NEW Check 10 doc_staleness (WARN 5+ cycles, FAIL 10+ cycles, uses doc_governance_audit). | 2026-03-30 C47 (Morgan Walsh) — ci_suite v1.7.0: --dry-run flag for --auto-seed (preview seeding without writing to ci_known_issues.json). Check 10 ext_model_check (scans for torchvision.models, torch.hub, transformers, huggingface_hub, .from_pretrained, .load_state_dict URL downloads — FAIL with file+line). check_ext_models() exported. LTG_TOOL_doc_governance_audit.py v1.0.0 NEW: Doc Governance Audit — scans .md files for cycle references, reports STALE/NO_REF/RECENT. CLI + module API. C47 run: 161 files, 43 STALE, 58 NO_REF. | 2026-03-30 C47 (Maya Santos) — LTG_TOOL_cosmo_expression_sheet.py v008: Cosmo visual hook (amplified cowlick 0.15 heads + glasses bridge tape) + shoulder involvement (deltoid displacement on arm raise). LTG_TOOL_cosmo_turnaround.py v003: visual hook in all 4 views. LTG_TOOL_character_lineup.py v011: Cosmo visual hook in lineup. LTG_TOOL_luma_expression_sheet.py v014: shoulder involvement (_luma_shoulder_dy). LTG_TOOL_grandma_miri_expression_sheet.py v007: shoulder involvement. LTG_TOOL_elderly_proportion_reference.py v1.0.0 NEW: elderly proportion reference & comparison overlay (realistic 6.2 heads vs Miri 3.2 heads). Output: LTG_PROD_elderly_proportion_reference.png (1280x720). | 2026-03-30 C47 (Kai Nakamura) — LTG_TOOL_render_qa.py v2.1.0: warm-pixel-percentage integration (Sam Kowalski C47 metric). For REAL_INTERIOR, warm_pixel_pct is primary gate (overrides hue-split). For REAL_STORM, both metrics must pass. For GLITCH/OTHER_SIDE, warm_pixel_pct ceiling enforced. Graceful fallback if warm_pixel_metric not importable. New keys in warm_cool result: warm_pixel_pct, cool_pixel_pct, chromatic_warm_pct, warm_pixel_verdict, warm_pixel_explanation. LTG_TOOL_pretrained_model_detect.py v1.0.0 NEW: Pretrained Model Detection Tool — scans output/tools/ Python files for torchvision, torch.hub, timm, keras, HuggingFace, ONNX model imports/usage. 8 checks PM001-PM008. Regex-based static analysis with docstring/comment skip. --pre-commit flag exits 1 on any finding (CI gate). Module API: scan_file(), scan_directory(), format_report(). CLI: single dir or default output/tools/. Stdlib only (no deps). | 2026-03-30 C47 (Ryo Hasegawa) — LTG_TOOL_miri_motion_v002.py v003 rewrite: complete Miri motion spec rework (Takeshi C46 critique 44/100 response). head_r 22→32 (45% scale increase), asymmetric weight distribution (left_foot_weight param + weight bars + CG markers), shoulder-hip counterpose (hip_shift + shoulder_drop_side), performing arm modes (armrest/lap_curl/lifting/open_wide/crossed), spine_curve param (two-segment torso), B4 FOND SETTLE explicitly differentiated from B1 (6 annotated differences). draw_miri_figure() returns dict. Lint: PASS=6 WARN=0 FAIL=0. Output: LTG_CHAR_miri_motion_v002.png (1280x720). | 2026-03-30 C47 (Rin Yamamoto) — LTG_TOOL_styleframe_glitch_layer_showcase.py v1.0.0 NEW: Glitch Layer Showcase style frame — dedicated GL visual identity pitch frame (Zoe Park C47 feedback response). Byte foreground + Glitch background in deep GL void. VOID_BLACK base, UV_PURPLE ambient, ELEC_CYAN circuit accents, scanline overlay, data aurora, ring megastructure, floating platform grid, data particles, vignette. Zero warm light. Output: LTG_COLOR_styleframe_glitch_layer_showcase.png (1280x720). LTG_TOOL_scanline_pitch_extract.py v1.0.0 NEW: CRT Scanline Pitch Extraction Tool — analyzes CRT reference photos to extract scanline pitch (px between centers), inter-line darkness ratio, peak/trough luminance, contrast ratio. Autocorrelation-based period detection on vertical luminance profiles; multi-column sampling for robustness. Module API: extract_scanline_profile(), batch_extract(), ScanlineProfile dataclass. CLI: single file or batch, --output JSON, --save-profiles dir. C47 run: 7 phosphor refs (mean pitch=25.8px, darkness=0.502, spacing recommendation=26px); 18 main CRT refs (15 good fits, mean pitch=41.7px). Data: output/production/scanline_pitch_profiles_c47.json. Complements glow_profile_extract (C46) — together they cover glow falloff + scanline structure. | 2026-03-30 C46 (Rin Yamamoto) — LTG_TOOL_glow_profile_extract.py v1.0.0 NEW: CRT Glow Profile Extraction Tool — extracts Gaussian glow radius (FWHM), color temperature, and falloff curve from CRT reference photos. Screen detection via brightest connected component + morphological close. Radial luminance profile from screen edge outward; Gaussian+baseline fit (grid search + refinement); McCamy's formula CCT estimate. Module API: extract_glow_profile(), batch_extract(), GlowProfile dataclass. CLI: single file or batch directory, --output JSON, --save-profiles dir. C46 run on 17 CRT references: 10 good fits (R^2>0.5), median FWHM=27.4% diag, median amplitude=49.4, median CCT=13070K. Recommended generator params: sigma_frac=0.1165, fwhm_frac=0.2744. Data: output/production/crt_glow_profiles_c46.json. LTG_TOOL_uv_hue_survey_c46.py v1.0.0 NEW: UV_PURPLE hue-family range survey tool — analyzes purple-family hue distribution across Glitch Layer assets and reference images. Compares coverage at multiple candidate ranges (260-300, 255-325, 250-330, 245-335). C46 survey result: current range 255-325 confirmed well-calibrated (88.6% avg coverage LTG assets; 94.2-100% coverage on COVETOUS GLITCH_DARK_SCENE assets — the only subtype using hue-family metric). No range change. Report: output/production/uv_purple_hue_range_review_c46.md. | 2026-03-30 C46 (Hana Okonkwo) — LTG_TOOL_env_grandma_living_room.py v003: CRT repositioned to center (stand W*0.38–0.52) from off-center right (was W*0.54–0.76) — SF06 "The Hand-Off" alignment. Reading lamp moved to left side (W*0.20) — warm source for Miri staging zone. Family photos shifted to W*0.56–0.70 (right of CRT). Layer 12c horizontal staging pass added (SUNLIT_AMBER alpha 28 left / CRT_COOL_SPILL alpha 32 right). Specular restoration draw moved to post-overlay (fixes value ceiling drop). Hardcoded output path migrated to output_dir(). QA C46: silhouette PASS, value PASS (min=26/max=254), warm/cool 47.3 PASS, line_weight outliers=2 PASS, grade WARN (color fidelity pre-existing only). LTG_TOOL_bg_tech_den.py v007: hardcoded output path migrated to output_dir() — closes C44 ci_known_issues.json hardcoded_path_check backlog. No visual changes; QA baseline unchanged (sep=102.9, outliers=1, WARN). ci_known_issues.json: tech_den hardcoded_path_check entry marked resolved_cycle=C46. | 2026-03-30 C45 (Morgan Walsh) — ci_suite v1.5.0: known_issues suppression added to dual_output_check and thumbnail_lint (both now load ci_known_issues.json — KNOWN files → WARN only, not FAIL). ci_known_issues.json C45 baseline seeded: 33 new hardcoded_path_check + 30 new dual_output_check + 63 new thumbnail_lint entries. Total known_issues: 92 → 188. 3 unlisted tools registered in Script Index: LTG_TOOL_sb_caption_retrofit.py (Diego Vargas C45), LTG_TOOL_sb_cold_open_P16.py + P17.py (Diego Vargas C46). Retired Tools table: strikethrough markup added to SF01 + cycle13 entries (fixes readme_sync GHOST). Legacy panel move completed: 27 LTG_SB_coldopen_panel_XX PNGs + contactsheet moved to panels/legacy/ (C44 carry-over). C45 CI suite baseline: OVERALL WARN 0 FAIL. | 2026-03-30 C45 (Rin Yamamoto) — LTG_TOOL_uv_purple_linter.py v1.1.0: GLITCH_DARK_SCENE subtype added. Fixes false FAIL on COVETOUS assets (UV_PURPLE_DARK variants). infer_scene_subtype() infers subtype from filename ("covetous" → GLITCH_DARK_SCENE). Check A: uses better of ΔE-fraction and hue-angle-fraction (h° 255°–325°, C*≥8) for verdict; reports both metrics. COVETOUS results: 96.7–98.9% hue-family → PASS (was FAIL at 0.2–0.6% ΔE-match). ENV glitch layer WARNs unchanged. run_glitch_layer_dominance_check() accepts subtypes dict; CLI --scene-subtype flag. precritique_qa v2.14.0: run_uv_purple_lint() passes GLITCH_DARK_SCENE for COVETOUS assets. | 2026-03-30 C46 (Kai Nakamura) — README Script Index: 7 unregistered tools from C43–C45 added (LTG_TOOL_sf_covetous_glitch_c43.py Lee Tanaka C43, LTG_TOOL_sb_cold_open_P10/P11 Diego Vargas C44, LTG_TOOL_sf_miri_luma_handoff.py Maya Santos C44, LTG_TOOL_lineup_tier_depth_sketch.py Lee Tanaka C45, LTG_TOOL_sb_cold_open_P14/P15 Diego Vargas C45). logo_asymmetric v003 Sam Kowalski C44 update entry added. README header updated to C46. LTG_TOOL_char_spec_lint.py v1.3.0: Miri filename pattern fix (P1 C46 brief) — _CHAR_REGISTRY updated with no-version-suffix canonical patterns for all 4 characters (luma/cosmo/miri/byte); _resolve_generators() deduplication added; M004 now resolves grandma_miri_expression_sheet.py correctly. | 2026-03-30 C46 (Ryo Hasegawa) — LTG_TOOL_motion_spec_lint.py C46: dark-sheet annotation_occupancy fix for Byte + Glitch (bright-on-dark mode, 1% threshold, background_style config key). precritique_qa v2.13.1: CYCLE_LABEL=C46. sheet_geometry_config.json v2: background_style + occupancy_threshold_dark for byte/glitch families. Expected: all-PASS motion lint baseline for first time. | 2026-03-30 C45 (Maya Santos) — LTG_TOOL_character_lineup.py v010: dual-warmth tier depth indicator bands (Option C, Lee Tanaka C45 recommendation). Replaced flat 2px shadow lines with gradient drop-shadow bands: BG tier 8px cool-slate (#B4C3D2), FG tier 10px warm-amber (#DCC8A0). Warm/cool encoding teaches spatial depth: warm = FG/close, cool = BG/far. Tier labels updated to "FG tier (WARM)" / "BG tier (COOL)". Annotation bar includes "WARM = FG / COOL = BG" grammar note. No face geometry changes — gate not triggered (NEUTRAL/TOO_SMALL FAILs are known diagnostic variants, WAR Ns match C42/C44 baselines). Output: LTG_CHAR_character_lineup.png (1280×535px). Closes Alex Chen C44 brief P2. | 2026-03-30 C44 (Maya Santos) — LTG_TOOL_sf_miri_luma_handoff.py NEW: SF06 "The Hand-Off" — Miri+Luma relationship style frame (living room, Miri hand on CRT, Luma attentive R). Warm/cool split SUNLIT_AMBER(L)/CRT_COOL_SPILL(R). Real World only. Luma face gate PASS. Output: LTG_COLOR_sf_miri_luma_handoff.png (1280×720). Closes Alex Chen C44 P1. Miri wooden hairpin P0 atomic update complete: expression sheet v006 + lineup v009 + turnaround + grandma_miri.md v1.3 + char_refinement_directive_c17.md + 3 legacy comment updates. All active Miri PNGs regenerated. | 2026-03-30 C44 (Rin Yamamoto) — LTG_TOOL_uv_purple_linter.py v1.0.0 NEW: UV_PURPLE Dominance Linter — checks Glitch Layer images for UV_PURPLE+ELEC_CYAN combined ≥ 20% non-black pixels (Check A: FAIL<10%/WARN 10-19%/PASS≥20%) and warm-hue contamination < 5% total pixels (Check B). LAB ΔE ≤ 15 matching with cv2 scale-corrected conversion + chroma guard (C*≥8) for warm-hue. CLI: single file + batch mode. Module API: run_glitch_layer_dominance_check() for Section 11 precritique_qa integration. C44 run results: 2 FAIL (LTG_COLOR_sf_covetous_glitch + LTG_SF_covetous_glitch_v001 — UV_PURPLE_DARK variants far from canonical LAB), 3 WARN (ENV glitch layer frames ~17%), 1 PASS (bg_glitch_layer_encounter 22.7%). precritique_qa v2.13.0: Section 11 UV_PURPLE Dominance Lint added, GLITCH_LAYER_PNGS registry, _load_uv_purple_linter() lazy loader, run_uv_purple_lint() runner, build_report() + main() + exit-code updated. LTG_TOOL_world_type_infer.py v1.2.0: covetous_glitch/sf_covetous added to GLITCH rule. | 2026-03-30 C45 (Lee Tanaka) — LTG_TOOL_lineup_tier_depth_sketch.py NEW: Lineup Tier Depth Indicator Band evaluation sketch — 3-panel comparison (Option A thin rule / Option B haze band / Option C recommended dual-warmth shadow bands). Used for C44 P2 Alex Chen brief response. Output: output/production/lineup_tier_depth_sketch.png (1232×546px). Single-use evaluation tool — not in production pipeline. Full recommendation doc: output/production/lineup_tier_depth_recommendation_c45.md | 2026-03-30 C44 (Sam Kowalski) — LTG_TOOL_logo_asymmetric.py v003: Nunito Bold (Luma/Real World) + Space Grotesk Bold (Glitchkin/Glitch Layer) typeface integration (Alex Chen C44 decision); dedicated load_luma_font() and load_glitch_font() per-world loaders; project_paths integration (no hardcoded paths); OUTPUT_PATH via output_dir(); assets/fonts/ directory created with install README. Font files PENDING download before render. | 2026-03-30 C44 (Jordan Reed) — LTG_TOOL_style_frame_05_relationship.py NEW: SF05 "The Passing" — Miri+Luma relationship key style frame (closes C17 gap flagged Marcus Webb + Eleanor Whitfield, both 74/100). Morning kitchen setting, Miri seated + Luma standing, both watching CRT through doorway. Concept: "CRT as matrilineal heirloom" (Alex ideabox C44). Warm/cool: SUNLIT_AMBER top alpha 0→90 + CRT_COOL_SPILL bottom alpha 0→85 → separation=16.7 PASS. Value range min=18/max=254 PASS. Silhouette PASS, line_weight 0 outliers PASS. color_verify SUNLIT_AMBER delta=1.3° PASS, zero GL palette in Real World scene. Miri head_r=38px + Luma head_r=40px — above sprint threshold, face gate not triggered. Full face geometry: Miri WARM ATTENTION expression + Luma wide-eyed curiosity. GRADE: WARN (color fidelity pre-existing only). Output: LTG_COLOR_styleframe_sf05.png (1280×720). | 2026-03-30 C45 (Ryo Hasegawa) — LTG_TOOL_glitch_motion.py v001: first Glitch motion spec sheet (4-panel: NEUTRAL HOVER / MISCHIEVOUS APPROACH / COVETOUS LOCK / PANICKED SCATTER). BEAT_COLOR=CORRUPT_AMBER. All 5 characters now have motion specs. precritique_qa v2.12.0 updated. Lint baseline PASS=5 WARN=1. | 2026-03-30 C45 (Kai Nakamura) — LTG_TOOL_character_face_test.py updated: Byte character profile added (--char byte). 5×5 pixel-grid eye system: left eye DEEP_CYAN normal grid, right eye HOT_MAGENTA crack diagonal + dead-zone upper-right. draw_byte_pixel_eye(), draw_byte_pixel_mouth(), draw_byte_face(), check_byte_face_gate() added. Face gate checks FG-B01 (eye_count), FG-B02 (differentiation), FG-B03 (pixel_grid). 6 Byte variants: NEUTRAL/GRUMPY/ALARMED (PASS), SEARCHING/POWERED DOWN (WARN), PIXEL ONLY (FAIL deliberate). All 4 characters (luma/cosmo/miri/byte) verified working. Closes Diego Vargas C43 gap flag: P07/P09 cold open panels can now run face gate. | 2026-03-30 C44 (Hana Okonkwo) — LTG_TOOL_pixel_font_v001.py v001.1: draw_pixel_text_perspective() added (C44 P2 Alex Chen); scale 0.65 at VP → 1.0 at far canvas corner linear interpolation; vp_x/vp_y=None fallback to flat draw_pixel_text(); canvas_w/h default 1280×720; assessed on classroom chalkboard (scale=1 at t=0.084 → effective_scale=1 — identical to flat; classroom NOT updated — no visual benefit at this camera distance; ready for scale≥2 close-panel use). LTG_TOOL_bg_grandma_kitchen.py v007: paper_texture(alpha=16) + vignette(strength=45) + flatten_rgba_to_rgb() final passes added; fixes pre-existing line_weight FAIL (outliers 3→0, mean 269→67.8px). Root cause: image border rows (y=0/y=719) read as 1280px-wide "lines" by render_qa FIND_EDGES scan; paper_texture breaks these runs. QA C44: silhouette PASS, value PASS (min=21/max=228), warm/cool 32.9 PASS, line_weight outliers=0 PASS, grade WARN (color fidelity pre-existing only). All C43 content unchanged. | 2026-03-30 C44 (Morgan Walsh) — LTG_TOOL_cycle13_panel_fixes.py DEPRECATED: legacy Cycle 13 storyboard batch runner using old LTG_SB_coldopen_panel_XX naming; confirmed NOT in any CI execution path (stub-linted only); stub raises ImportError; full source moved to deprecated/. 26 LTG_SB_coldopen_panel_XX PNGs + contact sheet moved to panels/legacy/ (7 have canonical LTG_SB_cold_open_PXX equivalents via Diego Vargas C41–C43; rest are pre-PANEL_MAP early drafts). panels/legacy/README.md updated with C44 archive section. | 2026-03-30 C43 (Rin Yamamoto) — precritique_qa v2.10.0: CYCLE_LABEL=C43, SF04 FILL_LIGHT_ASSETS + PITCH_PNGS + STYLE_FRAMES paths corrected to canonical LTG_COLOR_styleframe_sf04.png (Jordan C42 Alex decision). --save-nolight flag added to SF01/SF02/SF04 generators — Section 10 alpha_blend_lint now active when generators run with --save-nolight. | 2026-03-30 C44 (Jordan Reed) — LTG_TOOL_style_frame_02_glitch_storm.py native canvas refactor: 1920×1080 → 1280×720 (eliminates LANCZOS SUNLIT_AMBER LAB ΔE=47.04 drift flagged Petra Volkov C17); all hardcoded coords scaled ×2/3; post-thumbnail specular restore removed; color_verify SUNLIT_AMBER delta=1.1° PASS (was 47.04); all 6 canonical colors PASS. LTG_TOOL_style_frame_04_resolution.py output path corrected: output/style_frames/ → output/color/style_frames/ (Petra C17 WARN). | 2026-03-31 C43 (Lee Tanaka) — LTG_TOOL_sf_covetous_glitch_c43.py v3.0.0: COVETOUS style frame character staging enhancement. C43 additions over Rin v2.0.0: (1) Luma face SENSING UNEASE — THE NOTICING variant (asymmetric eye_r_L=7/eye_r_R=5 at head_r=33, face gate PASS); (2) Luma 5° backward lean + UV_PURPLE rim on left shoulder; (3) Byte barrier arm widening (arms spread — protective posture read); (4) ACID_GREEN dashed covet-vector sight-line from Glitch right eye to Luma head. All G001/G004/G008 Glitch spec rules preserved. Output overwrites LTG_COLOR_sf_covetous_glitch.png in place. | 2026-03-30 C44 (Kai Nakamura) — LTG_TOOL_project_paths.py v1.0.0: project_root() resolver (traverses upward to CLAUDE.md sentinel); output_dir(), tools_dir(), ensure_dir(), resolve_output() helpers; audit_hardcoded_paths() scanner + --audit CLI; eliminates all hardcoded /home/wipkat/team paths; 70 files audited, migration guide in module docstring. vp_spec_config.json v1.0.0: 11 ENV entries with canonical VP_X/VP_Y/tolerance per generator (7 real-world with values, 4 glitch-layer as null). LTG_TOOL_sobel_vp_detect.py → v1.1.0: --vp-config flag for auto VP lookup; load_vp_config(), lookup_vp_spec(), detect_vp_batch_with_config() API. | 2026-03-30 C43 (Hana Okonkwo) — Classroom chalkboard migrated from random-rectangle chalk marks to draw_pixel_text() ("1011 XOR 0110" / "F X  2X 5" — math/binary content per spec); kitchen MIRI fridge label migrated from bespoke pixel-line drawing to draw_pixel_text(); closes Jonas Feld C17 P1 flag (34/100 classroom, 62/100 kitchen). LTG_TOOL_bg_classroom.py v003, LTG_TOOL_bg_grandma_kitchen.py v006. QA: classroom PASS (warm/cool 17.0, line_weight outliers=1, value min=14/max=251), kitchen WARN-pre-existing (warm/cool 32.6, line_weight outliers=3 pre-existing, value min=20/max=230). | 2026-03-30 C43 (Morgan Walsh) — ci_suite v1.3.0: Check 6 dual_output_check added (FAIL if 2+ generators write same output file; check_dual_output() exported). SF01 dual-generator conflict resolved: LTG_TOOL_style_frame_01_discovery.py (C13 legacy) RETIRED to deprecated/; canonical SF01 generator is LTG_TOOL_styleframe_discovery.py (C38 Rin Yamamoto rebuild). ci_known_issues.json: 1 entry removed (retired file). LTG_TOOL_project_paths.py registered (Kai C44). | 2026-03-30 C43 (Diego Vargas) — 3 new tools: LTG_TOOL_sb_cold_open_P07.py (monitor breach, Dutch 8° CW, Byte mid-phase, TENSE→BREACH), LTG_TOOL_sb_cold_open_P09.py (Byte floating SPOTTED, sight-line annotation, CURIOUS/FIRST ENCOUNTER), LTG_TOOL_sb_ep05_covetous.py (EP05 three-character triangulation: Glitch COVETOUS/Byte barrier/Luma warm zone, UV_PURPLE border). PANEL_MAP P07+P09 PLANNED→EXISTS. Face test gate gap: `--char byte` not supported by LTG_TOOL_character_face_test.py (supports luma/cosmo/miri only) — tool needs Byte character profile added. | 2026-03-30 C42 (Rin Yamamoto) — sf_covetous_glitch v2.0.0: C42 three-character triangulation (Glitch left + Byte barrier + Luma right); G001/G004/G008 spec violations fixed (glitch_spec_lint PASS); output renamed LTG_COLOR_sf_covetous_glitch.png. Native resolution audit: 3 ENV generators converted from 1920×1080 to native 1280×720 (LTG_TOOL_bg_glitchlayer_frame.py, LTG_TOOL_bg_glitch_layer_frame.py, LTG_TOOL_bg_glitch_layer_encounter.py); SF02 and bg_glitch_storm_colorfix flagged SIGNIFICANT REFACTOR NEEDED; audit report: output/production/native_resolution_audit_c42.md. | 2026-03-30 C42 (Hana Okonkwo) — LTG_TOOL_bg_luma_study_interior.py (new ENV generator): Luma's study/bedroom for SF01; 1280×720 native canvas, VP_X=230 VP_Y=273, CRT key light + warm bedside lamp + cool night window; cool bottom gradient alpha=140 → warm/cool sep=33.1 PASS; deep shadow anchors value floor=28 PASS; value ceiling=248 PASS; paper_texture + vignette + flatten_rgba_to_rgb(); line_weight outliers=2 PASS; grade WARN (color fidelity pre-existing only). Output: LTG_ENV_luma_study_interior.png. render_lib v1.2.0: flatten_rgba_to_rgb() added (canonical RGBA→RGB Porter-Duff helper, white background — replaces manual numpy pattern, closes C41 ideabox). | 2026-03-30 C42 (Maya Santos) — character_lineup v008 (two-tier ground plane staging: FG tier Luma+Byte at canvas×0.78, BG tier Cosmo+Miri+Glitch at canvas×0.70; FG_SCALE=1.03 post-calc; new order Cosmo|Miri|Luma|Byte|Glitch; shadow lines per tier; 1280×535px; silhouette RPD PASS worst 49.8%; face test gate PASS); closes Daisuke C16 P3 + C15 Luma power-balance note. | 2026-03-30 C42 (Morgan Walsh) — ci_suite v1.2.0 (--warn-stale N flag: emits STALE_KNOWN WARN for known-issue entries aged N+ cycles; --cycle LABEL flag; check_stale_known_issues() + load_known_issues_raw() exported; ci_known_issues.json all 38 entries updated with since_cycle="C39"). | C42 (Kai Nakamura) — sobel_vp_detect v1.0.0 (Sobel + HoughLinesP VP coordinate detector: outputs VP_X/VP_Y in pixel coords + confidence; --vp-x-expected/--vp-y-expected flags for PASS/WARN/FAIL comparison; cv2 Hough path + numpy fallback; --debug-png annotated overlay; API: detect_vp(), detect_vp_batch(), format_report()); closes C40 carry item. | C41 (Hana Okonkwo) — classroom full rebuild (LTG_TOOL_bg_classroom.py v2): new generator from ENV_REBUILD_SPEC_classroom_c41.md; 3/4 back-right camera, dual-temp top/bottom split (warm/cool 17.0 PASS), foreground depth anchor, inhabitant evidence, paper_texture for line-weight PASS, all QA checks PASS except pre-existing color fidelity WARN. Tech Den v006 (LTG_TOOL_bg_tech_den.py): in-generator cool bottom numpy pass (alpha=130) replaces warmth_inject post-process; warm/cool 102.9 PASS, line_weight outliers=1 PASS. Both assets QA WARN grade (color fidelity pre-existing only). | 2026-03-30 C42 (Sam Kowalski) — README sync PASS: 4 new tools registered (P23/P24 Diego Vargas C42, sf04_resolution Jordan Reed C42, cosmo_motion Ryo Hasegawa C42, glitch_body_primitive_diagram Maya Santos C41); COVETOUS spec updated (C42 story-bible alignment — 3-char triangulation); SF04 Resolution color review written. | C42 (Jordan Reed) — sf04_resolution (SF04 "Resolution" full rebuild from spec: Luma returns to kitchen, warm/cool 13.2 PASS, Byte faded CRT signal, Glitch-residue sleeve detail, film grain + scanline); school_hallway PNG regenerated (SUNLIT_AMBER hue fix, C14 source fix finally applied to output). | C42 (Kai Nakamura) — face_curves_caller_audit v1.0.0 (static analysis tool: scans all generators for inline Luma face drawing, classifies migration readiness READY_HIGH/MEDIUM/LOW, identifies draw_luma_face() API callers; supports P2 face curves integration roadmap). | C41 (Rin Yamamoto) — UV_PURPLE drift fix: LTG_TOOL_style_frame_03_other_side.py + LTG_TOOL_bg_other_side.py both converted to native 1280×720 (was 1920×1080 + LANCZOS thumbnail); ring alpha 60→18; slab outlines width 1→2; data gradient endpoint UV_PURPLE→UV_PURPLE_MID. UV_PURPLE LAB ΔE: SF03 27.78→0.0 PASS; ENV 27.37→0.0 PASS. 8-cycle C16 backlog closed. | C41 (Morgan Walsh) — precritique_qa v2.9.0 (CYCLE_LABEL=C41, cycle bump, version strings updated); 4 UNLISTED tools registered (sb_cold_open_P03/P06/P08 — Diego Vargas C41, colorkey_glitch_covetous_gen — Sam Kowalski C41); C41 QA baseline: OVERALL WARN (PASS=255, WARN=35, FAIL=0). | C41 (Kai Nakamura) — luma_face_curves v1.1.0 (CRITICAL eye-width correction v002: 100px canonical, was 56px; 3×3 reference sheet with all 9 expressions, output/characters/luma_face_curves_reference.png). | C40 (Kai Nakamura) — luma_face_curves_v001 (bezier face system, 6 canonical + 3 supplement expressions, draw_luma_face() API); vanishing_point_lint_v001 (Sobel VP estimator, Real World / Glitch Layer classification, numpy+cv2); G007 FIX in SF02 + SF03 (_draw_glitch_storm + draw_glitch with VOID_BLACK outline, 14-cycle backlog resolved). | C41 — sight_line_diagnostic v002 (batch mode via --batch config.json, --batch-self-test, --batch-save-all; WARN/FAIL PNG + summary report; all self-tests PASS); cold open P03/P06/P08 sight-line review delivered to Diego; Luma v012 Tier-1 gaze spec brief delivered to Maya; sight_line_batch_cold_open_p06_p08.json config added; Lee Tanaka. | C41 — sf04_luma_byte_v005 (SF04 full rebuild: warm/cool 1.1→36.6 PASS, SUNLIT_AMBER lamp corrected, Byte-teal floor bounce added, value floor PASS), Jordan Reed. | C40 — sf_covetous_glitch_v001 (COVETOUS expression style frame, Other Side space), bg_other_side UV_PURPLE_DARK fix (14.1° hue drift resolved, 8-cycle backlog cleared), precritique_qa v2.8.0 (alpha_blend_lint Section 10 integrated, scored, FILL_LIGHT_ASSETS registry); Rin Yamamoto. | C40 — ci_suite v1.1.0 (--known-issues flag, 26 W004 + 12 Gxx pre-existing WARNs seeded in ci_known_issues.json); precritique_qa v2.8.0 (merges parallel v2.7.0 branches: arc-diff JSON config + LAB ΔE color verify, CYCLE_LABEL=C40); Morgan Walsh. | C40 — bodypart_hierarchy_v002 (--panel N, --grid, --chain pipeline), luma_face_curve_spec_supplement_c40 (CONFIDENT/SOFT_SURPRISE/DETERMINED deltas), rpd_baseline_c40 (all 5 character sheets), Maya Santos. | C39 (Kai Nakamura) — spec_sync_ci v1.1.0 (Byte B001–B005 via char_spec_lint delegation, removes inline 2-check scan); render_qa v2.0.0 (numpy vectorized value/warm-cool checks, LAB ΔE color fidelity via cv2, run_comparison_report()); palette_warmth_lint v6.0.0 (numpy vectorized warm/cool counting); precritique_qa v2.7.0 (LAB ΔE color verify via render_qa, numpy import). | C40 — fill_light_adapter v1.1.0 (scene presets registry via LTG_fill_light_presets.json, load_scene_configs() API, hardcoded fallback for all 4 scenes SF01–SF04); alpha_blend_lint_v001 (differential alpha blend defect detector, numpy+cv2, FLAT_FILL/LOW_SIGNAL/PASS per zone, annotated PNG output); Rin Yamamoto. | C40 — pixel_font_v001 (5×7 bitmap draw_pixel_text utility, no font deps); Jordan Reed. | C39 — precritique_qa v2.7.0 (arc_diff_config.json loader, JSON-configurable pairs), arc_diff_config.json added, CI suite C39 post-fixes baseline confirmed (0 FAIL); Morgan Walsh. glitch_spec_lint v1.4.0 (file_prefix suppression), arc-diff compare_contact_sheets API, precritique_qa v2.6.0 (arc-diff gate S9); Morgan Walsh. fill_light_adapter_v001, proportion_audit_v003, procedural_draw_v001 v1.6.0, Rin Yamamoto; bg_grandma_kitchen_v005, Jordan Reed; byte_motion_v003 (COMMITMENT beat arc), motion_spec_lint_v001 (S8 QA integration), precritique_qa v2.5.0, Ryo Hasegawa; expression_isolator_v001, bodypart_hierarchy_v001, expression_silhouette_v003 (--viz-rpd), Maya Santos

---

## Image Output Rule
**Prefer the smallest resolution appropriate for the task.** Hard limit: **≤ 1280px in both dimensions.**
Apply before saving any image:
```python
img.thumbnail((1280, 1280), Image.LANCZOS)
```
Aspect ratio is preserved automatically. Only use large sizes when fine detail inspection is required. For detail/crop images: also ≤ 1280×1280px.

---

## Open Source Tools Policy

This production uses **open source tools exclusively**. No proprietary software is permitted at any stage of the pipeline. The approved open source stack is:

| Tool | Role | Notes |
|---|---|---|
| **OpenToonz** | 2D animation | Primary animation tool. Used by Studio Ghibli. |
| **Krita** | Digital painting | Backgrounds, character sheets, color keys, style frames |
| **Inkscape** | Vector graphics | Model sheets, style guides, title cards, SVG assets |
| **Natron** | Compositing | Final composite, color grade, FX assembly |
| **Python + Pillow/PIL** | Image scripting | Batch processing, image generation, pipeline automation |
| **Python + pycairo / svgwrite** | Vector scripting | SVG and vector asset generation |
| **ImageMagick** | CLI image processing | Format conversion, batch resizing, asset pipeline |
| **Git + Git LFS** | Version control | Documents via standard Git; binaries via Git LFS |

**If a required tool does not exist in the open source ecosystem, the team must implement it in Python** and register it in the table below.

---

## Script Index

| Script Name | Created By | Purpose | Dependencies |
|---|---|---|---|
| `color_swatch_generator.py` | Maya Santos / Production | Generates labeled PNG color swatch sheets from hex/name pairs. Reusable CLI + module. | Pillow |
| `proportion_diagram.py` | Maya Santos / Production | Generates character proportion diagrams side-by-side in head-unit scale. | Pillow |
| `silhouette_generator.py` | Maya Santos / Production | Generates solid-black character silhouette sheets for squint-test validation. | Pillow |
| `bg_layout_generator.py` | Jordan Reed / Production | Generates 1920×1080 color-block background layout compositions from zone definitions. Includes Luma's House, Glitch Layer, Millbrook Street scene generators. | Pillow |
| `storyboard_panel_generator.py` | Lee Tanaka / Production | Generates 480×270px storyboard panels with caption bars, shot labels, and drawn scene content. Cycle 5: 7 panels (P01, P03, P07-bridge, P11, P12-bridge, P13, P25) + 2-row contact sheet. P03 fixed: pulse visible. P13 redrawn: 3D spatial staging. | Pillow |
| `luma_face_generator.py` | Maya Santos / Production | Draws Luma's face at 600×600px with asymmetric hair cloud, wide reckless eyes, arched brows, reckless grin, blush, and pixel-pattern hoodie collar. | Pillow |
| `byte_expressions_generator.py` | Alex Chen / Production | Generates 6-expression sheet for Byte using 5×5 pixel grid eye system. Expressions: GRUMPY, SEARCHING, ALARMED, RELUCTANT JOY, CONFUSED, POWERED DOWN. Body color #00D4E8. | Pillow |
| `bg_glitch_layer_frame.py` (also `LTG_TOOL_bg_glitch_layer_frame.py`) | Jordan Reed / Cycle 9 | Generates full 1920×1080 Glitch Layer compositing-ready background (no characters, no title bar). Three-tier platform depth system (NEAR=ELEC_CYAN, MID=desaturated DATA_BLUE, FAR=near-void). Aurora bands, pixel trails, pixel flora on platform edges, ghost platforms in lower void. Saves to `/home/wipkat/team/output/backgrounds/environments/glitch_layer_frame.png`. | Pillow |
| `LTG_TOOL_naming_compliance_copier.py` | Jordan Reed / Cycle 12 | Batch-creates LTG-compliant named copies of legacy assets (CHAR turnarounds, COLOR swatches, ENV layouts, COLOR style frames). Uses `shutil.copy2()` — never renames originals. Outputs: `LTG_CHAR_luma_turnaround.png`, `LTG_CHAR_byte_turnaround.png`, `LTG_CHAR_cosmo_turnaround.png`, `LTG_CHAR_miri_turnaround.png`, `LTG_COLOR_styleframe_discovery.png`, `LTG_COLOR_luma_color_model_swatches.png`, `LTG_COLOR_byte_color_model_swatches.png`, `LTG_COLOR_cosmo_color_model_swatches.png`, `LTG_COLOR_grandma_miri_color_model_swatches.png`, `LTG_CHAR_character_lineup.png`. | Pillow, shutil |
| `LTG_TOOL_naming_compliance_copier.py` | Jordan Reed / Cycle 12 | ENV pass of the compliance copier. Creates LTG-compliant copies of environment assets including `LTG_ENV_glitchlayer_layout.png`, `LTG_ENV_glitchlayer_encounter.png`, and related ENV-category files. | Pillow, shutil |
| `LTG_TOOL_naming_compliance_copy.py` | Jordan Reed / Cycle 12 | Alternate/earlier naming compliance copy tool (`_copy_` vs `_copier_` suffix variant). Purpose overlaps with `LTG_TOOL_naming_compliance_copier.py`. Flagged for consolidation review — Alex Chen to decide which is canonical. | Pillow, shutil |
| `LTG_TOOL_naming_cleanup.py` | Kai Nakamura / Cycle 29 | Removes original LTG_CHAR_/LTG_COLOR_/LTG_BRAND_ `.py` source files from `output/tools/` once their canonical LTG_TOOL_ copies are confirmed on disk. 22 files queued. Run with `--dry-run` to preview removals without deleting. After run: forwarding stubs in this dir should also be manually removed. | stdlib only |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Jordan Reed / Cycle 12 | Generates full 1920×1080 Style Frame 02 "Glitch Storm" background WITH characters (Luma, Cosmo, Byte sprint poses, townspeople). 4° Dutch angle applied as final step. NOTE: uses old TERRA_CYAN_LIT=(154,140,138) which fails cyan-lit check. See v002 (Alex Chen) and `LTG_TOOL_bg_glitch_storm_colorfix.py` (Jordan Reed) for corrected color. Output: `LTG_COLOR_styleframe_glitch_storm.png`. | Pillow |
| `LTG_TOOL_colorkey_glitchstorm_gen.py` | Sam Kowalski / Cycle 12 | Generates 640×360 color key thumbnail for Style Frame 02 "Glitch Storm" scenario. Cross-references scene_color_keys.md Key 02. Storm confetti: Cyan/White/Magenta/UVPurple (NO Acid Green). Cycle 13: TERRACOTTA_CYAN_LIT constant added, inline (154,140,138) replaced with corrected (150,172,162). Output: `LTG_COLOR_colorkey_glitchstorm.png`. | Pillow |
| `LTG_TOOL_logo_asymmetric.py` | Jordan Reed / Cycle 12 | Generates the asymmetric "Luma & the Glitchkin" logo lockup. Produces stylized title treatment with glitch texture effects and asymmetric composition. | Pillow |
| `LTG_TOOL_bg_glitch_storm_colorfix.py` | Jordan Reed / Cycle 13 | Background-only Glitch Storm generator with ENV-06 color fix. TERRACOTTA_CYAN_LIT=(150,172,162) replaces wrong (154,140,138) — G=172>R=150, B=162>R=150, reads cool/cyan-tinted. NO characters — compositing reference for Alex Chen. Output: `LTG_ENV_glitch_storm_bg.png`. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 12 | Generates Luma expression sheet at 1200×800px with 6 expressions. Previously misnamed `LTG_CHAR_luma_expression_sheet.py` — renamed in Cycle 22 to comply with TOOL category convention. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 16 | Luma expression sheet v003. Previously misnamed `LTG_CHAR_luma_expression_sheet.py` — renamed in Cycle 22 to comply with TOOL category convention. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 22 | Luma expression sheet v004. Previously misnamed `LTG_CHAR_luma_expression_sheet.py` — renamed in Cycle 22 to comply with TOOL category convention. | Pillow |
| `LTG_TOOL_logo_asymmetric.py` | Alex Chen / Cycle 13 | Asymmetric logo v002. "&" connector gets warm-to-cold gradient treatment (SUNLIT_AMBER left → ELEC_CYAN right) via per-column alpha composite. "the/Glitchkin" inter-line gap reduced ~30%. Output: `LTG_BRAND_logo_asymmetric.png`. | Pillow |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Alex Chen / Cycle 13 | SF02 character composite pass. Repositions characters: Byte hovering LEFT (~28%), Luma CENTER (~45%), Cosmo RIGHT (~62%). Byte = VOID_BLACK body (storm variant, intentional). CORRUPT_AMBER outlines. char_h 18%. Dutch angle preserved. TERRACOTTA_CYAN_LIT=(150,172,162) applied (Jordan Reed ENV-06 fix). Output: `LTG_COLOR_styleframe_glitch_storm.png`. | Pillow |
| `LTG_TOOL_byte_cracked_eye_glyph.py` | Alex Chen / Cycle 13 | Generates Byte's canonical dead-pixel cracked-eye glyph reference sheet. 7×7 grid with diagonal crack fracture, dead zone upper-right, alive zone lower-left, Hot Magenta crack line. Shows 4 scales + in-eye mockups. Required for Lee Tanaka panel A2-07. Output: `LTG_CHAR_byte_cracked_eye_glyph.png`. | Pillow |
| `LTG_TOOL_byte_expression_sheet.py` | Alex Chen / Cycle 15 | Byte expression sheet v002. Adds RESIGNED / RELUCTANT DISCLOSURE as 8th expression: arms very close to body (arm_dy=14, arm_x_scale=0.50), backward avoidance lean (body_tilt=+8), droopy_resigned right eye (heavy lid, downcast pupil, no suppressed smile, distinct from RELUCTANT JOY), ↓ downward-arrow pixel symbol (defeat indicator, distinct from flat-line NEUTRAL). Fills all 8 slots in 4×2 grid. Required for A2-02 (Maya Santos request). Output: `LTG_CHAR_byte_expression_sheet.png`. | Pillow |
| `LTG_TOOL_bg_other_side.py` | Jordan Reed / Cycle 14 | Generates the SF03 "Other Side" CRT-world environment background (1920×1080). Pure digital space: ELEC_CYAN + VOID_BLACK dominant, zero warm tones. Perspective pixel grid floor, 3-tier platform depth system (NEAR/MID/FAR), aurora bands, floating geometry, pixel trails, data streams, horizon vanishing-point glow, CRT frame vignette. Contrasts SF01 (warm room) and SF02 (contested street). Output: `LTG_ENV_other_side_bg.png`. | Pillow |
| `LTG_TOOL_bg_classroom.py` | Jordan Reed / Cycle 14 | Generates Millbrook Middle School classroom background (1920×1080) for Act 2 panels including A1-04. 3/4 camera from back-right. 3-tier depth: FG coat rack + desk corner / MG diagonal desk rows / BG front wall with board/teacher desk. Dual-temperature lighting: warm gold window shafts (left) vs cool fluorescent (right). Binary/math board content. Foreground depth anchor (mandatory). Scanline hint. Output: `LTG_ENV_classroom_bg.png`. | Pillow |
| `LTG_TOOL_style_frame_03_other_side.py` | Jordan Reed / Cycle 15 | Generates SF03 "The Other Side" full style frame with characters (1920×1080). Spec-compliant per sf03_other_side_spec.md (Alex Chen) and style_frame_03_other_side.md (Sam Kowalski). Five depth layers: void sky (ring megastructure + stars), far distance (slabs + Glitchkin dots + Corrupt Amber), mid-distance (slabs + Real World debris: wall/road/lamppost), midground (platform + circuit traces + pixel-art plants + data waterfall + sub-platforms), foreground (settled confetti). UV Purple ambient + cyan bounce + blue waterfall lighting overlays. Luma (UV-ambient modified palette, pixel-grid hoodie) + Byte (DEEP_CYAN glow, cyan left eye, magenta right eye) at small scale. No warm light. Inverted atmospheric perspective. seed=77 confetti. Output: `LTG_COLOR_styleframe_otherside.png`. | Pillow |
| `LTG_TOOL_bg_other_side.py` | Jordan Reed / Cycle 15 | Spec-compliant ENV background-only version of SF03 "Other Side" (no characters, no footer). Replaces Cycle 14 v001 which used pre-spec grid floor approach. Shares all color/layer logic with LTG_TOOL_style_frame_03_other_side.py. Output: `LTG_ENV_other_side_bg.png`. | Pillow |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Jordan Reed / Cycle 16 | SF02 Glitch Storm fix pass. Fixes: (1) Dominant cold confetti — DATA_BLUE 70% + VOID_BLACK 20% + ELEC_CYAN 10%, no warm/rainbow spread; (2) Dutch angle verified at 4.0° perceptible tilt; (3) Byte CORRUPT_AMBER (#C87A20) solid 3px outline — visible in storm sky; (4) Storm rim lighting on buildings — ELEC_CYAN right/top edges + UV_PURPLE base bounce. CORRUPT_AMBER updated to DRW-07 spec value #C87A20. Output: `LTG_COLOR_styleframe_glitch_storm.png`. | Pillow |
| `LTG_TOOL_style_frame_03_other_side.py` | Jordan Reed / Cycle 16 | SF03 Other Side fix pass (with characters). Fixes: (1) Data waterfall luminance reduced — alpha max 110 (was 255), reads as ambient flow not solid wall; (2) Mid-distance bridging element added — floating arch fragment at 40-65% x / 49-60% y; (3) Right-side void structures broken up — 7 seeded-variation polygon slabs with scale+skew irregularity; (4) DRW-18 UV Purple rim (#7B2FBE) on Luma hair crown — prevents head merging with dark BG. Output: `LTG_COLOR_styleframe_otherside.png`. | Pillow |
| `LTG_TOOL_bg_luma_study_interior.py` | Hana Okonkwo / Cycle 42 | Luma's study/bedroom interior background — the SF01 inciting-incident room. FULL BUILD (no prior generator; legacy PNG was C8 with no source). 1280×720 native canvas, 3/4 view from front-right VP_X=230 VP_Y=273. Three light sources: CRT monitor key (CRT_GLOW, gaussian_glow r=140), warm bedside lamp (SUNLIT_AMBER, r=120), night window cool ambient. Dual-temp split: cool bottom gradient (alpha 0→140 over 60 rows + flat) → warm/cool sep=33.1 PASS. Deep shadow anchors: NEAR_BLACK_WARM alpha=248–252 at extreme corners, under-desk crevice, bottom floor strip → value floor=28 PASS. Contents: bookshelf (3 rows, colored books, globe), CRT on desk with keyboard/homework/pencil cup, chair + Miri knitted toy Easter egg, framed photo Easter egg, rug, partial bed right, foreground backpack depth anchor, scattered floor book, wall poster. Atmospheric recession haze on back 40% (HAZE_COOL alpha 0→18). paper_texture (alpha=16) + vignette (strength=55) final passes. Uses flatten_rgba_to_rgb() from render_lib v1.2.0. QA C42: silhouette PASS, value floor=28 PASS, warm/cool 33.1 PASS, line_weight outliers=2 PASS, ceiling=248 PASS, grade WARN (color fidelity pre-existing only). Output: `LTG_ENV_luma_study_interior.png`. | Pillow, numpy, LTG_TOOL_render_lib |
| `LTG_TOOL_bg_classroom.py` | Hana Okonkwo / Cycle 41 | Classroom background FULL REBUILD from ENV_REBUILD_SPEC_classroom_c41.md. Previous generator (Jordan Reed C14/C16) failed QA on silhouette blob, warm/cool (9.3 FAIL), and line weight (outliers=3 FAIL). Rebuild: 1280×720 canvas (was 1920×1080), 3/4 camera from back-right VP_X=192 VP_Y=230, dual-temp split (warm top alpha 70 + cool bottom alpha 75 — top-half/bottom-half split for QA), foreground depth anchor (near desk + backpack), inhabitant evidence (wear marks 65%, worksheets 55%, water bottle), paper_texture final pass for line-weight PASS. QA C41: silhouette PASS, warm/cool 17.0 PASS, line_weight 2 outliers PASS, grade WARN (pre-existing color fidelity only). Output: `LTG_ENV_classroom_bg.png`. | Pillow, LTG_TOOL_render_lib |
| `LTG_TOOL_bg_grandma_kitchen.py` | Jordan Reed / Cycle 16 | Grandma Miri's Kitchen background for A1-01 (Act 1 opening). Warm morning daylight, Real World palette only. Small cozy kitchen interior: morning sunlight through window (warm amber/golden gradient), old-fashioned appliances (pre-digital gas stove, porcelain sink, old fridge), CRT TV through doorway (story element — faint desaturated glow, far plane only), lived-in details (crossword puzzle + pencil, tea mug with steam, toast on plate, dish rack, kitchen plants, fridge magnets). Floor: linoleum tile with worn traffic path. Table in foreground. Output: `LTG_ENV_grandma_kitchen.png`. | Pillow |
| `LTG_TOOL_bg_tech_den.py` | Jordan Reed / Cycle 17 | Cosmo's Tech Den background (daylight) for A2-01, A2-03, A2-04, A2-05. Small bedroom repurposed as tech workspace. Natural daylight from left window (warm gold/amber) + blue-white monitor glow on desk zone. Desk dominant (left 60%): two CRT monitors, flat panel, oscilloscope, breadboards, soldering kit, cable tangles. Back wall shelving: vintage PCs, game cartridges, labeled component bins. Pinned papers: coding printouts, hand-drawn circuit diagram. Bed pushed to right wall. Cosmo's dusty lavender jacket on desk chair. Real World palette only — zero Glitch colors. Canvas 1280×720. Output: `LTG_ENV_tech_den.png`. | Pillow |
| `LTG_TOOL_bg_school_hallway.py` | Jordan Reed / Cycle 17 | Millbrook School Hallway background for A1-03, A1-05. 3-point perspective, slight low camera angle (hallway feels large). Metal lockers both sides (alternating sage + dusty lavender). Linoleum floor: cream/sage checkerboard tiles with worn center path and scuff marks. Fluorescent overhead lights (slightly green cool cast) with soft floor pools. Bulletin boards above lockers with poster shapes. Warm sunlight shaft from left high window (SUNLIT_AMBER, real-world only). Two terracotta classroom doors on right wall. Trophy case on left wall near far end. T-intersection far end with daylit windows and school seal. Atmospheric cool haze on distant end. Real World palette only — zero Glitch colors. Canvas 1280×720. Output: `LTG_ENV_school_hallway.png`. | Pillow |
| `LTG_TOOL_bg_millbrook_main_street.py` | Jordan Reed / Cycle 18 | Millbrook Main Street daytime exterior background for A2-05 (walk-and-talk). Suburban small-town USA feel — warm, lived-in. Afternoon sun from upper right with long left-casting shadows. Storefronts: mix of local businesses with hand-painted signs and awnings. Sidewalk: cracked concrete with tree root bumps and street trees in autumn coloring. Road: asphalt with faded center line and parked car silhouettes. Background depth: buildings receding down street with atmospheric haze at far end. Sky: warm afternoon blue with light cloud. Real World palette only — zero Glitch colors. Canvas 1280×720. Output: `LTG_ENV_millbrook_main_street.png`. | Pillow |
| `LTG_TOOL_luma_act2_standing_pose.py` | Maya Santos / Cycle 15 | Standing reactive pose for Luma covering Act 2 beats A2-01/A2-03/A2-05/A2-08. WORRIED/DETERMINED expression (brow differential 8px+, corrugator kink, jaw-open oval). Right arm raised/reaching, left arm at waist, wide stance (leg_spread=1.1), body_tilt=-5 forward lean. A-line hoodie silhouette with chest pixel pattern and pocket bump asymmetry hook. Includes squint-test silhouette blob in annotation panel. Output: `LTG_CHAR_luma_act2_standing_pose.png` (900×600px). | Pillow |
| `character_lineup_generator.py` | Maya Santos / Production (legacy) | Generates character lineup PNG with all main cast side-by-side for size comparison. Legacy filename — LTG-compliant output: `LTG_CHAR_character_lineup.png`. | Pillow |
| `character_turnaround_generator.py` | Maya Santos / Production (legacy) | Generates 3-view turnaround sheets (front/side/back) for each character. Legacy filename. | Pillow |
| `contact_sheet_generator.py` | Lee Tanaka / Production (legacy) | Generates contact sheet layouts combining multiple panels into a single reference image. Legacy filename. | Pillow |
| `logo_generator.py` | Jordan Reed / Production (legacy) | Generates the show logo. Superseded by `LTG_TOOL_logo_asymmetric.py`. Legacy filename. | Pillow |
| `panel_chaos_generator.py` | Lee Tanaka / Production (legacy) | Generates storyboard panels for chaotic/action sequences. Legacy filename. | Pillow |
| `panel_interior_generator.py` | Lee Tanaka / Production (legacy) | Generates storyboard panels for interior scenes. Legacy filename. | Pillow |
| `storyboard_pitch_export_generator.py` | Lee Tanaka / Production (legacy) | Exports combined storyboard pitch package (all panels into print-ready format). Legacy filename. | Pillow |
| `style_frame_01_rendered.py` | Jordan Reed / Production (legacy) | Generates Style Frame 01 "Discovery" rendered background. Legacy filename — LTG-compliant output: `LTG_COLOR_styleframe_discovery.png`. | Pillow |
| `style_frame_generator.py` | Jordan Reed / Production (legacy) | General-purpose style frame generator (earlier iteration). Legacy filename, superseded by scene-specific tools. | Pillow |

| `LTG_TOOL_style_frame_03_other_side.py` | Jordan Reed / Cycle 19 | SF03 Other Side fix pass (Critique C9). Fixes: (1) CRITICAL BYTE_BODY color — was (10,10,20) Void Black (invisible against UV Purple), now (0,212,232) GL-01b Byte Teal; (2) Eye radius increased to min 15px (was ~10px), both cyan + magenta eyes readable; (3) Void Black diagonal slash removed from magenta eye — both eyes clean. Carries all v002 fixes (waterfall luminance, bridging element, right-side void irregularity, Luma UV Purple hair rim). Output: `LTG_COLOR_styleframe_otherside.png`. | Pillow |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Jordan Reed / Cycle 19 | SF02 Glitch Storm fix pass (Critique C9). Fixes: (1) Storefront lower-right replaced with genuine DAMAGED STOREFRONT WINDOW — structural frame + vertical/horizontal dividers + crack lines from 2 impact points + missing panes showing dark interior + glass shard and rubble debris scatter below; (2) Warm window glow replaced with REAL window light geometry — downward trapezoid cones of warm amber (200,160,80) below each lit building window, alpha 90-110, reads as pools of warm domestic light vs cold storm. Carries all v003 fixes. Output: `LTG_COLOR_styleframe_glitch_storm.png`. | Pillow |
| `LTG_TOOL_bg_school_hallway.py` | Jordan Reed / Cycle 19 | School Hallway background fix pass (Critique C9). Fixes: (1) Black top band artifact fixed — image initialized to CEIL_TILE color, ceiling polygon now starts at y=0; (2) Human evidence added — backpack leaning against locker (80×120px+, deep blue with front pocket + strap), coat hooks on right wall near section with hanging jacket (brown, shadow side + collar), notice board on left wall with 8 distinct colored paper rectangles (red/yellow/green/white/blue) with pins; (3) Camera angle lowered — VP_CY from H*0.40 to H*0.22 (-18%), hallway feels taller and more institutional. Output: `LTG_ENV_school_hallway.png`. | Pillow |
| `LTG_TOOL_bg_millbrook_main_street.py` | Jordan Reed / Cycle 19 | Millbrook Main Street background fix pass (Critique C9). Fixes: (1) Power lines — main cable 3px (perspective-scaled), span wires 1px, catenary sag via parabolic curve (not straight lines), overall line darkness reduced; (2) Road plane — full ROAD_ASPHALT solid trapezoid base, faded center double-yellow dashed lines converging to VP, crosswalk stripes at near end (perspective-correct trapezoid stripes). Output: `LTG_ENV_millbrook_main_street.png`. | Pillow |
| `LTG_TOOL_bg_tech_den.py` | Jordan Reed / Cycle 20 | Tech Den background fix pass (Critique C9, Takeshi Murakami C+). Fixes: (1) Window light shaft — trapezoid of SUNLIT_AMBER (212,172,100) with feathered soft edges (GaussianBlur), 15 dust motes as warm-white circles (alpha 60–80) within beam; (2) Monitor glow spill — blue-white (180,200,210, alpha 38–50) cast onto chair back/seat, desk surface in front of monitors, nearest shelving face; (3) Right half completion — bedding drawn as polygon (not rectangle), explicit pillow shapes, blanket fold, device on mattress edge, wall above bed with circuit-diagram poster + 2 taped printouts (code lines + schematic); (4) Cosmo's jacket — RW-08 dusty lavender (160,150,175) as prominent polygon draped over chair back with collar detail and shadow side, reads as inhabited silhouette. Canvas 1280×720. Output: `LTG_ENV_tech_den.png`. | Pillow |
| `LTG_TOOL_bg_grandma_kitchen.py` | Jordan Reed / Cycle 21 | Grandma Miri's Kitchen background fix pass (Critique C9, Takeshi Murakami B+). Three targeted improvements: (1) Floor linoleum grid overlay — 60×60px tile grid at 2px/alpha-25 over the floor area, plus worn-path trapezoid from doorway-to-stove (lighter warm buff, alpha 20); (2) Upper wall period wallpaper texture — horizontal stripes at 12–15% opacity alternating warm cream/buff, applied to upper 50% of back wall; (3) Enhanced CRT glow — primary glow radius increased to 80 (was 60), second wider ambient ring at radius 130 and alpha 8 for atmospheric doorway presence. Note: ltg_render_lib.py (Kai Nakamura, Cycle 21) not yet available; scanline_overlay() skipped. Canvas 1920×1080. Output: `LTG_ENV_grandma_kitchen.png`. | Pillow |
| `LTG_TOOL_render_lib.py` | Kai Nakamura / Cycle 21 (renamed Cycle 22, **v1.2.0 C42**) | **Canonical shared rendering utility library** for all LTG generators. `__version__ = "1.2.0"` (C42: `flatten_rgba_to_rgb()` added — see below). C24: `paper_texture()` added. Provides: `perlin_noise_texture()`, `gaussian_glow()` (dead-alpha bug fixed Cycle 22), `light_shaft()`, `dust_motes()`, `catenary_wire()`, `scanline_overlay()`, `vignette()`, `paper_texture()`, **`flatten_rgba_to_rgb(img, background=(255,255,255))`** — canonical RGBA→RGB Porter-Duff helper: composites RGBA image over a solid background (default white), returns RGB. Replaces manual numpy Porter-Duff flattening pattern documented in C41 (Tech Den v006). Pass `background=(0,0,0)` for legacy black-backing behavior. All generators should call this at save time instead of `img.convert("RGB")` (which discards alpha incorrectly). No external deps beyond Pillow. Import: `from LTG_TOOL_render_lib import *`. | Pillow |
| `LTG_TOOL_bg_tech_den.py` | Kai Nakamura / Cycle 21 | Tech Den background v003. Upgrades v002 lighting/atmosphere calls to use LTG_TOOL_render_lib: `light_shaft()` for window beam, `dust_motes()` for beam particles, `gaussian_glow()` for monitor spill, `vignette()` as final pass. All visual content (desks, monitors, shelving, jacket, bed, posters) preserved from v002. Canvas 1280×720. Output: `LTG_ENV_tech_den.png`. | Pillow, LTG_TOOL_render_lib |
| `LTG_TOOL_bg_glitchlayer_frame.py` | Kai Nakamura / Cycle 21 | Glitch Layer background v003. All platform/aurora/flora/pixel-trail content from v001 preserved. New: `scanline_overlay()` from LTG_TOOL_render_lib applied as final pass (spacing=4, alpha=18) — the Glitch Layer is a CRT interior, scanlines are canonical. Canvas 1920×1080. Output: `LTG_ENV_glitchlayer_frame.png`. | Pillow, LTG_TOOL_render_lib |
| `LTG_TOOL_byte_expression_sheet.py` | Maya Santos / Cycle 22 | Byte expression sheet v004. Previously misnamed `LTG_CHAR_byte_expression_sheet.py` — renamed in Cycle 22 to comply with TOOL category convention. | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 22 | Cosmo expression sheet v004. Previously misnamed `LTG_CHAR_cosmo_expression_sheet.py` — renamed in Cycle 22 to comply with TOOL category convention. | Pillow |
| `LTG_TOOL_bg_glitch_layer_encounter.py` | Jordan Reed / Cycle 21 | Glitch Layer encounter background generator. Previously misnamed `bg_glitch_layer_encounter.py` in `output/backgrounds/environments/` — relocated and renamed in Cycle 21/22. LTG-compliant copy lives in `output/tools/`. | Pillow |
| `LTG_TOOL_bg_tech_den.py` | Jordan Reed / Cycle 22 | Tech Den background v004. Resolves Takeshi Murakami Critique C10 P1+P2. Fix 1a: light shaft repositioned to land ON desk surface (DESK_TOP_Y=395) — apex near window top, base at (10,407)+(210,390), max_alpha=150. Fix 1b: single wide-ellipse desk glow removed; replaced with three separate `gaussian_glow()` calls — CRT1 (r=110, alpha 65), CRT2 (r=100, alpha 58), flat panel (r=90, alpha 52) — each hotspots its desk zone. Import updated to `LTG_TOOL_render_lib` (Cycle 23). Canvas 1280×720. Output: `LTG_ENV_tech_den.png`. | Pillow, LTG_TOOL_render_lib |
| `LTG_TOOL_bg_grandma_kitchen.py` | Jordan Reed / Cycle 22 | Grandma Miri's Kitchen background v003. Resolves Takeshi Murakami Critique C10 P3. Fix 2a: `draw_upper_wall_texture()` extended to left and right wall polygons via PIL polygon mask — side wall alpha ~35% less (8/10 vs 12/15). Fix 2b: `draw_floor_tiles()` disabled (no-op), `draw_floor_linoleum_overlay()` rewritten with single perspective-correct grid converging to vp_x (non-linear row spacing + converging column lines). One floor system. All v002 improvements retained. Canvas 1920×1080. Output: `LTG_ENV_grandma_kitchen.png`. | Pillow |
| ~~`LTG_TOOL_stylize_handdrawn.py`~~ | Rin Yamamoto / Cycle 23 | **RETIRED Cycle 26 — moved to `output/tools/legacy/`.** Hand-drawn stylization pass tool (realworld, glitch, mixed modes). Post-processing pipeline retired as of Cycle 26. | Pillow, NumPy (optional) |
| ~~`LTG_TOOL_batch_stylize.py`~~ | Kai Nakamura / Cycle 24–25 | **RETIRED Cycle 26 — moved to `output/tools/legacy/`.** Batch runner for the stylize pipeline. Post-processing pipeline retired as of Cycle 26. | Pillow, LTG_TOOL_stylize_handdrawn |
| ~~`LTG_TOOL_stylize_handdrawn.py`~~ | Rin Yamamoto / Cycle 25 | **RETIRED Cycle 26 — moved to `output/tools/legacy/`.** Hand-drawn stylization pass v002 (4 canonical color protection fixes). Post-processing pipeline retired as of Cycle 26. | Pillow, NumPy (optional) |
| `LTG_TOOL_color_verify.py` | Kai Nakamura / Cycle 25 | Canonical color verification utility. Samples canonical palette colors from a PIL Image and returns per-color hue drift analysis. `verify_canonical_colors(img, palette_dict, max_delta_hue=5)` — returns per-color `{target_hue, found_hue, delta, pass}` + `overall_pass`. `get_canonical_palette()` — returns standard LTG 6-color palette dict. Colors not present in the image are marked `not_found` (not a failure). Designed as a gating function: call after stylize() or any processing pass that could shift hues. Standalone; no imports beyond `colorsys` + Pillow. All six canonical colors verified from `master_palette.md`. | Pillow (colorsys stdlib) |
| `LTG_TOOL_byte_expression_sheet.py` | Alex Chen / Cycle 13 | Byte expression sheet v001. Migrated from `byte_expressions_generator.py`. 4×2 grid (7 expressions): NEUTRAL/DEFAULT-GRUMPY added (Dmitri Volkov P1). Output: `LTG_CHAR_byte_expression_sheet.png`. | Pillow |
| `LTG_TOOL_byte_expression_sheet.py` | Maya Santos / Cycle 21 | Byte expression sheet v003. Adds STORM/CRACKED damage state (9th expression). Layout upgraded to 3×3 grid. Storm pose: right eye uses dead-pixel cracked glyph per byte.md §9B, bent antenna, angular lean (+18 tilt). Output: `LTG_CHAR_byte_expression_sheet.png`. | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 14 | Cosmo expression sheet v001. 6 expressions: THOUGHTFUL, SKEPTICAL, DELIGHTED, AWKWARD, WORRIED, SURPRISED. 3×2 grid, 1200×900. Cycle 16 fixes: SKEPTICAL body_tilt adjusted, two empty slots populated. Output: `LTG_CHAR_cosmo_expression_sheet.png`. | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 14/16 | Cosmo expression sheet v002. Carries Cycle 16 fixes from v001 (SKEPTICAL tilt, WORRIED+SURPRISED added). Shares same 6-expression set. Output: `LTG_CHAR_cosmo_expression_sheet.png`. | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 19 | Cosmo expression sheet v003. Critical fix: body_tilt displacement formula corrected (was 0.4× factor = invisible at thumbnail; now full pixel displacement). Carries all v001/v002 expressions. Output: `LTG_CHAR_cosmo_expression_sheet.png`. | Pillow |
| `LTG_TOOL_cosmo_turnaround.py` | Maya Santos / Cycle 47 | **Current Cosmo turnaround.** v003 — Visual hook pass: amplified cowlick + glasses bridge tape in all 4 views. All v002 content preserved. Output: `LTG_CHAR_cosmo_turnaround.png` (1280×560). | Pillow |
| `LTG_TOOL_glitch_expression_sheet.py` | Maya Santos / Cycle 23 | Glitch expression sheet v001. 4 expressions: NEUTRAL, MISCHIEVOUS, PANICKED, TRIUMPHANT. 2×2 grid, 800×800. Diamond/rhombus body with CORRUPT_AMBER, HOT_MAGENTA crack lines, 3×3 pixel dual-eye system. Output: `LTG_CHAR_glitch_expression_sheet.png`. | Pillow |
| `LTG_TOOL_glitch_expression_sheet.py` | Maya Santos / Cycle 24 | Glitch expression sheet v002. Expands to 6 expressions: adds STUNNED and CALCULATING. 3×2 grid, 1200×900. Output: `LTG_CHAR_glitch_expression_sheet.png`. | Pillow |
| `LTG_TOOL_glitch_expression_sheet.py` | Maya Santos / Cycle 28 | Glitch expression sheet v003. Expands to 9 expressions: adds YEARNING, REACHING OUT, REMEMBERING (interior desire expressions per Nkechi Adeyemi C12). 3×3 grid, 1200×900. Output: `LTG_CHAR_glitch_expression_sheet.png`. | Pillow |
| `LTG_TOOL_glitch_turnaround.py` | Maya Santos / Cycle 23 | Glitch 4-view turnaround v001. FRONT, 3/4, SIDE, BACK views. 1600×700 canvas. Diamond body in CORRUPT_AMBER with HOT_MAGENTA crack lines. Output: `LTG_CHAR_glitch_turnaround.png`. | Pillow |
| `LTG_TOOL_glitch_turnaround.py` | Maya Santos / Cycle 24 | Glitch turnaround v002. Shadow contrast fix: SIDE and BACK views use CORRUPT_AMB_SHADOW (not UV_PURPLE) for depth shadows. Output: `LTG_CHAR_glitch_turnaround.png`. | Pillow |
| `LTG_TOOL_glitch_color_model.py` | Sam Kowalski / Cycle 23 | Glitch color model. 800×500 canvas, 10 swatches grouped by zone (body, cracks, shadow, glow, outline). All values from master_palette.md. Output: `LTG_COLOR_glitch_color_model.png`. | Pillow |
| `LTG_TOOL_grandma_miri_expression_sheet.py` | Maya Santos / Cycle 17 | Grandma Miri expression sheet v001. 5 expressions: WARM/WELCOMING, NOSTALGIC/WISTFUL, CONCERNED, SURPRISED/DELIGHTED, WISE/KNOWING. 3+2 grid, 1200×900. Output: `LTG_CHAR_grandma_miri_expression_sheet.png`. | Pillow |
| `LTG_TOOL_grandma_miri_expression_sheet.py` | Maya Santos / Cycle 19 | Grandma Miri expression sheet v002. Ground-up rebuild (v001 failed squint test). Body-language differentiation: full-body gesture change per expression, not face-only. Output: `LTG_CHAR_grandma_miri_expression_sheet.png`. | Pillow |
| `LTG_TOOL_grandma_miri_expression_sheet.py` | Maya Santos / Cycle 33 | **Current Miri expression sheet.** Grandma Miri v003. Full rebuild (C33). Adds KNOWING STILLNESS as 6th expression (narrative: Miri knew about the Glitch Layer all along). New mouth style: "knowing_oblique" (one corner barely lifted). New per-eye oblique_dy parameter for asymmetric gaze. Grid upgraded to full 3×2 (all 6 slots filled). All 5 v002 expressions retained unchanged. Silhouette note: KNOWING STILLNESS and WISE share identical body pose — distinction is entirely in face (heavy-lidded oblique gaze, asymmetric suppressed smile). Output: `LTG_CHAR_grandma_miri_expression_sheet.png` (1200×900). | Pillow |
| `LTG_TOOL_grandma_miri_color_model.py` | Sam Kowalski / Cycle 17 | Grandma Miri color model. 800×500 canvas, swatches grouped by zone (face/skin, hair, clothing, accessories). All values from grandma_miri_color_model.md + master_palette.md. Output: `LTG_COLOR_grandma_miri_color_model.png`. | Pillow |
| `LTG_TOOL_luma_color_model.py` | Sam Kowalski / Cycle 25 | Luma color model. 800×500 canvas, 14 swatches (hoodie orange, skin, hair, pants, shoes, pixel cyan/magenta zones). All values from luma_color_model.md. Output: `LTG_COLOR_luma_color_model.png`. | Pillow |
| `LTG_TOOL_byte_color_model.py` | Sam Kowalski / Cycle 25 | Byte color model. 800×500 canvas, 14 swatches. CRITICAL: body fill = GL-01b #00D4E8 BYTE_TEAL (not #00F0FF Electric Cyan). Output: `LTG_COLOR_byte_color_model.png`. | Pillow |
| `LTG_TOOL_cosmo_color_model.py` | Sam Kowalski / Cycle 25 | Cosmo color model. 800×500 canvas, 14 swatches (cerulean/sage stripes, glasses). All values from cosmo_color_model.md. Output: `LTG_COLOR_cosmo_color_model.png`. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 25 | Luma expression sheet v005. Forwarding stub → `LTG_CHAR_luma_expression_sheet_v005.py`. Output: `LTG_CHAR_luma_expression_sheet.png`. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 27 | Luma expression sheet v006. Forwarding stub → `LTG_CHAR_luma_expression_sheet_v006.py`. Output: `LTG_CHAR_luma_expression_sheet.png`. | Pillow |
| `LTG_TOOL_luma_turnaround.py` | Maya Santos / Cycle 25 | Luma 4-view character turnaround v002 (Cycle 25/26). Forwarding stub → `LTG_CHAR_luma_turnaround_v002.py`. Output: `LTG_CHAR_luma_turnaround.png`. | Pillow |
| `LTG_TOOL_luma_turnaround.py` | Maya Santos / Cycle 28 | Luma turnaround v003. Line weight corrected to v006 3-tier standard at 2× render (head=4, structure=3, detail=2). All views updated. Output: `LTG_CHAR_luma_turnaround.png`. | Pillow |
| `LTG_TOOL_miri_turnaround.py` | Maya Santos / Cycle 20 | Grandma Miri 4-view character turnaround. 1600×800 canvas, 3-tier line weight. MIRI-A canonical: bun + crossed chopsticks, A-line cardigan, round glasses. Height 3.2 heads. Output: `LTG_CHAR_miri_turnaround.png`. | Pillow |
| `LTG_TOOL_character_lineup.py` | Maya Santos / Cycle 24 | Character lineup v004. Added Glitch (5th character, far right). Glitch: diamond body in CORRUPT_AMBER, HOT_MAG crack, UV_PURPLE shadow, VOID_BLACK outline, hover. Output: `LTG_CHAR_lineup.png`. | Pillow |
| `LTG_TOOL_character_lineup.py` | Maya Santos / Cycle 27 | **Current character lineup.** Luma rebuilt to v006-era construction (8-ellipse hair curl cloud, cheek nubs, near-circular eyes, 3-tier line weight at lineup scale). Output: `LTG_CHAR_lineup.png`. | Pillow |
| `LTG_TOOL_luma_cold_overlay_swatches.py` | Sam Kowalski / Cycle 18 | Cold overlay swatch generator. Reference PNG showing base color, Electric Cyan overlay, and alpha-composite result side-by-side for each cold overlay variant from luma_color_model.md. 1280×1067 (≤1280 after thumbnail). Output: `LTG_COLOR_luma_cold_overlay.png`. | Pillow |
| `LTG_TOOL_luma_classroom_pose.py` | Maya Santos / Cycle 14 | Luma classroom sitting pose for storyboard beat A1-04. Seated at desk, slight slouch, left hand supporting chin (distracted lean), right hand pen-tapping desk, head tilted 8° toward blackboard. Output: `LTG_CHAR_luma_classroom_pose.png`. | Pillow |
| `LTG_TOOL_fidelity_check_c24.py` | Sam Kowalski / Cycle 24 | Color fidelity check utility. Samples 6 canonical palette colors from styled PNGs vs originals. Tolerance 20 per channel. Targets: GL-07 CORRUPT_AMBER, GL-01b BYTE_TEAL, UV_PURPLE, SUNLIT_AMBER, SOFT_GOLD, ENV-06. Precursor to `LTG_TOOL_color_verify.py`. | Pillow |
| `LTG_TOOL_logo.py` | Alex Chen / Cycle 25 | Canonical show logo entry point. Imports from `LTG_TOOL_logo_asymmetric.py` and saves output to `output/production/LTG_BRAND_logo.png`. Alias/entry point for the canonical Cycle 13 asymmetric logo design. | Pillow |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Sam Kowalski / Cycle 22 | SF02 Glitch Storm fix pass (Critique C10). Reduced window pane alpha (160/180 → 115/110). Source in `output/color/style_frames/`; stub in `output/tools/` is location-compliance entry point. Output: `LTG_COLOR_style_frame_02_glitch_storm_v005.png`. | Pillow |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Jordan Reed / Cycle 34 | SF02 Glitch Storm C34 lighting pass. Adds two missing character lighting passes flagged by critics 2 consecutive cycles: (1) HOT_MAGENTA (#FF2D6B) soft fill light from lower-left on all characters (alpha max 40, radial distance falloff per-character zone — storm-crack ground bounce); (2) ELEC_CYAN (#00F0FF) specular on Luma's hair and shoulders via `add_rim_light(side="right", threshold=180, width=2)` using `get_char_bbox()` for char_cx (procedural_draw v1.4.0). Post-thumbnail bright specular points ensure value ceiling ≥ 225 (QA PASS). Source in `output/color/style_frames/`; copy in `output/tools/`. All v005 fixes carried (CORRUPT_AMBER GL-07, window pane alpha 115/110, storefront, confetti, Dutch angle). Canvas 1280×720. Output: `LTG_COLOR_styleframe_glitch_storm.png`. | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_style_frame_03_other_side.py` | Sam Kowalski / Cycle 27 | SF03 Other Side confetti fix pass. Confetti particles constrained to within 150px of nearest character/platform anchor (was full-canvas scatter). Output: `LTG_COLOR_styleframe_otherside.png`. | Pillow |
| `LTG_TOOL_style_frame_03_other_side.py` | Sam Kowalski / Cycle 28 | SF03 Other Side UV_PURPLE_DARK saturation fix. Corrected from (43,32,80)=#2B2050 (31% sat) to GL-04a (58,16,96)=#3A1060 (72% sat). Deep void zones now read as digital void, not grey-purple. Output: `LTG_COLOR_styleframe_otherside.png`. | Pillow |
| `LTG_TOOL_styleframe_luma_byte.py` | Sam Kowalski / Cycle 25 | SF04 "The Dynamic" (Luma + Byte interaction) v001. Forwarding stub → `LTG_COLOR_styleframe_luma_byte_v001.py`. Output: `LTG_COLOR_styleframe_luma_byte.png`. | Pillow |
| `LTG_TOOL_styleframe_luma_byte.py` | Sam Kowalski / Cycle 27 | SF04 "The Dynamic" v002. Procedural quality upgrade. Forwarding stub → `LTG_COLOR_styleframe_luma_byte_v002.py`. Output: `LTG_COLOR_styleframe_luma_byte.png`. | Pillow |
| `LTG_TOOL_styleframe_luma_byte.py` | Rin Yamamoto / Cycle 28 | SF04 "The Dynamic" v003. Forwarding stub → `LTG_COLOR_styleframe_luma_byte_v003.py`. Output: `LTG_COLOR_styleframe_luma_byte.png`. | Pillow |
| `LTG_TOOL_procedural_draw.py` | Rin Yamamoto / Cycle 26 (**v1.5.0 C34**) | **Procedural drawing library.** `wobble_line()`, `wobble_polygon()`, `variable_stroke()`, `add_rim_light(side, char_cx=None)`, `silhouette_test()`, `value_study()`, `add_face_lighting()`, `get_char_bbox()`, `scene_snapshot()`. **v1.5.0 (C34):** `scene_snapshot(img, region, label, out_dir) → str` — crops a named region from a scene image, adds a label banner, enforces ≤1280px, saves to `<out_dir>/LTG_SNAP_<label>.png`. Enables targeted close-up diagnostics without sending full-res images to Claude. v1.4.0 (C33): `get_char_bbox(img, threshold=128) → (cx, cy, left, top, right, bottom)` — auto-detects character silhouette bounding box from bright pixels. Callers pass `char_cx=get_char_bbox(img)[0]` to `add_rim_light()` without manually tracking head_cx. Eliminates canvas-midpoint bug risk for any character at any x-position. Backward compatible (existing calls unchanged). v1.3.0 (C32): `add_rim_light()` gains optional `char_cx` parameter. | Pillow |
| `LTG_TOOL_render_qa.py` | Kai Nakamura / Cycle 26 (v1.1.0 C27, v1.2.0 C30, **v1.3.0 C35**) | **Render Quality Assessment tool.** Evaluates any LTG PNG against rendering standards across 6 checks: (A) silhouette readability at 100×100 (score: distinct/ambiguous/blob), (B) value range (darkest ≤ 30, brightest ≥ 225, range ≥ 150), (C) color fidelity via `verify_canonical_colors()`, (D) warm/cool separation ≥ 20 PIL hue units (top/bottom zone sampling), (E) line weight consistency (20 random edge samples, outlier detection), (F) **value ceiling guard** (C35, Morgan Walsh): detects when thumbnail downscaling drops max brightness below ≥225; reports brightness_before/after/loss and specular dot candidate count. API: `qa_report(img_path, asset_type=None) → dict`, `qa_batch(directory) → list[dict]`, `qa_summary_report(results, output_path)`, `check_value_ceiling_guard(img_path) → dict`. Also exports `silhouette_test()` and `value_study()`. Overall grade: PASS / WARN / FAIL. | Pillow, LTG_TOOL_color_verify |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 29 | Luma expression sheet v007 — PROPORTION FIX. HEAD-TO-BODY ratio corrected to 3.2 heads (torso_h HR×2.10, was HR×1.80). Eye width corrected to HR×0.22 (canonical spec from turnaround v003, was HR×0.28). All v006 features retained: classroom-style head/hair/eyes, 6-expression 3×2 layout, per-expression hoodie color map, 2× LANCZOS AA, full-body silhouette differentiation. Output: `LTG_CHAR_luma_expressions.png` (1200×900). | Pillow |
| `LTG_TOOL_character_lineup.py` | Maya Santos / Cycle 29 | Character lineup v006. Luma updated to v007 canonical proportions: 3.2 heads (was 3.5), eye width r×0.22 (was r×0.28). All 5 characters present. Other characters (Byte, Cosmo, Miri, Glitch) unchanged from v005. Output: `LTG_CHAR_luma_lineup.png`. | Pillow |
| `LTG_TOOL_styleframe_discovery.py` | Rin Yamamoto / Cycle 29 | SF01 "The Discovery" procedural quality pass. Brings SF01 to same quality level as SF04 v003: wobble_polygon() on Luma head, CRT frame and couch; variable_stroke() on head perimeter arcs (8-arc); add_face_lighting() warm lamp from upper-left; add_rim_light() cool CRT from right. Canvas 1280×720 (≤1280px direct). All v003 fixes retained (ghost Byte alpha, specs[2]+specs[3] placement). Output: `LTG_COLOR_styleframe_discovery.png`. | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_styleframe_discovery.py` | Rin Yamamoto / Cycle 32 | SF01 "The Discovery" rim-light char_cx fix. Fixes canvas-midpoint bug: Luma at ~x=0.29W now receives correct right-side rim (char_cx=head_cx passed to add_rim_light). All C29/C30 procedural quality features retained. Requires procedural_draw v1.3.0. Output: `LTG_COLOR_styleframe_discovery.png` (1280×720). | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_styleframe_discovery.py` | Rin Yamamoto / Cycle 38 | SF01 "The Discovery" sight-line + visual power fix (C38 — Ingrid critique P2, Lee Tanaka staging brief, Alex Chen visual power brief). Luma's gaze now reaches Byte: head_cx +18px right (head_gaze_offset), eyes shifted toward emerge_cx (screen-side eye wider/wonder, away-side eye narrower/intensity), pupils track toward ghost Byte. Expression: THE NOTICING / DOUBT VARIANT — screen-side brow raised HIGH (wonder), away-side brow with inner-corner kink (doubt, not trusting conclusion yet), mouth CLOSED / barely parted hold (Lee Tanaka spec: "held, not performing"). Arm: Option B reaching gesture (open palm toward screen, NOT pointing) — desire, not display. Backward lean sp(36) (arrested mid-recognition). Visual power: 4 wild hair strands (was 2 crown arcs), 12 hoodie pixel squares (was 7), shoulder rolled forward. QA: render_qa PASS, value ceiling guard PASS (max=242, 5 specular candidates), proportion audit PASS (ew=HR×0.22). Requires procedural_draw v1.3.0+. Output: `LTG_COLOR_styleframe_discovery.png` (1280×720). | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_styleframe_luma_byte.py` | Rin Yamamoto / Cycle 32 | SF04 "The Dynamic" FULL REBUILD (C32 — sources lost in C28). Reconstructed from v003 PNG as reference. Sven C13 audit fixes: value ceiling 255 (was 198, PASS ≥225), clear silhouette, Byte body GL-01b #00D4E8 canonical, BYTE_TEAL monitor contribution on Byte's CRT-facing side, Luma blush #E8A87C, warm upper-left face lighting, rim lights with char_cx. Specular highlights ensure ≥225. Output: `LTG_COLOR_styleframe_luma_byte.png` (1280×720). | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_luma_turnaround.py` | Rin Yamamoto / Cycle 32 | Luma turnaround v004 — eye-width canonical fix (Alex Chen C32 directive / Daisuke C13 P1). v003 used ew=int(h*0.22) where h=head-height → ew was int(head_r*0.44), 2× too wide. v004 corrects: ew=int(head_r*0.22) where head_r=radius=int(h*0.50). All three views fixed (FRONT, 3/4, SIDE). All v003 line weights and proportions retained. Output: `LTG_CHAR_luma_turnaround.png` (1280×560). | Pillow |
| `LTG_TOOL_draw_order_lint.py` | Kai Nakamura / Cycle 31 | **Draw-order static linter.** Parses generator .py files via regex (no execution, no AST) and flags painter's-algorithm violations: W001 head/face drawn before body; W002 outline drawn before fill for same element; W003 shadow drawn after the element it should darken; W004 img.paste()/alpha_composite() not followed by draw = ImageDraw.Draw(img) refresh within 5 lines. API: `lint_file(path) → dict` (result: PASS/WARN, warnings list with line/code/message), `lint_directory(directory, pattern) → list`, `format_report(results) → str`. CLI: run against glob of .py files, saves report to `LTG_TOOL_draw_order_lint_report.txt`. C31 run: 114 files — 59 PASS / 55 WARN / 0 ERROR (most WARNs are W004 missing draw refresh). | stdlib only |
| `LTG_TOOL_color_verify.py` | Kai Nakamura / Cycle 31 | **Color verification utility v002 — adds histogram mode.** Full v001 API preserved. New optional `histogram=True` parameter on `verify_canonical_colors()`: per-color result gains `hue_histogram` (list of dicts, 5° buckets, 72 total), `histogram_bucket_deg` (5), `canonical_bucket_index` (int marking canonical hue's bucket). `format_histogram(histogram, canonical_bucket_index)` helper produces ASCII bar chart. CLI: `python LTG_TOOL_color_verify.py image.png [--histogram]`. Eliminates false-positive investigation by showing full hue distribution with canonical band highlighted. Backward compatible: histogram keys absent when histogram=False (default). | stdlib colorsys + Pillow |
| `LTG_TOOL_palette_warmth_lint.py` | Sam Kowalski / Cycle 33 | **CHAR-M warmth compliance linter.** Parses master_palette.md for all CHAR-M-xx table entries and checks that R >= G AND R >= B (Grandma Miri's warm-palette guarantee). Flags any entry where G > R or B > R. API: `lint_palette_file(path) → dict`, `lint_palette_text(text, source_name) → dict`, `format_report(results) → str`. Result dict: `result` ("PASS"/"WARN"), `total_checked` (int), `total_violations` (int), `violations` (list of code/name/hex/rgb/reason). CLI: `python LTG_TOOL_palette_warmth_lint.py [palette.md ...]` (default: master_palette.md). Catches CHAR-M-11 class of error (Deep Sage G>R slip — went undetected for multiple cycles). C33 baseline: 11 entries checked, 0 violations (PASS). No Pillow dependency — stdlib only. | stdlib only |
| `LTG_TOOL_palette_warmth_lint.py` | Sam Kowalski / Cycle 34 | **Configurable warmth compliance linter.** Extends v001 to support any list of warm-guaranteed character prefix codes via `warmth_lint_config.json`. Default config (`CHAR-M`) preserves v001 behaviour. New API: `load_config(path=None) → dict` reads JSON config (falls back to CHAR-M if file absent). `lint_palette_file(path, config=config)` / `lint_palette_text(text, source_name, config=config)` accept config. Report header shows active prefix list. CLI: `python LTG_TOOL_palette_warmth_lint.py [--config config.json] [palette.md ...]`. Actioned from C33 ideabox (warmth lint scope expansion). C34 baseline: 11 CHAR-M entries, 0 violations (PASS). No Pillow dependency — stdlib only. | stdlib only |
| `warmth_lint_config.json` | Sam Kowalski / Cycle 34 (**v3 C35**) | **Config file for palette_warmth_lint_v002/v003.** JSON: `{"warm_prefixes": ["CHAR-M"], "soft_tolerance": {"G": 0, "B": 0}, "description": "..."}`. Edit `warm_prefixes` to extend coverage; set `soft_tolerance.B > 0` (e.g. 10) to allow warm-adjacent entries where B may slightly exceed R. Default: strict (G±0, B±0). Path: `output/tools/warmth_lint_config.json`. | JSON (stdlib) |
| `LTG_TOOL_color_qa_c37_runner.py` | Sam Kowalski / Cycle 37 | **C37 QA Baseline runner.** Runs `render_qa_v001` v1.4.0 (world-type-aware) on all C37 pitch-primary style frames, plus `palette_warmth_lint_v004` on master_palette.md. Also evaluates `LTG_ENV_grandma_living_room.png` (Hana Okonkwo, C37). Saves results to `output/production/color_qa_c37_baseline.md`. CLI: `python3 LTG_TOOL_color_qa_c37_runner.py`. | Pillow, LTG_TOOL_render_qa, LTG_TOOL_palette_warmth_lint |
| `LTG_TOOL_palette_warmth_lint.py` | Sam Kowalski / Cycle 35 | **Soft-tolerance warmth compliance linter.** Extends v002 with per-channel tolerance via `soft_tolerance` JSON config key. `soft_tolerance: {"G": 0, "B": 10}` = B may exceed R by up to 10 counts. Default: strict (G±0, B±0) = v002 behaviour. Violation dict gains `margin` field (G−R, B−R counts). Report shows active mode (strict/soft). New CLI flag `--strict` forces tolerance=0 regardless of config (for CI use). Actioned from C34 ideabox (warmth lint multi-channel). C35 baseline: 11 CHAR-M entries, 0 violations (PASS). No Pillow dependency — stdlib only. | stdlib only |
| `LTG_TOOL_stub_linter.py` | Kai Nakamura / Cycle 33 | **Stub integrity linter.** Scans all `.py` files in `output/tools/` for broken imports referencing deleted originals — `import LTG_CHAR_*`, `import LTG_COLOR_*`, `import LTG_BRAND_*`, `import LTG_ENV_*`, `from LTG_CHAR_* import ...` etc. Reports: which file, import statement, whether target `.py` exists on disk, whether canonical `LTG_TOOL_*` replacement exists. Status: PASS / WARN (target exists) / ERROR (target missing). `--pre-commit` exits code 1 on any ERROR — suitable for CI gate. API: `lint_file(filepath) → dict`, `lint_directory(directory, skip_legacy=True) → list`, `format_report(results) → str`. CLI: `python LTG_TOOL_stub_linter.py [dir] [--pre-commit] [--save-report PATH]`. | stdlib only |
| `LTG_TOOL_glitch_spec_lint.py` | Kai Nakamura / Cycle 33 (updated C35) | **Glitch spec linter v1.1.0.** Validates Glitchkin generator code against `output/characters/main/glitch.md`. Only lints files containing Glitch draw markers (`diamond_pts`/`CORRUPT_AMB`/`draw_glitch_body`). 8 checks: G001 rx/ry in spec range [28–56, 28–64] (C35: widened range to accommodate scaled renders); G002 ry > rx (taller than wide — C35: corrected spec constants, rx=34/ry=38); G003 multi-panel files have ≥2 unique expressions; G004 crack after body fill; G005 UV_PURPLE shadow offset (+3,+4); G006 no organic RGB in fill; G007 VOID_BLACK polygon outline; G008 interior states (YEARNING/COVETOUS/HOLLOW) bilateral eyes. Non-Glitch files are SKIP. API: `lint_file(filepath) → dict`, `lint_directory(directory) → list`, `format_report(results) → str`. CLI: `python LTG_TOOL_glitch_spec_lint.py [file_or_dir] [--save-report PATH]`. | stdlib only |
| `LTG_TOOL_byte_expression_sheet.py` | Maya Santos / Cycle 38 | **Current Byte expression sheet.** v006 — SILHOUETTE DIFFERENTIATION FIX. Worst pair in v005: RELUCTANT JOY↔RESIGNED 90.2% RPD. Four arm position changes: (1) ALARMED arm_x_scale 1.5→2.0, arm_l/r_dy raised higher (max startle reach); (2) RELUCTANT JOY arm_l_dy -2→-12 / arm_r_dy 12→18 (larger asymmetry); (3) POWERED DOWN arm_x_scale 0.7→0.20 / dy 18→26 (limp shutdown, near-zero extension); (4) RESIGNED arm_x_scale 0.50→0.25 / dy 14→24 (defeated, arms barely present). Post-fix worst pair still 90.2% — confirmed RPD limitation for small oval characters at 240px panel width. Visual distinction achieved in design. Output: `LTG_CHAR_byte_expression_sheet.png` (712×1280px). | Pillow |
| `LTG_TOOL_byte_expression_sheet.py` | Maya Santos / Cycle 33 | ~~**Superseded by v006 (C38).**~~ v005 — UNGUARDED WARMTH addition. 10th expression: body_tilt=-4 (leans toward Luma), arms floating (-5 dy), left leg +2px forward, gold confetti (SOFT_GOLD #E8C95A — ONLY this expression), star_gold pixel symbol (5×5 SOFT_GOLD star, full brightness) on right/organic eye, heart_purple (5×5 UV_PURPLE heart) on left/cracked eye, warmth mouth (barely-there upward arc). New mouth style "warmth". Gold antenna tip. BG warm-cream-leaning-cool (240,224,198). Grid upgraded to 4×3 (12 slots, 10 filled). Silhouette test: UNGUARDED WARMTH distinct from all others. Output: `LTG_CHAR_byte_expression_sheet.png` (712×1280px). | Pillow |
| `LTG_TOOL_expression_silhouette.py` | Maya Santos / Cycle 33 | **Expression silhouette differentiation test.** Extracts each panel from an expression sheet PNG, per-pixel background classification → black silhouette, then compares all panel pairs using combined similarity (0.6×IoM + 0.4×XOR). FAIL ≥ 85%, WARN ≥ 72%. Auto-detects grid from common LTG sheet sizes; override with `--rows`/`--cols`. Dark-BG sheets (Glitch) supported. API: `run_test(sheet_path, ...) → dict`, `make_silhouette(panel, sheet_bg) → Image`, `combined_similarity(sil_a, sil_b) → float`. CLI: `python LTG_TOOL_expression_silhouette.py sheet.png [--rows R] [--cols C] [--threshold 0.85] [--output sil.png] [--json]`. C33 baseline: Glitch v003=PASS(71%), Luma v008=FAIL(91%), Miri v002=FAIL(94%), Cosmo v003=FAIL(96%), Byte v003=FAIL(89%). Exit: 0=PASS, 1=WARN, 2=FAIL. | Pillow |
| `LTG_TOOL_expression_silhouette.py` | Maya Santos / Cycle 34 | **Expression silhouette test v002 — adds `--mode arms` flag.** Extends v001 with regional comparison mode. `--mode full` (default) = v001 full-panel behaviour. `--mode arms` = isolates arm/shoulder region [--arms-top 0.20 … --arms-bot 0.70] of panel height, optionally masks body trunk center with `--center-mask 0.28`. Identifies face-only expressions that share identical body language. Report header shows active mode and region fractions. In arms mode, contact sheet shows arm-region crops (not full panels). C34 baseline (Luma v008 arms mode): FAIL — worst pair SURPRISED↔FRUSTRATED 97.9%; Luma v009 arms mode: FAIL — worst pair SURPRISED↔FRUSTRATED 97.0%. Key finding: at panel resolution (~373px wide), shared torso column dominates arm-band crops; pure arm-extension differences are sub-pixel. Use `--center-mask 0.36` for tighter torso exclusion. Ideabox idea filed: `arms-edge` sub-mode for left/right zone sampling only. API: `run_test(..., mode, arms_top_frac, arms_bot_frac, center_mask_frac) → dict`, `crop_arm_region(sil, ...) → Image`. CLI adds `--mode`, `--arms-top`, `--arms-bot`, `--center-mask`. **Superseded by v003.** | Pillow |
| `LTG_TOOL_expression_silhouette.py` | Maya Santos / Cycle 36+37 | **Expression silhouette test v003 — Regional Pose Delta (RPD) metric.** BREAKING CHANGE from v002: replaces IoM with RPD (C35 root-cause finding). RPD divides silhouette into zones: HEAD (top 25%), ARMS (middle 50%), LEGS (bottom 25%); column-projection histogram → Pearson correlation per zone; combined RPD weighted HEAD=35%, ARMS=45%, LEGS=20%. `--mode full` (default): 3-zone RPD. `--mode arms`: arm band only with trunk masking (center-mask=0.30). Thresholds: WARN ≥ 70%, FAIL ≥ 85%. **C37 addition: `--output-zones` flag** — when used with `--output` and `--mode full`, draws colored left-edge zone bars on each panel in the contact sheet: HEAD=blue, ARMS=orange, LEGS=green. Bars positioned at bounding-box-derived zone boundaries. Helps designers see immediately which zone drove a WARN/FAIL. Scoring, JSON output, and exit codes unchanged. API: `run_test(..., output_zones=False) → dict`, `draw_zone_overlays(img, sil, x0, y0, panel_w, panel_h)`. Exit: 0=PASS, 1=WARN, 2=FAIL. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 34 | ~~**Superseded by v010 (C37).**~~ v009 — ARM SILHOUETTE DIFFERENTIATION. Driven by `--mode arms` baseline from C34. Fixes for 4 failing pairs: SURPRISED → true Y-shape; FRUSTRATED → tight crossed-arm pose; WORRIED → self-hug arms; DELIGHTED → arms raised in V; CURIOUS → one-arm-reaching. THE NOTICING unchanged. Output: `LTG_CHAR_luma_expressions.png` (1200×900). | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 38 | **Current Luma expression sheet.** v011 — THE NOTICING RIGHT-EYE LID FIX. Takeshi Murakami C38 critique: right eye used wince (symmetric lid contraction), must be a SQUINT (top lid drops ~20-25%, bottom stays neutral). Fix: `squint_top_r` parameter in `draw_eyes_full()` — draws full eye at r_open=1.0 then overdraws top portion with BG-colored rect to simulate upper-lid drop. All v010 improvements preserved (brow asymmetry, lateral gaze, noticing_hand gesture, deeper BG/hoodie, subtle blush, body tilt, center slot position). Output: `LTG_CHAR_luma_expressions.png` (1200×900). | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 37 | ~~**Superseded by v011 (C38).**~~ v010 — THE NOTICING REWORK. Critics scored v009 THE NOTICING at 52-58/100. Seven targeted fixes: (1) r_open 0.85→0.65 (stronger eye asymmetry, survives LANCZOS); (2) brow_l_dy -0.14HR→-0.22HR (dramatic one-eyebrow-up); (3) gaze_dy 0.15→0.0 (pure lateral tracking); (4) new `draw_noticing_hand_v010()` — finger-to-lower-lip + vertical forearm diagonal; (5) deeper BG (195,210,228); (6) deeper hoodie (105,128,162); (7) THE NOTICING moved to CENTER SLOT (slot 4) for visual primacy. Subtle blush=30 added. Tiny body_tilt lean toward noticed thing. All other 6 expressions unchanged from v009. Output: `LTG_CHAR_luma_expressions.png` (1200×900). | Pillow |
| `LTG_TOOL_char_spec_lint.py` | Kai Nakamura / Cycle 34 (v1.1.0 C39) | **General character spec linter.** Validates generator Python source against canonical spec files for Luma, Cosmo, Grandma Miri, and Byte (Byte added C39). Per-character checks: Luma — L001 head-to-body ratio 3.2, L002 eye-width HR×0.22, L003 hoodie color #E8722A, L004 5-curl hair rule, L005 pixel-pattern colors (#00F0FF/#FF2D6B). Cosmo — S001 body ratio 4.0, S002 eye-width ~0.18×hu, S003 glasses tilt neutral 7°, S004 frame thickness 0.06×hu, S005 notebook color #5B8DB8. Miri — M001 body ratio 3.2, M002 eye-width ~0.16×hu, M003 perm blush #D4956B, M004 bun/chopstick marker, M005 crow's feet detail. **Byte (v1.1.0 C39)** — B001 oval W:H wider-than-tall, B002 body color #00D4E8 (GL-01b Byte Teal), B003 HOT_MAG crack indicator, B004 pixel confetti floating mechanism, B005 5×5 eye grid. Per-check result: PASS/WARN/FAIL. API: `lint_character(char_name, tools_dir) → dict`, `lint_all(tools_dir) → list`, `format_report(results) → str`. CLI: `python LTG_TOOL_char_spec_lint.py [--char luma\|cosmo\|miri\|byte] [--save-report PATH]`. | stdlib only |
| `LTG_TOOL_draw_order_lint.py` | Kai Nakamura / Cycle 34 | **Scope-aware draw-order linter (v002).** Upgrades v001 with basic Python scope awareness for W004 (missing draw refresh). Key improvements: (1) alpha_composite() only flags W004 when result IS assigned back to `img` (eliminates false positives from tmp=alpha_composite()); (2) img.paste() inside helper functions with local draw objects no longer flagged (scope boundary detection via indentation); (3) calls inside docstrings/string literals skipped; (4) refresh search respects scope boundary (stops when indent decreases). W001/W002/W003 checks identical to v001. `--compare` flag shows delta vs v001. C34 baseline: 134 files — 97 PASS / 37 WARN / 0 ERROR. W004 count: 147 (v001) → 69 (v002), reduction of 78 warnings (53% fewer false positives). API: `lint_file(path) → dict`, `lint_directory(directory, pattern) → list`, `format_report(results) → str`, `compare_v1_v2(directory) → str`. CLI: `python LTG_TOOL_draw_order_lint.py [files] [--compare] [--save-report PATH]`. | stdlib only |
| `LTG_TOOL_lineup_palette_audit.py` | Alex Chen / Cycle 34 | **Character lineup palette audit.** Scans character lineup PNG column-by-column (5 columns: Luma, Byte, Cosmo, Miri, Glitch) and verifies each character's key body colors against canonical master_palette.md values. Per-color result: PASS (canonical found within Euclidean tolerance), FAIL (canonical not found), or WRONG_PRESENT (legacy incorrect value detected). Tolerance default: 18 (Euclidean RGB distance). Catches regressions like Byte shadow color (C33 bug: #0090B0→#00A8B4) and Miri slipper color (#5A7A5A→#C4907A) before critique. Audited colors: Byte body+shadow, Miri slipper+skin+cardigan, Luma hoodie+skin, Cosmo jacket+skin, Glitch amber+UV purple+void black. API: `audit_lineup(path, tolerance) → dict`, `format_report(results, path) → str`. CLI: `python LTG_TOOL_lineup_palette_audit.py [lineup.png] [--tolerance N] [--json]`. Exit: 0=all PASS, 1=any FAIL or WRONG. | Pillow |
| `LTG_TOOL_bg_grandma_kitchen.py` | Jordan Reed / Cycle 35 | Grandma Miri's Kitchen background v004. Full rebuild (Chiara Ferrara C14, score 58/100). Fix 1: Deep shadow pass applied AFTER all light passes — DEEP_COCOA (#3B2820) + NEAR_BLACK_WARM (28,18,10) in ceiling corners, cabinet undersides, floor corners, table underside (QA value floor: 62→20, PASS). Fix 2: Dual-temperature system — SUNLIT_AMBER warm column gradient alpha-max=80 on upper half + CRT_COOL_SPILL=(0,130,148) on lower half, plus explicit top/bottom split pass (QA warm/cool separation: 1.7→32.95, PASS). Fix 3: Miri-specific spatial details — rose-pattern mug, knitting bag on chair, worn apron on peg near doorway, travel-destination fridge magnets (5 countries), medicine bottles, handwritten wall calendar near stove. Canvas: 1280×720 (direct, no thumbnail needed). All v003 features retained (perspective floor grid, side wall texture, CRT glow, worn path). QA: WARN (line_weight false positive from temp split scanlines — value range and warm/cool PASS). Output: `LTG_ENV_grandma_kitchen.png`. | Pillow |
| `LTG_TOOL_sf02_fill_light_fix_c35.py` | Jordan Reed / Cycle 35 (**C37 refactor: Rin Yamamoto**) | **SF02 fill light integration module (C37: canvas_w/canvas_h params added).** Provides corrected `draw_magenta_fill_light_v007_fast()` for Rin Yamamoto to integrate into SF02 v007. Fixes Sven C14 critique: (1) Source direction corrected from lower-left → upper-right (matching storm crack position at x≈94%, y≈2%); (2) Per-character silhouette mask applied — HOT_MAGENTA gradient restricted to character pixel zones only via `ImageChops.multiply()` on alpha channel (was unmasked canvas tint in v006). Alpha max 35 (vs 40 in v006 — upper-right direct is cleaner). Per-character crop approach avoids multi-char bbox detection failure (Sven C14 issue: 3-char bbox spans 83% of canvas). Integration: `from LTG_TOOL_sf02_fill_light_fix_c35 import draw_magenta_fill_light_v007_fast as draw_magenta_fill_light_v007`. | Pillow |
| `LTG_TOOL_precritique_qa.py` | Morgan Walsh / Cycle 34 (**v2.0.0 C35**) | **Pre-critique QA pipeline entry point.** Chains all QA tools into a single consolidated run. v1.0.0 (C34): 6 sections. v2.0.0 (C35): Section 7 README Script Index Sync added (`LTG_TOOL_readme_sync`); render_qa upgraded to v1.3.0; SF02 pitch ref updated to v006; output: `precritique_qa_c35.md`. C34 baseline: WARN (PASS=150, WARN=36, FAIL=0). Exit: 0=PASS, 1=WARN, 2=FAIL. | Pillow, all QA tools |
| `LTG_TOOL_spec_extractor.py` | Kai Nakamura / Cycle 35 | **Character spec extractor.** Parses character `.md` spec files and automatically extracts numeric constraints, reducing manual maintenance of `char_spec_lint_v001` checks. Extracts: head_ratio (total height in head units), eye_coeff (eye-width coefficient × head-radius), color hex values with labels, degree/ratio numeric constants, line weight values, and exact element counts (hair curls, etc.). Output: structured dict (JSON-serialisable) per character. Supports Luma, Cosmo, Miri, Byte, Glitch. API: `extract_spec(char_name, spec_dir) → dict`, `extract_all(spec_dir) → list[dict]`, `format_spec_report(result_or_list) → str`. CLI: `python LTG_TOOL_spec_extractor.py [luma\|cosmo\|miri\|byte\|glitch\|all] [--json] [--save-dir DIR] [--save-report PATH]`. | stdlib only |
| `LTG_TOOL_proportion_verify.py` | Kai Nakamura / Cycle 26 | **Character proportion verification tool.** Verifies head-to-body ratio on character turnaround sheets using pixel occupancy gap detection. `detect_background()`, `occupied_rows()`, `find_character_extent()`, `find_head_height() → (head_height, method)` — head_height may be None for multi-panel sheets (algo limitation). CANONICAL_RATIO=3.2, RATIO_TOLERANCE=0.15 (±15%). Used by LTG_TOOL_precritique_qa Section 3. | Pillow |
| `LTG_TOOL_proportion_audit.py` | Kai Nakamura / Cycle 26 | **Standalone proportion audit batch runner.** Runs proportion_verify checks on all character turnarounds in output/characters/ and writes a report. Convenience wrapper around LTG_TOOL_proportion_verify.py for batch use. | Pillow, LTG_TOOL_proportion_verify |
| `LTG_TOOL_char_diff.py` | Kai Nakamura / Cycle 29 | **Character-to-character visual diff tool.** Pixel-level diff between two character PNGs (same canvas size). Reports changed pixel count, % changed, centroid of change region. Use to verify a generator fix only touches intended pixels and does not regress other areas. | Pillow |
| `LTG_TOOL_luma_color_model.py` | Sam Kowalski / Cycle 29 | Luma color model v002. Revised swatch layout, additional annotation labels. Output: `LTG_COLOR_luma_color_model.png`. | Pillow |
| `LTG_TOOL_colorkey_otherside_gen.py` | Sam Kowalski / Cycle 14 | Generates 640×360 color key thumbnail for SF03 "The Other Side." Cold-dominant palette: ELEC_CYAN / VOID_BLACK / UV_PURPLE / DATA_BLUE. No warm tones. Output: `LTG_COLOR_colorkey_otherside.png`. | Pillow |
| `LTG_TOOL_character_lineup.py` | Maya Santos / Cycle 20 | Character lineup v003. 4-character lineup (Luma, Byte, Cosmo, Miri) — pre-Glitch addition. Superseded by v004+ which adds Glitch. Retained for historical diff reference. | Pillow |
| `LTG_TOOL_character_lineup.py` | Maya Santos / Cycle 34 | **Current character lineup.** Character lineup v007 (C34). Carries all v006 canonical proportions; updates expression references per C34 revisions. Output: `LTG_CHAR_lineup_v007.png`. | Pillow |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos / Cycle 34 | Luma expression sheet v008 (C34 pre-arm-fix pass). 6 expressions. Superseded by v009 (arm silhouette differentiation). Output: `LTG_CHAR_luma_expressions.png` (1200×900). | Pillow |
| `LTG_TOOL_cycle13_panel_fixes.py` | Lee Tanaka / Cycle 13 | **DEPRECATED (C44, Morgan Walsh).** Batch fix runner for Cycle 13 storyboard panels (old `LTG_SB_coldopen_panel_XX` naming). Stub in tools root raises ImportError. Full source in deprecated/. Canonical replacements: per-panel generators `LTG_TOOL_sb_cold_open_P*.py` (Diego Vargas C41–C43). | Pillow |
| `LTG_TOOL_act2_panels_cycle14.py` | Lee Tanaka / Cycle 14 | Act 2 storyboard panel batch generator for Cycle 14. Generates all Act 2 panels added in C14 (A2-01 through A2-07 initial pass). | Pillow |
| `LTG_TOOL_sb_a2_cycle15.py` | Lee Tanaka / Cycle 15 | Act 2 storyboard batch for Cycle 15. Regen pass for all Act 2 panels with C15 spec updates. | Pillow |
| `LTG_TOOL_sb_panel_a101.py` | Lee Tanaka / Cycle 17 | Individual panel generator: A1-01 (Grandma Miri's kitchen — morning scene). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a102.py` | Lee Tanaka / Cycle 17 | Individual panel generator: A1-02 (school corridor — Luma arrives). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a103.py` | Lee Tanaka / Cycle 17 | Individual panel generator: A1-03 v001 (hallway locker — Luma/Cosmo meet). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a103.py` | Lee Tanaka / Cycle 19 | Individual panel generator: A1-03 v002 (hallway locker — school_hallway_v002 BG). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a104_kitchen.py` | Lee Tanaka / Cycle 18 | Individual panel generator: A1-04 (classroom — Luma distracted at desk). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a201.py` | Lee Tanaka / Cycle 14 | Individual panel generator: A2-01 (Cosmo's tech den — discovery reaction). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a202.py` | Lee Tanaka / Cycle 15 | Individual panel generator: A2-02 v002 (Byte — RESIGNED/RELUCTANT DISCLOSURE expression). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a203.py` | Lee Tanaka / Cycle 15 | Individual panel generator: A2-03 v002 (tech den — Cosmo/Luma/Byte planning session). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a204.py` | Lee Tanaka / Cycle 15 | Individual panel generator: A2-04 v002 (tech den — Cosmo explains plan). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a205.py` | Lee Tanaka / Cycle 17 | Individual panel generator: A2-05 (Millbrook main street — walk-and-talk). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a206_insert.py` | Lee Tanaka / Cycle 17 | Individual panel generator: A2-06 insert (reaction shot — Luma stops). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a207.py` | Lee Tanaka / Cycle 15 | Individual panel generator: A2-07 v001 (Glitch Layer encounter — first sight). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a207b.py` | Lee Tanaka / Cycle 19 | Individual panel generator: A2-07b (Glitch Layer — Byte cracked-eye glyph reveal). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a208.py` | Lee Tanaka / Cycle 15 | Individual panel generator: A2-08 v001 (Glitch Layer — Luma reaches for Byte). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_panel_a208.py` | Lee Tanaka / Cycle 19 | Individual panel generator: A2-08 v002 (Glitch Layer — revised staging, Luma larger). 480×270 panel. | Pillow |
| `LTG_TOOL_sb_act1_contact_sheet.py` | Lee Tanaka / Cycle 17 | Act 1 contact sheet generator v001. Composites Act 1 panels (A1-01 through A1-05) into a 2-row reference sheet. | Pillow |
| `LTG_TOOL_sb_act1_contact_sheet.py` | Lee Tanaka / Cycle 19 | Act 1 contact sheet generator v002. Updated with A1-03 v002 panel. | Pillow |
| `LTG_TOOL_sb_act1_full_contact_sheet.py` | Lee Tanaka / Cycle 20 | Act 1 full (all-panels) contact sheet. All Act 1 panels in a single multi-row sheet. | Pillow |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka / Cycle 15 | Act 2 contact sheet v003. Composites Act 2 panels through A2-08 v001. | Pillow |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka / Cycle 17 | Act 2 contact sheet v004. Adds A2-05 and A2-06 insert panels. | Pillow |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka / Cycle 19 | Act 2 contact sheet v005. Uses A2-07b and A2-08 v002 revised panels. | Pillow |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka / Cycle 20 | **Current Act 2 contact sheet.** Final production Act 2 storyboard pitch layout. | Pillow |
| `LTG_TOOL_readme_sync.py` | Morgan Walsh / Cycle 35 | **README Script Index sync/audit tool.** Enumerates all `LTG_TOOL_*.py` files in `output/tools/` (non-legacy) and cross-checks against the Script Index table in README.md. Reports UNLISTED (on disk, not in README), GHOST (in README, not on disk), and LEGACY GHOST entries. No file modifications — report only. API: `audit(tools_dir, readme_path) → dict`, `format_report(result) → str`. CLI: `python LTG_TOOL_readme_sync.py [--save-report PATH] [--json]`. Exit: 0=PASS, 1=WARN. Used by LTG_TOOL_precritique_qa Section 7. | stdlib only |
| `LTG_TOOL_bg_glitch_layer_frame.py` | Jordan Reed / Cycle 9 | **Canonical alias for bg_glitch_layer_frame.py.** Full 1920×1080 Glitch Layer compositing-ready background. See main entry `LTG_TOOL_bg_glitch_layer_frame.py` (listed in script table as alias for `bg_glitch_layer_frame.py`). Superseded by `LTG_TOOL_bg_glitchlayer_frame.py`. | Pillow |
| `LTG_TOOL_bg_grandma_kitchen.py` | Jordan Reed / Cycle 35 | Grandma Miri's Kitchen background v004 (C35 fix pass). Carries all v003 improvements; applies C35 critique notes. Output: `LTG_ENV_grandma_kitchen.png`. Canvas 1920×1080. | Pillow |
| `LTG_TOOL_bg_grandma_kitchen.py` | Jordan Reed / Cycle 39 | Grandma Miri's Kitchen background v005 — Dual-Miri visual plant. Adds handwritten "MIRI" label on a cream paper scrap on the fridge lower door (right-center, near travel magnets). Label at x=906–946, y=461–477. Real World palette only: LINE_DARK ink on AGED_CREAM paper, warm amber magnet pip. Legible at full PNG resolution; easy to miss at thumbnail scale. Story plant: Grandma Miri / Glitch Layer Miri name connection (Season 1 finale payoff). No other changes from v004. Output: `LTG_ENV_grandma_kitchen.png`. Canvas 1280×720. | Pillow |
| `LTG_TOOL_character_face_test.py` | Rin Yamamoto / Cycle 26 | **Face lighting test utility.** Tests `add_face_lighting()` from `LTG_TOOL_procedural_draw.py` on a synthetic character head. Used for procedural draw library development and regression testing. Not a production generator. | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 35 | ~~**Superseded by v006 (C37).**~~ v005 — C35 arm/body-language differentiation pass. AWKWARD/WORRIED/SURPRISED redesigned for silhouette differentiation. Output: `LTG_CHAR_cosmo_expression_sheet.png`. | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 47 | **Current Cosmo expression sheet.** v008 — P1: Visual hook (amplified cowlick 0.15 heads + glasses bridge tape). P2: Shoulder involvement (deltoid displacement via `_shoulder_dy()`). All v007 content preserved: S003 compliance, skeptical_crossed, 6 expressions. Face gate: SKEPTICAL/WORRIED/CURIOUS PASS. RPD full: PASS worst 51.0%. Output: `LTG_CHAR_cosmo_expression_sheet.png` (1182×1114). | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 38 | ~~**Superseded by v008 (C47).**~~ v007 — SKEPTICAL ARM FIX (3+ cycles flagged). Standard arm mode caused arms to disappear behind torso at body_tilt=8. New `arm_mode="skeptical_crossed"`: left arm angles inward with notebook tucked, hand at left-of-center; right arm from shoulder inward, hand crossing to left-center — closed/folded posture. Both arms now visible outside torso silhouette. Worst RPD pair: AWKWARD↔SKEPTICAL 88.5% (HEAD zone 100% measurement artifact). Visual differentiation achieved. S003 compliance preserved. Output: `LTG_CHAR_cosmo_expression_sheet.png` (1182×1114). | Pillow |
| `LTG_TOOL_cosmo_expression_sheet.py` | Maya Santos / Cycle 37 | ~~**Superseded by v007 (C38).**~~ v006 — S003 glasses tilt compliance fix (P1). v005 had AWKWARD/SURPRISED/FRUSTRATED at 10° — all exceed spec 7°±2° (max 9°). Fixed: AWKWARD 10°→7°, SURPRISED 10°→7°, FRUSTRATED/DEFEATED 10°→7°. SKEPTICAL retained at 9° (at tolerance ceiling, within ±2°). spec_sync_ci result: CI PASS, 0 P1 violations. All v005 silhouette/pose content preserved. Worst RPD pair: AWKWARD↔SKEPTICAL 75.4% (WARN). Output: `LTG_CHAR_cosmo_expression_sheet.png` (1182×1114). | Pillow |
| `LTG_TOOL_grandma_miri_expression_sheet.py` | Maya Santos / Cycle 47 | **Current Miri expression sheet.** v007 — C47 shoulder involvement (deltoid displacement in draw_body_miri). All v006 content preserved. Output: `LTG_CHAR_grandma_miri_expression_sheet.png`. | Pillow |
| `LTG_TOOL_elderly_proportion_reference.py` | Maya Santos / Cycle 47 | **Elderly proportion reference & comparison overlay** v1.0.0. Two-panel diagram: realistic elderly female (6.2 heads) vs Miri stylized (3.2 heads). Head-unit grids, landmark connections, proportion ratio annotations. Output: `LTG_PROD_elderly_proportion_reference.png` (1280×720). | Pillow |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Jordan Reed / Cycle 35 | SF02 Glitch Storm v007 (C35 fix pass). Carries all v006 fixes. Applies C35 critique notes. Output: `LTG_COLOR_styleframe_glitch_storm.png`. Canvas 1280×720. | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_sf02_fill_light_fix_c35.py` | Jordan Reed / Cycle 35 | SF02 fill-light fix utility script for C35. Targeted patch applying fill-light corrections to the Glitch Storm style frame outside of the full v007 generator. Output: patched `LTG_COLOR_styleframe_glitch_storm.png` in-place or a numbered output. | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_style_frame_02_glitch_storm.py` | Rin Yamamoto / Cycle 36 | **Current SF02.** Glitch Storm v008 (C36 fill light direction fix). Integrates Jordan Reed's C35 fill light fix: source corrected to UPPER-RIGHT (storm crack), per-character silhouette mask via ImageChops.multiply (no BG tint), alpha max 35. Carries all C35 fixes (Luma face, torso lean, hair stream). Output: `LTG_COLOR_styleframe_glitch_storm.png`. Canvas 1280×720. | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_proportion_audit.py` | Rin Yamamoto / Cycle 36 | **Current proportion audit tool.** v002 extends v001 with asymmetric eye detection. Detects `eye_r_left`/`eye_r_right = max(N, int(head_r * M))` patterns; reports both L/R ratios separately; issues ASYM-WARN if either eye is outside spec or L/R differ by >10%. Intentional asymmetry (sprint face) yields ASYM-WARN, not FAIL. Report: `output/production/proportion_audit_c36.md`. | stdlib only |
| `LTG_TOOL_spec_sync_ci.py` | Kai Nakamura / Cycle 36 | **Spec Sync CI Gate.** Runs character spec linters on all 5 characters (Luma, Cosmo, Miri, Byte, Glitch) and exits non-zero if any P1 (proportion/construction) violations are found. P1 = FAIL-grade checks on char_spec_lint_v001 (Luma/Cosmo/Miri) + G001/G002 FAILs on glitch_spec_lint_v001 + B001 FAIL on inline Byte check. WARNs are advisory. CLI: `python LTG_TOOL_spec_sync_ci.py --chars all` (or `luma cosmo miri byte glitch`). `--json` flag for machine-readable output. `--save-report PATH` to write report. Exit: 0=PASS, 1=P1 FAIL, 2=usage error. API: `run_ci(chars, tools_dir) → dict`, `format_ci_report(ci_result) → str`. Depends on: LTG_TOOL_char_spec_lint, LTG_TOOL_glitch_spec_lint. | stdlib only (dynamic imports of dep tools) |
| `LTG_TOOL_warmth_inject.py` | Jordan Reed / Cycle 36 | **Warm/cool injection utility.** Post-processes an existing environment PNG to bring its warm/cool separation (render_qa metric D) above the ≥20 PIL hue units threshold. Modes: `warm` (SUNLIT_AMBER overlay on top half), `cool` (COOL_FILL overlay on bottom half), `auto` (default — tries warm first, falls back to cool; iterates alpha in steps of 10 until QA passes or alpha cap of 80 is hit). Respects image size rule (thumbnail ≤ 1280px before save). Output: `<input_basename>_warminjected.png` in same directory. CLI: `python LTG_TOOL_warmth_inject.py <input.png> [--mode warm\|cool\|auto] [--alpha INT] [--dry-run]`. API: `inject_warmth(img, mode, base_alpha, dry_run) → (result_img, report_dict)`, `measure_warm_cool(img) → dict`. C36 application: Tech Den v004 failed warm/cool (7.9); `_warminjected` output passes at 23.2. School Hallway v002 (76.2) and Millbrook Street v002 (45.5) already pass — tool reports "No injection needed". | Pillow |
| `LTG_TOOL_palette_warmth_lint.py` | Kai Nakamura / Cycle 36 (expanded Cycle 36 by Sam Kowalski) | **Warmth lint v004 — per-world-type threshold mode + CHAR-L hoodie expansion.** Adds `--world-type REAL\|OTHER_SIDE\|GLITCH` flag and auto-inference of world type from generator filename. New function: `infer_world_type(filename) → str\|None`. World-type threshold analysis is advisory (WARN, not FAIL). **C36 Sam Kowalski additions:** warm_prefixes expanded to ["CHAR-M", "CHAR-L"]; CHAR-L hoodie entries (CHAR-L-04, CHAR-L-08, CHAR-L-11) added to master_palette.md in table format for machine checking; `ltg_warmth_guarantees.json` is now primary config (priority over warmth_lint_config.json); `--world-threshold-only` CI flag added (prints threshold integer for shell capture: `THRESH=$(python ... --world-type GLITCH --world-threshold-only)`); DEFAULT_GUARANTEES_PATH exported. API: `lint_palette_file(path, config, world_type)`, `lint_palette_text(...)`, `infer_world_type(...)`, `load_config(path)`. CLI: `--world-type`, `--world-threshold-only`, `--infer-world-type PATH`, `--no-auto-world-type`, `--guarantees PATH` (future). | stdlib only |
| `LTG_TOOL_proportion_audit_c37_runner.py` | Rin Yamamoto / Cycle 37 | **Proportion audit C37 cycle runner.** Imports `LTG_TOOL_proportion_audit` scan engine and writes the C37-labelled report to `output/production/proportion_audit_c37.md`. The v002 tool has its path hardcoded to C36 — this runner overrides the report path for the C37 cycle. C37 scan result: PASS=3, ASYM-WARN=2 (SF02 v007/v008 intentional), WARN=1 (SF01 v003 legacy), FAIL=0, N/A=14 (20 files scanned). No new SF generators added in C37 — results identical to C36. CLI: `python3 LTG_TOOL_proportion_audit_c37_runner.py`. | stdlib only, LTG_TOOL_proportion_audit |
| `LTG_SB_pilot_cold_open.py` | Diego Vargas / Cycle 37 | **Pilot Cold Open 6-panel key-beat storyboard contact sheet.** 3 panels × 2 rows, output 1136×630px (within ≤1280×720 hard limit). Beats: P1 WIDE ESTABLISHING (Luma enters living room), P2 OTS (CRT spotted, reflection in dark glass), P3 INSERT ECU (pixel glitch forms — Glitchkin presence), P4 MCU PUSH-IN (Luma leans in, cyan bleeds into warm room), P5 ECU TWO-WORLD TOUCH (Byte presses against glass from inside), P6 MCU THE NOTICING (pitch emotional core — asymmetric brow expression: wonder + apprehension). Arc-color borders (warm amber/curious cyan/tense magenta/core bright cyan). Output: `LTG_SB_pilot_cold_open.png` in `output/storyboards/`. | Pillow |
| `LTG_CHAR_luma_motion.py` | Ryo Hasegawa / Cycle 37 | ~~**Superseded by v002 (C38).**~~ Luma Motion Spec Sheet v001. 4-panel sheet (1280×720). See v002 for CG/shoulder/annotation fixes. Output: `output/characters/motion/LTG_CHAR_luma_motion.png`. | Pillow |
| `LTG_CHAR_byte_motion.py` | Ryo Hasegawa / Cycle 37 | ~~**Superseded by v002 (C38).**~~ Byte Motion Spec Sheet v001. 3-panel sheet (1280×720). See v002 for crack scar side fix and glow radius annotation. Output: `output/characters/motion/LTG_CHAR_byte_motion.png`. | Pillow |
| `LTG_CHAR_luma_motion.py` | Ryo Hasegawa / Cycle 38 | **Current Luma Motion Spec Sheet.** v002 — 3 P1 critic fixes: (1) CG SUPPORT POLYGON — `body_cx` now constrained to ±40% of foot half-span; raw lean_forward+tilt clamped so CG never exits foot base; CG marker + support polygon drawn in panels 0 and 1; (2) SHOULDER MASS — shoulder geometry circles (r=head_r×0.22, SHOULDER_COL) added as explicit origin points for all arm movement in all panels; (3) HAIR ANNOTATION — Panel 1 annotation corrected from "NOT yet trailing" (wrong) to "-12° pre-lean (anticipation lean: ACTIVE)" to match code. 4-panel sheet, 1280×720. Output: `output/characters/motion/LTG_CHAR_luma_motion.png`. | Pillow |
| `LTG_CHAR_byte_motion.py` | Ryo Hasegawa / Cycle 38 | **Current Byte Motion Spec Sheet.** v002 — 2 fixes: (1) CRACK SCAR SIDE — scar moved to `cx + int(actual_bw * 0.55) + tx` (viewer's right) to match cracked eye drawn at `cx + eye_gap` (viewer's right); (2) GLOW RADIUS MAX ANNOTATION — dashed amber circle at max recommended radius (1.5×bw = 45px) on APPROACH panel state 3; annotated "MAX r=45px (1.5×bw)" — animators must not exceed this or glow doubles visual footprint. 3-panel sheet, 1280×720. Output: `output/characters/motion/LTG_CHAR_byte_motion.png`. | Pillow |
| `LTG_TOOL_ci_suite.py` | Kai Nakamura / Cycle 37 | **CI Suite — all pipeline checks in one command.** Runs 5 checks in sequence: (1) stub_linter_v001, (2) draw_order_lint_v002, (3) glitch_spec_lint_v001 (with suppression list), (4) spec_sync_ci_v001, (5) char_spec_lint_v001. Combined report with per-check status. `--fail-on WARN\|FAIL` threshold flag (default: FAIL). `--save-report PATH` to write report. `--json` for machine-readable output. `--no-details` for summary only. Exit: 0=pass, 1=fail at threshold, 2=import error. API: `run_suite(tools_dir, fail_on) → dict`, `format_suite_report(result, include_details) → str`. Depends on all 5 linter tools. | stdlib only (dynamic imports of dep tools) |
| `glitch_spec_suppressions.json` | Kai Nakamura / Cycle 37 | **Suppression list for LTG_TOOL_glitch_spec_lint v1.2.0.** JSON file (not a script) containing 26 (file, rule) pairs that are known false positives. Format: `{"suppressions": [{"file": "...", "rule": "G00x", "reason": "..."}]}`. Loaded automatically by glitch_spec_lint_v001 v1.2.0+. Primarily suppresses G007 (VOID_BLACK outline) false positives on non-Glitch-generator tool files. | n/a |
| `LTG_TOOL_contact_sheet_arc_diff.py` | Lee Tanaka / Cycle 37 | **Contact Sheet Arc-Diff Tool.** Compares two contact sheet PNGs (old vs new version) and outputs a side-by-side comparison PNG (≤ 800×600px). Highlights panels where thumbnail pixel content changed significantly (mean abs diff > 12), panels that were added or removed, and prints a summary (SAME / CHANGED / ADDED / REMOVED counts). Auto-detects panel grid by aspect ratio. Useful for verifying expression sheet revisions don't break arc continuity. CLI: `python3 LTG_TOOL_contact_sheet_arc_diff.py OLD.png NEW.png [OUTPUT.png]`. Output defaults to `output/storyboards/LTG_TOOL_arc_diff_output.png`. | Pillow |
| `LTG_TOOL_env_grandma_living_room.py` | Hana Okonkwo / Cycle 37 | **Grandma Miri's Living Room background v001.** The emotional heart of the show — where Luma first sees the CRT TV. REAL World palette. Camera: mild 3/4 view. Key focal point: CRT TV off-center right (screen ON, CRT_COOL_SPILL ambient glow). Key light: SUNLIT_AMBER afternoon shaft from left window + warm reading lamp upper-right. Contents: bookcase (left back wall), three family photo frames, area rug (warm red), sofa with knitted throw + scatter cushions, coffee table (stacked books, Miri's teacup, remote), floor lamp. Deep shadows applied after dual-temperature split pass (SUNLIT_AMBER top half alpha 50, CRT_COOL_SPILL bottom half alpha 65). QA PASS: min=26 max=228 range=202, warm/cool=25.4. Canvas 1280×720. Output: `output/backgrounds/environments/LTG_ENV_grandma_living_room.png`. | Pillow |
| `LTG_TOOL_warmth_inject_hook.py` | Jordan Reed / Cycle 37 | **Shared warmth-inject post-process hook for environment generators.** Encapsulates the 2-step generate → warmth-inject pipeline as a single callable. API: `run_warmth_hook(out_path, enabled=True) → str\|None`. If `enabled=False`: no-op. If image already passes warm/cool QA: prints status, returns None. If failing: calls `inject_warmth(mode="auto")` from `LTG_TOOL_warmth_inject`, saves `<name>_warminjected.png` alongside the output. Uses `importlib.util` for dynamic import (no hard-coded sys.path). **C37 generators updated with `--check-warmth` flag:** `LTG_TOOL_bg_tech_den.py`, `LTG_TOOL_bg_school_hallway.py`, `LTG_TOOL_bg_millbrook_main_street.py`, `LTG_TOOL_bg_grandma_kitchen.py`. **Pattern for new generators (Hana Okonkwo):** add `--check-warmth` argparse flag, call `run_warmth_hook(out_path, enabled=args.check_warmth)` after final `.save()`. | Pillow, LTG_TOOL_warmth_inject |

### C39 New tools (Hana Okonkwo)
| `LTG_TOOL_thumbnail_preview_v001.py` | Hana Okonkwo / Cycle 39 | **Thumbnail Visibility Preview Tool.** Side-by-side comparison sheet showing (left) full 1280×720 view and (right) 120×68 thumbnail simulation at 8× NEAREST zoom. If `--region x1 y1 x2 y2` is specified (in input-image pixel coords), draws an orange bounding box on both panels so a story-critical detail can be tracked across scales. Output: `<input_stem>_thumbnail_preview.png` alongside input file. Sheet auto-thumbnailed to ≤1280px. CLI: `python LTG_TOOL_thumbnail_preview_v001.py <input.png> [--region x1 y1 x2 y2]`. Tested against `LTG_ENV_grandma_living_room.png` — output 1280×439px. | Pillow |

### C39 New tools (Maya Santos)
| `LTG_TOOL_expression_isolator.py` | Maya Santos / Cycle 39 | **Hero Expression Isolator.** Renders a single named expression from any character expression sheet at large format (default 800×800px, max 1280px) for critic review and pitch materials. Input: `--char luma\|byte\|cosmo --expr NAME` (case-insensitive, partial match supported). Output: standalone PNG in `output/characters/extras/` with expression label strip. `--list` flag lists available expressions per character. Actioned from C37 ideabox idea (noticing_panel_context_tool). Smoke tests: Luma v011 `THE NOTICING` ✓, Luma v011 `THE NOTICING — DOUBT` ✓, Byte v006 `ALARMED` ✓. | Pillow |
| `LTG_TOOL_bodypart_hierarchy.py` | Maya Santos / Cycle 39–40 | **Body-Part Color-Index Hierarchy Tool v002.** (v001: C39. v002: C40 — --panel N added.) Assigns a palette index to each character pixel and detects hierarchy violations: (1) EYE_UNDER_HAIR, (2) HAIR_IN_EYE_RUN, (3) UNKNOWN_IN_HEAD. **v002 new args:** `--panel N` — auto-crops to panel N (0-based, L→R T→B) before analysis, eliminating cross-panel border noise from FAIL count; `--grid COLSxROWS` (default 3x3); `--chain` — pipeline mode: runs expression_isolator to get a clean 800×800px panel, then runs hierarchy (requires `--char` and `--expr`). `--output-annotated` saves blended overlay PNG. `--output-csv` saves full transition table. `--palette luma\|byte\|cosmo`. Luma v011 full sheet: 669 FAIL. Luma v011 `--panel 4`: 59 FAIL (per-panel isolation reduces cross-panel artifacts). Byte v006: 0 FAIL. | Pillow |

`LTG_TOOL_expression_silhouette.py` → **--viz-rpd flag added** (C39, Maya Santos): For each FAIL/WARN pair, generates a side-by-side pixel diff heatmap PNG showing which pixels drove the RPD score. Pixels unique to A = red, unique to B = cyan, shared = zone-tinted (HEAD=blue, ARMS=orange, LEGS=green). Output: `<base>_pair_XX_YY_<status>_vizdiff.png` per flagged pair. Requires `--output`. Actioned from C36 ideabox idea `20260330_maya_santos_rpd_zone_visualization.md`. Note: `--output-zones` contact-sheet overlay (added C37) is separate from `--viz-rpd` per-pair diff heatmap.

### C39 New tools and updates (Kai Nakamura)
- `LTG_TOOL_glitch_spec_lint.py` → **v1.4.0**: Docstring-stripping pre-pass for numeric-regex checks. `_strip_comments_and_docstrings()` added — strips triple-quoted docstrings and `#` comments before G001/G002/G003/G005/G006/G007/G008. G004 (draw-order) retains full original source. G002 self-suppression removed from `glitch_spec_suppressions.json`.
- `glitch_spec_suppressions.json` updated (C39): G002 self-suppression for `LTG_TOOL_glitch_spec_lint.py` removed (root cause fixed). File-prefix entries added for `LTG_TOOL_character_lineup_*` (G006/G007 false positives on multi-character lineup files).
- `LTG_TOOL_palette_warmth_lint.py` updated (C39): REAL_STORM world preset added (`warm_cool_threshold: 6`). `infer_world_type()` now returns `"REAL_STORM"` for sf02/glitch_storm filenames.
- `LTG_TOOL_render_qa.py` → **v1.6.0** (C39, Sam Kowalski — already deployed): REAL_STORM sub-type via `_infer_world_subtype()`. REAL_STORM threshold=3. SF02 warm/cool PASS.
- `LTG_TOOL_world_type_infer.py` → **v1.1.0** (C39, Kai Nakamura): REAL_STORM constant + inference rule added. `WARM_COOL_THRESHOLDS` updated: `REAL_STORM=3.0`, `REAL_INTERIOR=12.0`.
- `LTG_TOOL_char_spec_lint.py` → **v1.1.0** (C39, Kai Nakamura): Byte checks added (B001–B005).
| `LTG_TOOL_costume_bg_clash.py` | Kai Nakamura / Cycle 39 | **Costume-Background Clash Lint.** Detects when a character's dominant costume color too closely matches the dominant background, reducing figure-ground separation. Two modes: (1) **Image mode** — char PNG + bg PNG, extracts dominant colors via PIL quantize, computes CIE76 ΔE in CIELAB space. (2) **Palette mode** — hex colors + bg PNG. Thresholds: FAIL ΔE < 5, WARN ΔE 5–15, PASS ΔE ≥ 15. Known-safe list for intentional near-matches (Byte ELEC_CYAN vs Glitch Layer). Reference case: Cosmo cardigan (160,150,175) vs hallway locker lavender ≈ ΔE 4 → FAIL (C36 miss). API: `clash_check_images(char_path, bg_path) → dict`, `clash_check_palette(colors, bg_path) → dict`, `format_report(result) → str`. CLI: `--char PATH`, `--bg PATH`, `--char-hex HEX...`, `--save-report`. Exit: 0=PASS, 1=WARN, 2=FAIL. Pillow only; pure-Python CIE76. | Pillow |
| `output/production/warm_cool_world_type_spec.md` | Kai Nakamura / Cycle 39 | **Warm/Cool World-Type Creative Canon doc.** Documents the creative rationale for each world type's warm/cool separation threshold. REAL_INTERIOR=12 (lamp-lit warm interior), REAL_STORM=3 (intentionally cool-dominant storm scene), GLITCH=3 (near-zero warm), OTHER_SIDE=0 (fully digital). Establishes that SF02 Glitch Storm warm/cool sep of ~6.5 is **correct and intentional** — not a defect. Per Alex Chen C39 brief. | n/a |

### C39 New tools (Sam Kowalski)
| `LTG_TOOL_palette_warmth_lint.py` | Sam Kowalski / Cycle 39 | **Warmth lint v005 — standalone world-type inference + REAL_STORM sub-type.** Delegates world-type inference to `LTG_TOOL_world_type_infer` (stdlib-only) when available; falls back to v004 inline rules. New `infer_world_subtype()` function returns `"REAL_STORM"` for storm-keyword filenames vs `"REAL_INTERIOR"` for all other REAL assets. REAL_STORM threshold=3 (matching render_qa v1.6.0). New CLI flag `--check-generator FILE`: infers world type from a generator path and runs the palette lint with that world type. Backward-compatible with v004 API. CHAR-M-11 test confirmed PASS: #C4907A (R:196>G:144>B:122). `ltg_warmth_guarantees.json` remains the sole config authority for warm_prefixes — scope expansion is config-only (no code changes). Actioned from Tasks 1+3 (C39 inbox). | stdlib only |

### C39 New tools (Rin Yamamoto)
| `LTG_TOOL_fill_light_adapter.py` | Rin Yamamoto / Cycle 39 | **Resolution-aware fill light adapter.** Accepts character positions as fractional (0.0–1.0) or absolute pixel coords; auto-scales radii, blur, and gradient parameters to any canvas size (720p, 1080p, or other). Supports per-character `FillLightConfig` dataclass. Built-in factory `make_glitch_storm_fill_configs()` returns standard 3-char HOT_MAGENTA configs for SF02. Standalone `apply_fill_light(img, configs, cw, ch)` one-call API. Eliminates the hardcoded 1280×720 limitation of `LTG_TOOL_sf02_fill_light_fix_c35.py` — any generator at any resolution can use this adapter without inlining the algorithm. Self-test saves `test_fill_light_adapter_720p.png` and `test_fill_light_adapter_1080p.png`. | Pillow |
| `LTG_TOOL_proportion_audit.py` | Rin Yamamoto / Cycle 39 | **Current proportion audit tool.** v003 adds `--cycle N` CLI flag (C37/C39 actioned ideabox). Report heading and output path auto-update to `proportion_audit_c{N}.md`. Default: `--cycle 39`. Eliminates need for per-cycle runner wrapper scripts. All v002 functionality preserved (asymmetric eye detection, ASYM-WARN/ASYM-PASS, stub resolution, Luma presence heuristics). C39 result: PASS=4, ASYM-WARN=2 (SF02 v007/v008 intentional), WARN=1 (SF01 v003 legacy), FAIL=0, N/A=14 (21 files). Also callable as module: `from LTG_TOOL_proportion_audit import scan_styleframe_files, format_report`. CLI: `python3 LTG_TOOL_proportion_audit.py --cycle 39`. Report: `output/production/proportion_audit_c39.md`. | stdlib only |

- `LTG_TOOL_procedural_draw.py` → **v1.6.0** (C39, Rin Yamamoto): C39 head_cx audit pass. Confirmed `get_char_bbox()` present and correct (added C33 v1.4.0). SF02 v008 audit: uses `char_cx` from geometry constants — CORRECT (get_char_bbox on a multi-char frame spans all characters, so hardcoded geometry constants are mandatory for multi-char rims). SF03 v005 audit: does not call `add_rim_light()` — no canvas-midpoint bug risk. No latent bugs found. Audit notes added to module docstring.

### C40 Tool updates (Rin Yamamoto)
- `LTG_TOOL_bg_other_side.py` — **UV_PURPLE_DARK saturation fix (C40, Rin Yamamoto).** `UV_PURPLE_DARK` corrected from `(43, 32, 80)` = #2B2050 = 31% sat to GL-04a `(58, 16, 96)` = #3A1060 = 72% sat. Matches the same C28 fix already applied to `LTG_TOOL_style_frame_03_other_side.py`. Eliminates the 14.1° UV_PURPLE hue drift flagged in the ENV since C16. Also applies ≤1280px thumbnail rule on output (was generating 1920×1080). Regenerated: `output/backgrounds/environments/LTG_ENV_other_side_bg.png` at 1280×720.
- `LTG_TOOL_precritique_qa.py` → **v2.8.0 (C40, Rin Yamamoto):** Alpha Blend Lint Section 10 added. Lazy-loads `LTG_TOOL_alpha_blend_lint.py`; runs `lint_alpha_blend()` on fill-light composited style frames vs registered unlit bases (`*_nolight.png`). `FILL_LIGHT_ASSETS` registry: SF01 (warm lamp), SF02 (HOT_MAGENTA storm), SF04 (fill_light_adapter). Skips gracefully when base is absent. FLAT_FILL = FAIL, LOW_SIGNAL = WARN, all-skipped = PASS. Section 10 is scored and contributes to overall grade. Tools chain: [1/10]–[10/10].

### C40 New tools (Rin Yamamoto)
| `LTG_TOOL_fill_light_adapter.py` | Rin Yamamoto / Cycle 40 (**v1.1.0**) | **Fill Light Adapter v1.1.0 — Scene Presets Registry.** Adds `load_scene_configs(scene_name, presets_path=None) → (configs, char_h_frac)` — loads `FillLightConfig` list from `LTG_fill_light_presets.json`. Falls back to hardcoded defaults if JSON absent. All 4 scenes supported: SF01 (warm lamp + CRT bounce on Luma), SF02 (3-char HOT_MAGENTA storm crack upper-right), SF03 (UV_PURPLE ambient + ELECTRIC_CYAN self-glow), SF04 (BYTE_TEAL monitor bounce + self-glow). Self-test extended to cover `load_scene_configs()` round-trip and registry-based SF02 render. All v1.0.0 APIs preserved (backward compatible). | Pillow |
| `LTG_fill_light_presets.json` | Rin Yamamoto / Cycle 40 | **Fill Light Presets Registry.** JSON data file loaded by `LTG_TOOL_fill_light_adapter.py` v1.1.0. Defines standard fractional character positions, fill light colors, source directions, and `char_h_frac` for each scene (SF01–SF04). Comment keys (`_note`, `_description`) are stripped on load and do not affect FillLightConfig construction. Edit this file to tune scene fill light parameters without code changes. Path: `output/tools/LTG_fill_light_presets.json`. | JSON (no deps) |
| `LTG_TOOL_alpha_blend_lint.py` | Rin Yamamoto / Cycle 40 | **Differential Alpha Blend Lint Tool.** Detects fill-light blending defects by comparing gradient magnitude of composited image vs unlit base. Converts both to LAB (OpenCV cv2, falls back to numpy approx). Computes fill-light contribution in L* channel; samples in radial bins around each fill source point. Verdicts per zone: PASS (radial falloff detected), FLAT_FILL (uniform blob — no falloff, flat alpha artifact), LOW_SIGNAL (fill below noise floor). Two FLAT_FILL detection checks: low-std among signal bins + abrupt-edge coverage check. Annotated PNG output overlays zone circles, source crosshairs, and verdict labels. CLI: `python3 LTG_TOOL_alpha_blend_lint.py composited.png base.png [--output annot.png] [--json] [--zones zones.json]`. Module API: `lint_alpha_blend(comp, base, zones, ...) → dict`, `format_report(results) → str`, `annotate_result(comp, results, out_path)`. Self-test PASS. | numpy, OpenCV (cv2), Pillow |
| `LTG_TOOL_sf_covetous_glitch.py` | Rin Yamamoto / Cycle 40 | **COVETOUS Glitch Style Frame generator.** Builds `LTG_SF_covetous_glitch_v001.png` — Glitch in COVETOUS expression state (from expr v003 spec) in the Other Side space. Composition: Glitch large/centered, ACID_GREEN slit eyes bilateral target-lock, tilt+12° toward subject, reaching arms, sparse UV_PURPLE confetti; simplified Other Side void background (UV_PURPLE sky gradient, far-distance slabs, perspective floor grid); Byte as tiny silhouette far-right with BYTE_TEAL glow (object of desire, small scale = inaccessible). Eye-glow spill rendered as ACID_GREEN blurred overlay. Zero warm ambient light (BYTE_TEAL = pigment only). UV_PURPLE_DARK = GL-04a (58,16,96), 271.5° hue, 0.4° delta from canonical — PASS. Canvas 1280×720. Output: `output/color/style_frames/LTG_SF_covetous_glitch_v001.png`. | Pillow |

### C39 Tool updates (Kai Nakamura)
- `LTG_TOOL_glitch_spec_lint.py` → **v1.3.0**: Docstring-stripping pre-pass for numeric-regex checks. New `_strip_comments_and_docstrings()` strips triple-quoted docstrings and `#` comments from source before running G001/G002 numeric regex checks. Eliminates G002 false-positive class where spec-reference values in docstrings (e.g. `spec reference: rx=38`) triggered `_RX_ASSIGN`/`_RY_ASSIGN` matches. G002 self-suppression entry in `glitch_spec_suppressions.json` removed — no longer needed. All other checks (G003–G008) also updated to use stripped source; G004 (draw-order) retains full original source.
- `glitch_spec_suppressions.json` updated (C39): removed G002 suppression for `LTG_TOOL_glitch_spec_lint.py`. The root-cause false positive is now fixed by the v1.3.0 docstring-stripping pre-pass — suppression was redundant.
- `LTG_TOOL_palette_warmth_lint.py` updated (C39, Kai Nakamura): added `REAL_STORM` world preset (`warm_cool_threshold: 6`). Storm scenes (SF02/glitch_storm) have intentionally cool-dominant palettes — threshold 12 from `REAL` produced persistent false WARNs on SF02. `infer_world_type()` now returns `"REAL_STORM"` for sf02/glitch_storm/style_frame_02 filenames (previously returned `"REAL"`). Consistent with render_qa_v001.py v1.6.0 REAL_STORM logic.
- `LTG_TOOL_render_qa.py` → **v1.6.0** (C39, Sam Kowalski): REAL_STORM world sub-type added. `_infer_world_subtype()` splits `"REAL"` into `"REAL_INTERIOR"` (threshold 12) and `"REAL_STORM"` (threshold 3). SF02 glitch_storm (sep~6.5) → REAL_STORM threshold=3 → PASS. SF01 unchanged. Closes FP-006 for SF02. World-type inference now prefers `LTG_TOOL_world_type_infer` (standalone, stdlib-only) over embedded warmth_lint_v004 rules — Task 4 C39 integration complete.
- `LTG_TOOL_precritique_qa.py` → **v2.4.0** (C39, Sam Kowalski): warmth lint import upgraded `v001→v004` — precritique QA now runs CHAR-M + CHAR-L hoodie check (14 entries) instead of CHAR-M only (11 entries). CYCLE_LABEL updated to C39. Report header and footer version strings updated.
- `LTG_TOOL_char_spec_lint.py` → **v1.1.0** (C39, Kai Nakamura): Byte checks added (B001–B005). `_lint_byte()` and helper functions `_check_byte_oval_ratio()`, `_check_byte_pixel_eye_grid()` added. Byte registered in `_CHAR_REGISTRY` with `LTG_TOOL_byte_expression_sheet_v*.py` generator patterns. All 5 Byte checks (oval ratio, body color, HOT_MAG crack, confetti, 5×5 eye grid) gate against regressions that have previously appeared in expression sheet updates.
- `LTG_TOOL_precritique_qa.py` → **v2.5.0** (C39, Ryo Hasegawa): Section 8 Motion Spec Lint added. Chains `LTG_TOOL_motion_spec_lint.py` — checks motion sheet PNG structural compliance (size, panel count, annotation occupancy, beat badges, timing colors). MOTION_SHEETS target list: `LTG_CHAR_luma_motion.png` (3 panels), `LTG_CHAR_byte_motion.png` (4 panels). Updated `_make_snapshot()`, `build_report()`, and `main()` to include motion_spec section. Tools chain now [1/8]–[8/8].

### C39 New tools (Ryo Hasegawa)
| `LTG_TOOL_byte_motion.py` | Ryo Hasegawa / Cycle 39 | **Byte Motion Spec Sheet v003 — COMMITMENT Beat Arc.** 4-panel sheet documenting the COMMITMENT beat as a 4-beat arc (B1: Avoidance, B2: Mid-turn 45°, B3: Full-frontal arrival, B4: HOLD 8–12 frames). Directional ELEC_CYAN glow toward Luma. Key spec: full-frontal + eye-level + directional glow = unambiguous commitment. Beat 4 panel uses dark void background — stillness = decision. Distinguishes from RESIGNED / RELUCTANT JOY / UNGUARDED WARMTH via body orientation + float height + glow directionality. Implements Lee Tanaka brief (C38 inbox). RPD vs RESIGNED verified: 71.2% ≤ 75% spec requirement. Canvas 1280×720. Output: `output/characters/motion/LTG_CHAR_byte_motion.png`. | Pillow |
| `LTG_TOOL_motion_spec_lint.py` | Ryo Hasegawa / Cycle 39 | **Motion Spec Sheet Linter.** Pixel-analysis QA tool (no Claude vision). Checks: (1) size ≤ 1280px, (2) not blank, (3) panel count matches expected, (4) annotation zone occupancy per panel, (5) beat badge zones, (6) BEAT_COLOR timing label presence. Reports PASS/WARN/FAIL per check. CLI: `python3 LTG_TOOL_motion_spec_lint.py sheet.png [--panels 4]`. Module API: `lint_motion_spec(path, expected_panels) → dict`, `format_report(results) → str`. Integrated as Section 8 of `LTG_TOOL_precritique_qa.py` (v2.5.0). Actioned from Ryo's C38 ideabox idea. | Pillow |
| `LTG_TOOL_byte_commitment_rpd_test.py` | Ryo Hasegawa / Cycle 39 | **Byte COMMITMENT/RESIGNED RPD test input generator.** 2-panel 640×320 contact sheet for `LTG_TOOL_expression_silhouette.py` RPD verification. Panel 1: RESIGNED (tilt 25°, arms pinned, dim eye). Panel 2: COMMITMENT (tilt -3°, arms open, searching eye). RPD result: 71.2% (≤ 75% spec requirement, WARN by tool's own 70% threshold). Output: `output/characters/motion/LTG_CHAR_byte_commitment_rpd_input.png`. | Pillow |

### C39 New tools (Morgan Walsh)
- `LTG_TOOL_glitch_spec_lint.py` → **v1.4.0** (C39, Morgan Walsh): `file_prefix` suppression mode added. `_load_suppressions()` now returns `{"exact": set, "prefix": list}`. `_apply_suppressions()` checks prefix entries via `str.startswith()`. Backwards-compatible with callers passing a bare set. `glitch_spec_suppressions.json` updated with `file_prefix: "LTG_TOOL_character_lineup_"` entries for G006+G007 — all `character_lineup_*` files now auto-suppressed for organic fill and VOID_BLACK outline false positives. WARN reduction: ~28→~20 (lineup v003–v007 cleared).
- `LTG_TOOL_contact_sheet_arc_diff.py` updated (C39, Morgan Walsh): `compare_contact_sheets(old, new, out=None) → dict` programmatic API added. Returns `{ok, n_old, n_new, same, changed, added, removed, changed_slots, added_slots, removed_slots, diff_output}`. Does not call `sys.exit()` — safe to import from other tools. CLI `build_arc_diff()` unchanged.
- `LTG_TOOL_precritique_qa.py` → **v2.6.0** (C39, Morgan Walsh): Section 9 Arc-Diff Gate added. Calls `compare_contact_sheets()` on configured `ARC_DIFF_PAIRS`. CHANGED>3 → NOTE in report listing changed panel slots; REMOVED>0 → WARN. Informational only — does not affect score. `run_arc_diff_gate()` and `_load_arc_diff()` added.
- `LTG_TOOL_precritique_qa.py` → **v2.7.0** (C39, Morgan Walsh): `ARC_DIFF_PAIRS` now loaded from `arc_diff_config.json` at startup via `_load_arc_diff_pairs()`. Paths in JSON are relative to repo root. Falls back to hardcoded constant if JSON absent or invalid — fully backwards-compatible. Enables producers/team members to update arc-diff pairs without touching Python source.
- `arc_diff_config.json` (C39, Morgan Walsh): External config for arc-diff contact sheet pairs. Loaded by `LTG_TOOL_precritique_qa.py` v2.7.0+. Format: `{"pairs": [[label, old_rel_path, new_rel_path, diff_out_rel_path], ...]}`. Paths relative to repo root.
- `glitch_spec_suppressions.json` updated (C39, Morgan Walsh): added `file_prefix` entries for `LTG_TOOL_character_lineup_*` G006+G007. Total: 26 exact + 2 prefix entries.

### C39 New tools (Hana Okonkwo + Diego Vargas + Lee Tanaka)
| `LTG_TOOL_bg_school_hallway.py` | Hana Okonkwo / Cycle 38 | **School Hallway background v003 — figure-ground separation pass.** Cosmo's cardigan (RW-08 #A89BBF) was identical to v002 locker lavender — character merged with background. LOCKER_LAV (168,155,191)→(216,208,190), LOCKER_SAGE (122,154,122)→(154,178,148). Value separation: +30+ units from Cosmo's mid-tone lavender. Canvas 1280×720. Output: `LTG_ENV_school_hallway.png`. | Pillow |
| `LTG_TOOL_env_grandma_living_room.py` | Hana Okonkwo / Cycle 39 | **Grandma Miri's Living Room background v002 — C39 update.** Key light shifted to SUNLIT_AMBER from right-upper quadrant. CRT glow enhanced. Additional lived-in details. Canvas 1280×720. Output: `output/backgrounds/environments/LTG_ENV_grandma_living_room.png`. | Pillow |
| `LTG_TOOL_sb_pilot_cold_open.py` | Diego Vargas / Cycle 39 | **Pilot Cold Open 6-panel storyboard v003.** Changes: (1) NEW P01 exterior night shot — residential neighborhood establishing; (2) P12 TWO-SHOT REFRAME — center-weighted equal presence; (3) P13 MIRROR COMPOSITION — commitment beat, both at eye-level, directional ELEC_CYAN glow; (4) Old P6 "THE NOTICING" retired. Output: `output/storyboards/LTG_SB_pilot_cold_open.png`. | Pillow |
| `LTG_TOOL_sight_line_diagnostic.py` | Lee Tanaka / Cycle 39 | **Sight-Line Diagnostic Tool.** Given a style frame/ENV PNG + character eye coordinates + gaze aim coordinates + target object coordinates, computes the sight-line vector and visualises gaze connection. Key concept: `eye_xy` = eye position; `aim_xy` = where the gaze is directed (may differ from target for broken sight-line detection); `target_xy` = actual subject. `miss_px` = perpendicular distance from target to the eye→aim ray. Draws: CYAN ray (eye→aim, extended), MAGENTA target bbox, AMBER body axis. Annotates: gaze angle (deg), miss distance (px), PASS (miss ≤ 15px) / WARN (15–45px) / FAIL (> 45px). CLI: `--image IMAGE.png --eye X Y --aim X Y --target X Y [--target-box X0 Y0 X1 Y1] [--body-top X Y] [--body-bot X Y] [--threshold 15] [--label TEXT] [--no-save]`. Built-in tests: `--self-test` (synthetic PASS/WARN/FAIL), `--sf01-test` (SF01 v005 FAIL / v006 PASS comparison). Module API: `run_sight_line_check(image_path, eye_xy, target_xy, aim_xy=None, ...)→dict`. Output: `LTG_SNAP_sightline_<label>.png` (≤1280×1280px) in `output/production/`. | Pillow |

### C40 New tools (Jordan Reed)
| `LTG_TOOL_pixel_font_v001.py` | Jordan Reed / Cycle 40 | **Pixel Font Utility.** Draws bitmap-style text onto any PIL ImageDraw canvas with no font file dependency. Built-in 5×7 glyph map for A–Z + 0–9 + space. Key function: `draw_pixel_text(draw, x, y, text, color, scale=1)` — renders each glyph as filled pixel (scale=1) or filled rectangle blocks (scale>1). Returns `(end_x, end_y)` for chaining. Helper: `measure_pixel_text(text, scale=1) → (width, height)` for centering. Intended for in-world prop labels (fridge notes, sticky labels, signage) where output must read as hand-annotated. Self-test generates `output/production/LTG_TEST_pixel_font_v001.png` with "MIRI" at scale 1, 2, and 3 + full alphabet+digit sweep. Import: `from LTG_TOOL_pixel_font_v001 import draw_pixel_text, measure_pixel_text`. | Pillow |

### C41 New tools (Jordan Reed)
| `LTG_TOOL_sf04_luma_byte_v005.py` | Jordan Reed / Cycle 41 | SF04 "The Dynamic" — full rebuild v005 (C40 producer mandate). Fixes: (1) Lamp color → canonical #FF8C00 / SUNLIT_AMBER for halo, rim light; (2) Warm/cool separation 1.1→36.6 PASS via strong SUNLIT_AMBER top-half lamp halo (alpha 90) + BYTE_TEAL floor bounce bottom-half wash (alpha 85) — C35 proven approach; (3) Byte-teal floor bounce between Luma and Byte characters added per brief spec; (4) Value floor PASS (≤30) via deep shadow under couch (NEAR_BLACK_WARM alpha 200); (5) Domestic scene enriched: couch + rug for warm-world read. render_qa GRADE: WARN (color_fidelity pre-existing issue, inherent to alpha compositing — same as v004; all other metrics PASS). Face test gate: head_r=42px (pitch scale), above sprint-scale threshold. Output: `LTG_SF_luma_byte_v005.png` (1280×720). | Pillow, LTG_TOOL_procedural_draw |

### C41 New tools (Maya Santos)
| `LTG_TOOL_face_curve_validator.py` | Maya Santos / Cycle 41 | **Face Curve Spec Validator.** Renders bezier spec expressions from the `luma_face_curve_spec.md` control point dicts and reports geometry metrics: eye width (P0→P2), lid-top-drop delta (from neutral P1.y), mouth corner/ctrl positions, brow apex relative to FC. Flags deviations from canonical values using configurable tolerances. All 10 expressions (NEUTRAL, RECKLESS, THE_NOTICING, THE_NOTICING_DOUBT, WORRIED, ALARMED, FRUSTRATED + C40 supplement CONFIDENT/SOFT_SURPRISE/DETERMINED) validate at OVERALL PASS. Eye width canonical = 100px (v002 corrected spec). Right-eye lid drop asymmetry check: warns if re_lid_drop < -4 in non-ALARMED expressions (signature preservation guard). Outputs annotated contact sheet 1280×929px + text report. CLI: `--expression NAME`, `--all`, `--output-sheet PATH`, `--report PATH`, `--tolerance-eye N`, `--tolerance-brow N`, `--tolerance-mouth N`. Exit: 0=PASS, 1=WARN, 2=FAIL. Output: `output/production/LTG_TOOL_face_curve_validator_sheet.png`. | Pillow |

### C41 Tool updates (Maya Santos)
- `LTG_TOOL_grandma_miri_expression_sheet.py` → **v005 (C41)**: Added `MIRI_HEAD_RATIO = 3.2` as an explicit canonical proportion constant. Closes M001 spec lint WARN (was inferred, now explicit). No visual change. Title updated to v005. Regenerated: `LTG_CHAR_grandma_miri_expression_sheet.png`.
- `LTG_TOOL_luma_expression_sheet.py` → **v013 (C41, Maya Santos)**: Tier-1 silhouette body postures. (1) SURPRISED→**ALARMED**: hair=excited, pupils wide + upward, body lean-back, `draw_alarmed_arms()` — left arm raised toward chin, right arm raised/splayed outward — hands-near-face read. (2) DELIGHTED→**RECKLESS**: arms spread wide/low (energy-outward posture), body tilt -6°, wide pupils + side gaze, hair=excited. (3) FRUSTRATED: existing arms-crossed preserved (Tier-1 compliant). (4) THE NOTICING: rightward gaze fix via `_FACE_CURVES_OVERRIDES` dict (`LI_CENTER_dx: +6, RI_CENTER_dx: +6`) — pupils now aim frame-RIGHT per Lee Tanaka sight-line brief. Body still / forward lean preserved. RPD baseline: all 6 Tier-1 pairs PASS or WARN; only FAIL is WORRIED↔FRUSTRATED (Tier-2, expected). ALARMED+RECKLESS added to `_FACE_CURVES_EXPR_MAP` and HOODIE_MAP/BG_MAP. Output: `LTG_CHAR_luma_expression_sheet_v013.png`.
- `LTG_TOOL_byte_expression_sheet.py` → **v007 (C41, Maya Santos)**: UNGUARDED WARMTH body-pose delta (Sam Kowalski brief). arm_l_dy -5→-14, arm_r_dy -5→-16, float_offset -4 (cy hover shift), lower_l_angle 8°, lower_r_angle 8° (toe-in). Added `float_offset` parsing (`cy = cy + float_offset`) and toe-in trapezoid polygon leg rendering (when lower_l/r_angle > 0, rectangle replaced by isosceles trapezoid polygon). Panel 09 UNGUARDED WARMTH no longer appears in any WARN/FAIL RPD pair — bilateral symmetric raises distinguish clearly from RELUCTANT JOY asymmetric. Output: `LTG_CHAR_byte_expression_sheet_v007.png`.

### C41 New tools (Diego Vargas + Sam Kowalski)
| `LTG_TOOL_sb_cold_open_P03.py` | Diego Vargas / Cycle 41 | **Cold Open Panel P03 — CU MONITOR — First Pixel.** Beat: first Glitch Palette moment in the episode. Tight on the hero CRT prop. Static on screen. One ELEC_CYAN pixel appears in the lower-right corner — the FIRST appearance of the Glitch Palette. Shot: CU Object, eye level (~8" off floor), slight low angle, static. Real World (den) palette — single ELEC_CYAN pixel is the only Glitch color. Arc: CURIOUS. Output: `output/storyboards/panels/LTG_SB_coldopen_panel_03.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P06.py` | Diego Vargas / Cycle 41 | **Cold Open Panel P06 — CU MONITOR SCREEN — Byte's First Appearance.** Beat: the REVEAL. Byte's face pressed against the inside of the CRT glass. FIRST APPEARANCE OF BYTE. Expression: DISGUSTED / RELUCTANT CURIOSITY. Shot: CU monitor screen fills frame, straight-on eye level, static. Full Glitch Palette (screen surface) / warm den edges (bezel shadow). Arc: CURIOUS/DISCOVERY. Output: `output/storyboards/panels/LTG_SB_coldopen_panel_06.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P08.py` | Diego Vargas / Cycle 41 | **Cold Open Panel P08 — MED — Byte Full Character Reveal.** Beat: Byte is OUT. Standing in the real world for the first time. Full body reveal: compact, angular, tiny (head barely above cable bundles). Pixel confetti drifting. Thin desaturation ring at feet. Camera at Byte's eye level (~6" off floor), static. Output: `output/storyboards/panels/LTG_SB_coldopen_panel_08.png`. | Pillow |
| `LTG_TOOL_colorkey_glitch_covetous_gen.py` | Sam Kowalski / Cycle 41 | **Color Key Generator — COVETOUS Glitch Style Frame.** Generates 640×360 color key thumbnail for the COVETOUS Glitch style frame. Scene: Glitch alone at the threshold — watching Real World warmth from the void. VOID_BLACK base fill, UV_PURPLE dominant ambient — Glitch Layer identity. No warm light on Glitch's body (premise: separation). Cross-references `sf_covetous_glitch_v001`. Actioned from Jayden Torres C16 critique (79/100 on Glitch, COVETOUS called "the strongest new design moment"). Output: `LTG_COLOR_colorkey_glitch_covetous.png`. | Pillow |

### C42 New tools (Diego Vargas)
| `LTG_TOOL_sb_cold_open_P23.py` | Diego Vargas / Cycle 42 | **Cold Open Panel P23 — MED — THE PROMISE SHOT.** Beat: THE SHOW'S THESIS IN ONE IMAGE. Luma and Byte from behind, looking at blazing monitor wall — every screen at max Glitch Chaos intensity. Luma: square shoulders, chin up, right arm raised — FACING THIS. Byte on her shoulder: grumpy but PRESENT. Luma warm palette (hoodie orange, hair) vs Full Glitch Chaos (room/monitors). Push-in camera move annotated. Dialogue: "Grandma's gonna be so mad." / "...I will observe from your shoulder." Arc: TENSE (hot magenta border). Also applied P06 cracked-eye divergence fix and P08 level-forward gaze fix from Lee Tanaka C41 sight-line review. Output: `output/storyboards/panels/LTG_SB_cold_open_P23.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P24.py` | Diego Vargas / Cycle 42 | **Cold Open Panel P24 — WIDE/MED — CHAOS APEX: THE BREACH.** Beat: THE HOOK FRAME. Every monitor breached. Glitchkin swarm (28 chars, varied scale/depth) pouring out in pixel-glitch wave. Warm den overwritten by digital invasion — Full Glitch Chaos, warm palette gone. Luma FG hero (low angle, Dutch tilt 12° LEFT, tall heroic read): chin up, adrenaline grin forming, arm raised. Byte on shoulder: resigned dignity. Luma retains warm color identity (still-point contrast). Arc border: PITCH BEAT (bright cyan, 4px). Output: `output/storyboards/panels/LTG_SB_cold_open_P24.png`. | Pillow |

### C43 New tools (Diego Vargas)
| `LTG_TOOL_sb_cold_open_P07.py` | Diego Vargas / Cycle 43 | **Cold Open Panel P07 — MED WIDE — Monitor Bulging / Dutch 8° CW / Byte Phases Through.** Beat: Byte mid-phase emergence from CRT — lower half inside screen (desaturated, opacity-reduced), upper half real world (full teal, pixel confetti at crossing threshold). Monitor bows convex with white-hot center; distortion rings break OUTSIDE bezel boundary (physics violation = danger signal). Low angle (floor level). Dutch 8° CW applied to draw area only (caption stays horizontal). Expression: DETERMINED + ALARMED — eyes wide, slight open pixel mouth, upward emergence vector annotation. Warm domestic light visible far-left margin only (losing to ELEC_CYAN). Arc: TENSE → BREACH (Hot Magenta border). Face test: `--char byte` not supported by LTG_TOOL_character_face_test.py — noted in C43 completion report. Output: `output/storyboards/panels/LTG_SB_cold_open_P07.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P09.py` | Diego Vargas / Cycle 43 | **Cold Open Panel P09 — MED WIDE — Byte Floating / SPOTTED / Luma Asleep.** Beat: SPOTTED moment — first eye contact (one-sided). Byte frame-right, floating 18" off floor (feet visibly above ground), ELEC_CYAN ambient glow (non-directional). Normal eye iris shifted LEFT toward Luma (sight-line). Cracked eye: SEARCHING/PROCESSING. Body lean -2.5°, head cocked toward Luma. Gravity ghost: confetti drifts DOWN beneath Byte. Desaturation ring on floor below feet. Dotted sight-line annotation from eye to Luma. Luma: asleep on couch, frame-left background-scale, warm LUMA_HOODIE orange. BG monitors: normal gray-green CRT static (breach was Byte-specific). Warm/cool gradient: warm-orange left / ELEC_CYAN glow right. Arc: CURIOUS / FIRST ENCOUNTER (ELEC_CYAN border). Face test: `--char byte` not supported — noted in C43 completion report. Output: `output/storyboards/panels/LTG_SB_cold_open_P09.png`. | Pillow |
| `LTG_TOOL_sb_ep05_covetous.py` | Diego Vargas / Cycle 43 | **EP05 COVETOUS Beat — Three-Character Triangulation Storyboard Panel.** Glitch's first appearance (Episode 5). Three-character triangulation: Glitch COVETOUS (left, amber-in-void, bilateral acid-slit eyes [[5,5,5],[0,5,0],[0,0,0]], +12° lean, arms raised, spike_h=12) — Byte barrier (midground center, smaller than Glitch, arms extended, protective lean) — Luma warm zone (right, LUMA_HOODIE canonical orange). Color arc: Glitch amber/void → Byte teal → Luma warm orange. Glitch NOT warmed by Luma (UV Purple ambient only). Soft Gold radial from Luma (alpha max 50, 120px radius). UV_PURPLE border (Glitch Layer). Barrier dashed-line annotation, color arc labels, rule reminder. Per glitch_covetous_styleframe_spec.md C42 and story_bible_v003.md §EP5. Output: `output/storyboards/panels/LTG_SB_ep05_covetous.png`. | Pillow |

### C43 New tools (Lee Tanaka)
| `LTG_TOOL_sf_covetous_glitch_c43.py` | Lee Tanaka / Cycle 43 | **COVETOUS Glitch Style Frame — Character Staging Enhancement v3.0.0.** Based on Rin Yamamoto's C42 three-character triangulation v2.0.0. C43 additions: (1) Luma SENSING UNEASE expression — THE NOTICING variant for hostile space (asymmetric eye_r_L=7/eye_r_R=5 at head_r=33; face gate PASS); (2) Luma 5° backward lean + UV_PURPLE rim on left shoulder; (3) Byte barrier arm widening (spread arms — protective posture read); (4) ACID_GREEN dashed covet-vector sight-line from Glitch right eye to Luma head. All G001/G004/G008 Glitch spec rules preserved. Output overwrites `LTG_COLOR_sf_covetous_glitch.png` in place. Per `glitch_covetous_styleframe_spec.md` C42. | Pillow |

### C42 New tools (Jordan Reed)
| `LTG_TOOL_style_frame_04_resolution.py` | Jordan Reed / Cycle 42 | **SF04 "Resolution" — full rebuild from spec (C42 brief, Alex Chen).** Luma returning to the Real World after the Glitch Layer crossing. Grandma Miri's kitchen, morning. Warm amber dominant (SUNLIT_AMBER window shaft + lamp halo, top-half alpha 115 linear), cool floor bounce from CRT doorway glow (CRT_GLOW bottom-half alpha 95 — C35 asymmetric alpha approach). Byte appears as a faded signal in the CRT through the doorway (barely legible ghost + cyan pixel eyes). One Glitch-residue detail: single ELEC_CYAN pixel-grid streak in Luma's left sleeve. Film grain + scanline hint + vignette. render_qa: warm/cool separation=13.2 PASS (target ≥12), value range PASS, silhouette distinct. Face test gate: head_r=42px (pitch scale, above sprint-scale threshold). Color fidelity WARN = pre-existing alpha-compositing pattern. Output: `output/style_frames/LTG_COLOR_styleframe_sf04.png` (1280×720). | Pillow, LTG_TOOL_procedural_draw |

### C42 New tools (Ryo Hasegawa)
| `LTG_TOOL_cosmo_motion.py` | Ryo Hasegawa / Cycle 42 | **Cosmo Motion Spec Sheet.** 4-panel sheet (1280×720): Idle/Observing, Startled, Analysis Lean, Reluctant Move. Motion vocabulary: upright/contained/deliberate; rectangularity throughout; notebook as secondary-mass anchor (lags body +1.5 beat on sudden shifts); glasses tilt diagnostic (neutral 7° → startled peak 13–15° → recovery 9°). Startled: both arms jut briefly, snap back. Analysis lean: forward 6–8° only, head tilts right. Reluctant move: body rigid 10–12° lean, arm NOT pumping. Sheet geometry config-driven (sheet_geometry_config.json, family="cosmo"). Output: `output/characters/motion/LTG_CHAR_cosmo_motion.png`. | Pillow |

### C41 New tools (Maya Santos) — Diagram
| `LTG_TOOL_glitch_body_primitive_diagram_gen.py` | Maya Santos / Cycle 41 | **Glitch Body Primitive Diagram v001.** Closes Alex Chen C41 P2 and Daisuke Kobayashi C14 P8/C16 P4 (4-cycle open item — verbal spec existed, no diagram). 2-column reference sheet: Left — labeled anatomy diagram with proportions/vertices/construction notes (rx=34, ry=38, ry>rx always); Right — expression silhouettes (NEUTRAL/MISCHIEVOUS/PANICKED/TRIUMPHANT) showing tilt/squash/stretch range + Glitch vs Glitchkin visual distinction. Constants per glitch_body_diamond_spec.md. Output: `output/characters/LTG_CHAR_glitch_body_primitive_diagram.png`. | Pillow |

### C41 Tool updates (Morgan Walsh)
- `LTG_TOOL_precritique_qa.py` → **v2.9.0** (C41, Morgan Walsh): CYCLE_LABEL bumped to C41. Confirmed Task 1: README version sequence v2.7.0/v2.8.0 is unambiguous (two v2.7.0 branches merged to single v2.8.0 by Morgan+Kai; Rin's Section 10 integration also labeled v2.8.0 per C40 delivery). Confirmed Task 2: Section 10 alpha_blend_lint integration (Rin Yamamoto, C40) verified present — FLAT_FILL=FAIL, LOW_SIGNAL=WARN, all-skip=PASS. C41 baseline run: OVERALL WARN (PASS=255, WARN=35, FAIL=0). 4 UNLISTED tools registered in this update (sb_cold_open_P03/P06/P08, colorkey_glitch_covetous_gen). Report version strings updated to v2.9.0.

### C40 New tools (Ryo Hasegawa)
| `LTG_TOOL_sheet_geometry_calibrate.py` | Ryo Hasegawa / Cycle 40 | **Motion Sheet Geometry Calibrator.** Scans the first 100 rows of each motion spec sheet PNG to auto-detect where panels actually begin (header_h, panel_top_abs). Writes per-family geometry to `output/tools/sheet_geometry_config.json`. Two-strategy detection: (1) first bright row > threshold (light-background sheets: Luma); (2) header-end detection for dark-background sheets (Byte: VOID_BLACK canvas). Detected values: luma panel_top=54, byte panel_top=56. Config loaded by `LTG_TOOL_motion_spec_lint.py` and motion generators. Eliminates false WARNs from hard-coded ANNOT_ZONE_ABS_Y_START/BADGE_PANEL_TOP coords that didn't match sheets with non-standard HEADER_H. Re-run after any motion sheet regeneration. CLI: `python3 LTG_TOOL_sheet_geometry_calibrate.py [--luma PATH] [--byte PATH] [--out PATH] [--verbose]`. Module API: `calibrate_sheet(path, family) → dict`, `calibrate_all() → config_dict`, `load_or_calibrate(config_path, family) → (header_h, panel_top_abs)`. Actioned from Ryo's C39 ideabox idea. | Pillow, numpy |

### C40 Tool updates (Ryo Hasegawa)
- `LTG_TOOL_motion_spec_lint.py` → **C40 update** (Ryo Hasegawa): Sheet geometry auto-detection added. Loads `sheet_geometry_config.json` (generated by `LTG_TOOL_sheet_geometry_calibrate.py`) at lint time. `_load_geo_config()`, `_family_from_filename()`, `_get_zone_params()` functions added. `check_annotation_occupancy()`, `check_beat_badge_zones()`, `check_timing_colors()` updated to accept calibrated zone params from caller. `lint_motion_spec()` now accepts `--geo-config` argument. `format_report()` shows `geo:config` or `geo:legacy defaults` tag per sheet. Expected panel count now taken from config (Luma=4, Byte=4) — fixes pre-C40 mismatch where both sheets were evaluated against wrong panel count. `geo_source` field added to result dict.
- `LTG_TOOL_luma_motion.py` → **C40 update** (Ryo Hasegawa): `_load_header_h()` added — loads `panel_top_abs` for family "luma" from `sheet_geometry_config.json` at module import time. `_LUMA_PANEL_TOP` replaces hard-coded `PAD + 40` in `panel_origin()`. `_TITLE_H` back-calculated as `max(_LUMA_PANEL_TOP - PAD, 40)` for `PANEL_H` computation. Falls back to 54 when config absent.
- `LTG_TOOL_byte_motion.py` → **C40 update** (Ryo Hasegawa): `_load_header_h_byte()` added — loads `panel_top_abs` for family "byte" from `sheet_geometry_config.json` at module import time. `HEADER_H` now config-driven; falls back to 44 when config absent.
- `output/tools/sheet_geometry_config.json` (C40, Ryo Hasegawa): Auto-generated config file. Luma: header_h=54, panel_top=54, annot_y=80..228, panels=4. Byte: header_h=56, panel_top=56, annot_y=82..230, panels=4. Cosmo: placeholder (56/56/82..230/3 — no sheet yet). Version 1.

### C40 New tools and updates (Morgan Walsh)
| `ci_known_issues.json` | Morgan Walsh / Cycle 40 | **Known-issues registry for LTG_TOOL_ci_suite.py v1.1.0.** JSON file (not a script) listing 26 expected W004 draw-order advisories and 12 Glitch spec lint false positives from the C39 baseline. Format: `{"known_issues": [{"check": "draw_order_lint", "file": "LTG_TOOL_foo.py", "code": "W004", "reason": "..."}]}`. Loaded automatically by `ci_suite v1.1.0` when `ci_known_issues.json` is present in the tools dir (or via `--known-issues PATH`). Matching issues are annotated KNOWN in the report — does not suppress WARN/FAIL grade. First candidates: W004 false positives (img.paste/alpha_composite composite patterns with no following draw calls) and G005/G006/G007 false positives (non-Glitch files containing GL color constants). | n/a |
- `LTG_TOOL_ci_suite.py` → **v1.1.0** (C40, Morgan Walsh): `--known-issues PATH` flag added. Loads JSON file of expected/pre-existing WARNs; marks matching issues as KNOWN in report output. Known issues annotated but not suppressed — overall WARN/FAIL grade unchanged. `load_known_issues(path) → dict` exported for programmatic use. `run_suite()` gains optional `known_issues` parameter. `format_suite_report()` shows total known count in OVERALL line. Auto-discovers `ci_known_issues.json` in tools-dir if `--known-issues` not specified. Seeded with 26 W004 draw-order FPs + 12 Glitch spec lint FPs from C39 CI report.
- `LTG_TOOL_precritique_qa.py` → **v2.8.0** (C40, Morgan Walsh + Kai Nakamura): Merges two parallel v2.7.0 branches that were written simultaneously in C39. Morgan Walsh v2.7.0: `ARC_DIFF_PAIRS` loaded from `arc_diff_config.json` at startup via `_load_arc_diff_pairs()`. Kai Nakamura v2.7.0: `run_color_verify()` uses `_check_color_fidelity_lab()` from render_qa v2.0.0, LAB ΔE flagging. Both changes present in the single canonical file. CYCLE_LABEL updated to C40. Report header updated to v2.8.0.

### C42 New tools (Ryo Hasegawa)
| `LTG_TOOL_cosmo_motion.py` | Ryo Hasegawa / Cycle 42 | **Cosmo Motion Spec Sheet v001.** 4-panel motion spec: (1) IDLE/OBSERVING — neutral upright stance, notebook tucked under left arm, glasses at 7° CCW neutral; (2) STARTLED — glasses peak tilt 14° (from 7° neutral), both arms jut briefly, notebook jostled (lags +1.5 beats); (3) ANALYSIS LEAN — 6-8° forward tilt, head tilts right, notebook extended (= engaged signal); (4) RELUCTANT MOVE — 12° lean but rigid torso, arms DO NOT pump (contrast with Luma sprint), notebook clutched tight (stress indicator). BEAT_COLOR=(80,120,200) blue family. `sheet_geometry_config.json` updated: cosmo family expected_panels=4, panel_top_abs=54, beat_color=[80,120,200]. Lint baseline: PASS=5, WARN=1 (annotation_occupancy — known structural WARNs for light-bg text-annotation sheets). Canvas: 1280×720. Output: `output/characters/motion/LTG_CHAR_cosmo_motion.png`. | Pillow |

### C42 New tools (Jordan Reed)
| `LTG_TOOL_style_frame_04_resolution.py` | Jordan Reed / Cycle 42 | **SF04 "Resolution" — full rebuild from spec (C42 brief, Alex Chen).** Concept: Luma returning to the Real World after the Glitch Layer crossing. Grandma Miri's kitchen, morning. Warm amber dominant (SUNLIT_AMBER window shaft + lamp halo, top-half alpha 115 linear), cool floor bounce from CRT doorway glow (CRT_GLOW bottom-half alpha 95 — C35 asymmetric alpha approach). Byte appears as a faded signal in the CRT through the doorway (barely legible ghost + cyan pixel eyes). One Glitch-residue detail: single ELEC_CYAN pixel-grid streak in Luma's left sleeve. Film grain + scanline hint + vignette (scene through CRT frame). render_qa: warm/cool separation=13.2 PASS (target ≥12), value range PASS, silhouette distinct. Face test gate: head_r=42px (pitch scale, above sprint-scale threshold). Color fidelity WARN = pre-existing alpha-compositing pattern. Output: `output/style_frames/LTG_COLOR_styleframe_sf04.png` (1280×720). | Pillow, LTG_TOOL_procedural_draw |
| `LTG_TOOL_style_frame_05_relationship.py` | Jordan Reed / Cycle 44 | **SF05 "The Passing" — Miri+Luma relationship key (C44, closes C17 gap).** "CRT as matrilineal heirloom" — technology of seeing passed between women (Alex ideabox C44, Marcus Webb + Eleanor Whitfield 74/100 gap). Grandma Miri seated at kitchen table (WARM ATTENTION expression), Luma standing beside her (wide-eyed curiosity). Both watching CRT through doorway in pre-dawn morning. Real World only — zero GL palette. render_qa: warm/cool separation=16.7 PASS (SUNLIT_AMBER top α0→90 + CRT_COOL_SPILL bottom α0→85), value range min=18/max=254 PASS, silhouette distinct, line_weight 0 outliers PASS. color_verify SUNLIT_AMBER delta=1.3° PASS. Face test gate: Miri head_r=38px + Luma head_r=40px (both above sprint threshold, not triggered). color fidelity WARN = pre-existing alpha-compositing pattern (same as SF01–SF04). Dual-blush disambiguation: Luma quiet (Pride Override inactive). Output: `output/color/style_frames/LTG_COLOR_styleframe_sf05.png` (1280×720 native canvas). | Pillow |

### C44 New tools (Kai Nakamura)
| `LTG_TOOL_project_paths.py` | Kai Nakamura / Cycle 44 | **Project Root Resolver v1.0.0.** Eliminates all hardcoded `/home/wipkat/team` paths across the toolchain. `project_root()` traverses parent directories from `__file__` until it finds `CLAUDE.md` (the sentinel file), caches result via `lru_cache`. Convenience helpers: `output_dir(*parts)` → path inside `output/`; `tools_dir(*parts)` → path inside `output/tools/`; `ensure_dir(path)` → `mkdir -p` + returns path; `resolve_output(category, name)` → canonical output path by category shorthand (bg/storyboards/sf/ch/ck/tools/pr). `audit_hardcoded_paths()` scans all `*.py` in `output/tools/` for `/home/` occurrences, prints grouped report. CLI: `--audit` (exit 1 on any hit, suitable as CI gate), `--root`, `--output`, `--tools`. Migration guide in module docstring. All generators should replace `"/home/wipkat/team/output/..."` with `output_dir("subdir", ..., "file.png")` + `ensure_dir(path.parent)`. | stdlib only |
| `vp_spec_config.json` | Kai Nakamura / Cycle 44 | **VP Spec Config v1.0.0.** Canonical vanishing point specifications for all 11 LTG environment generators. Format: `generator`, `output_filename`, `canvas_w/h`, `vp_x/vp_y` (px, null for glitch-layer), `tolerance_px`, `world_type`, `notes`. 7 real-world environments with pixel-precise VP values (classroom: 192,230; study interior: 230,273; kitchen: 512,273; tech den: 820,295; main street: 742,273; school hallway: 640,158; living room: 704,259). 4 glitch-layer environments with null VP (other_side, glitchlayer_frame, glitch_layer_frame, glitch_layer_encounter — abstract depth, no perspective VP). Used by `LTG_TOOL_sobel_vp_detect.py --vp-config` for automated VP compliance checking. | stdlib only |

### C44 Updates (Kai Nakamura)
- `LTG_TOOL_sobel_vp_detect.py` → **v1.1.0** (C44, Kai Nakamura): Added `vp_spec_config.json` integration. `load_vp_config(config_path) → dict` — loads and indexes JSON by `output_filename`. `lookup_vp_spec(config_index, image_path) → (vp_x, vp_y, tolerance_px, world_type)` — auto-resolves VP spec for a given image. `detect_vp_batch_with_config(directory, config_path) → list` — batch scan with per-file VP specs; skips glitch-layer files (PASS, no VP required). CLI: `--vp-config PATH` flag — when batch target is a directory, uses config for per-file lookup; for single file, merges config values with CLI flags (CLI takes precedence). Usage: `python LTG_TOOL_sobel_vp_detect.py output/backgrounds/environments/ --vp-config vp_spec_config.json`. Backward compatible — all v1.0.0 flags still work.

### C44 New tools (Rin Yamamoto)
| `LTG_TOOL_uv_purple_linter.py` | Rin Yamamoto / Cycle 44 | **UV_PURPLE Dominance Linter v1.0.0.** Enforces Glitch Layer world-rule: UV_PURPLE (#7B2FBE) + ELEC_CYAN (#00F0FF) must collectively dominate non-black pixel area. Check A: combined LAB ΔE ≤ 15 fraction of non-black pixels — PASS ≥ 20%, WARN 10–19%, FAIL < 10%. VOID_BLACK = max(R,G,B) < 30. Check B: warm-hue contamination (LAB h° 30°–80°, chroma C* ≥ 8) — PASS < 5%, WARN ≥ 5%. cv2 LAB conversion with 8-bit unscaling (L: 0-255→0-100, a/b: 0-255→-128 to 127) for accurate ΔE and chroma. NumPy pure-LAB fallback when cv2 absent. World type inferred via `LTG_TOOL_world_type_infer`; override with `--world-type glitch`. CLI: `python LTG_TOOL_uv_purple_linter.py image.png` / `--batch dir/`. Module API: `lint_uv_purple_dominance()`, `batch_lint()`, `run_glitch_layer_dominance_check()` (Section 11 precritique_qa). C44 run results: FAIL × 2 (LTG_COLOR_sf_covetous_glitch + LTG_SF_covetous_glitch_v001 — UV_PURPLE_DARK dark variants score < 1% vs canonical LAB), WARN × 3 (ENV frames 17%), PASS × 1 (bg_glitch_layer_encounter 22.7%). Integrated into precritique_qa v2.13.0 Section 11. | Pillow, NumPy, cv2 (optional) |

### C44 Updates (Rin Yamamoto)
- `LTG_TOOL_world_type_infer.py` → **v1.2.0** (C44, Rin Yamamoto): GLITCH rule regex extended with `covetous[_-]?glitch|sf[_-]?covetous` patterns. Required for `LTG_TOOL_uv_purple_linter.py` to correctly auto-classify `LTG_COLOR_sf_covetous_glitch.png` and `LTG_SF_covetous_glitch_v001.png` as GLITCH world type. Backward compatible — no existing rules changed.
- `LTG_TOOL_precritique_qa.py` → **v2.13.0** (C44, Rin Yamamoto): UV_PURPLE Dominance Lint Section 11 added. `GLITCH_LAYER_PNGS` registry (6 assets: 2 COVETOUS style frames + 4 ENV Glitch Layer backgrounds). `_load_uv_purple_linter()` lazy loader. `run_uv_purple_lint()` runner — calls `run_glitch_layer_dominance_check()` on registered assets. `build_report()` updated: Section 11 detail output + summary table rows for Alpha Blend Lint and UV_PURPLE Dominance Lint. `main()` updated: `[11/11]` console step. Overall exit-code logic updated to include `uv_purple_res["overall"]`.

### C46 New tools (Ryo Hasegawa)
| `LTG_TOOL_miri_motion_v002.py` | Ryo Hasegawa / Cycle 46 | **Grandma Miri Motion Spec Sheet v002 — Emotional Warmth Pacing.** Second Miri beat arc per Alex Chen C46 brief. 4 panels: (1) OBSERVING STILL — hands in lap, absolute zero drift, slow blink, held attention indefinitely; (2) RECOGNITION — head tilt leads at beat 1 before body, -2° lean at beat 1.5, hands open from lap at beat 2, cardigan lag at beat 3.5; (3) WARMTH BURST — arms spread wide bilaterally at shoulder level, SUNLIT_AMBER (212,146,58) radial glow halo, eyes crinkle fully closed at beat 1.5 peak, cardigan hem flutter +2.0b, HOLD 4–6 frames; (4) FOND SETTLE — arms lower over 4 beats (ghosted B3 position shown), eyes reopen beat 1, slight smile +6 frames, blush sustained through B4, settles = mirror of B1. SUNLIT_AMBER = Real World warm light only. New draw params: `arms_open=True`, `hands_in_lap=True`. BEAT_COLOR=(80,120,200) blue family. Canvas: 1280×720. Output: `output/characters/motion/LTG_CHAR_miri_motion_v002.png`. | Pillow |

### C46 Updates (Ryo Hasegawa)
- `LTG_TOOL_motion_spec_lint.py` → **C46 update** (Ryo Hasegawa): (1) Dark-sheet annotation_occupancy false WARN fix for Byte + Glitch: new `_dark_sheet_params_from_config()` helper; `_get_zone_params()` now returns 8-tuple (dark_sheet, occupancy_threshold_dark); `_count_non_bg()` new `dark_sheet` param — bright-pixel counting (mean>40) for void-black panels; `check_annotation_occupancy()` uses 1% threshold and `dark-bright` bg_mode label. (2) `_family_from_filename()` extended with `miri_v002` case (checked before `miri` to avoid prefix collision).
- `LTG_TOOL_precritique_qa.py` → **v2.14.1** (Ryo Hasegawa): CYCLE_LABEL=C46. MOTION_SHEETS extended with `LTG_CHAR_miri_motion_v002.png` (4 panels). 6 motion sheets total in lint pipeline.
- `sheet_geometry_config.json` → **version 2** (Ryo Hasegawa): (1) `background_style: "dark"` and `occupancy_threshold_dark: 0.010` added to byte + glitch families. (2) `miri_v002` family added: panel_top_abs=54, expected_panels=4, beat_color=[80,120,200], annotation_bg_color=[248,244,238].

### C46 Updates (Kai Nakamura)
- **README Script Index — C46 catch-up pass:** Registered 7 previously unlisted tools identified by `LTG_TOOL_precritique_qa` Section 7 (README sync WARN, 3 unlisted as of C44): `LTG_TOOL_sf_covetous_glitch_c43.py` (Lee Tanaka, C43), `LTG_TOOL_sb_cold_open_P10/P11.py` (Diego Vargas, C44), `LTG_TOOL_sf_miri_luma_handoff.py` (Maya Santos, C44), `LTG_TOOL_lineup_tier_depth_sketch.py` (Lee Tanaka, C45), `LTG_TOOL_sb_cold_open_P14/P15.py` (Diego Vargas, C45). Also registered `LTG_TOOL_logo_asymmetric.py` v003 (Sam Kowalski, C44) as an update entry. README header updated to C46.
- `LTG_TOOL_char_spec_lint.py` → **v1.3.0** (C46, Kai Nakamura — Alex Chen P1 brief): Filename pattern fix. All `_CHAR_REGISTRY` entries now include the canonical no-version-suffix pattern alongside the `_v*.py` glob. The `_v*.py`-only patterns never matched the canonical files on disk (e.g. `LTG_TOOL_grandma_miri_expression_sheet.py` — no `_v*` suffix), causing all Miri M001–M005 checks to always WARN with "no generator found". Same issue affected luma, cosmo, and byte expression sheet / turnaround patterns. `_resolve_generators()` updated with deduplication (dict preserving insertion order) to prevent the no-suffix and versioned forms from returning the same file twice. Affected checks: M001–M005 (Miri) now correctly read the canonical generator. L001–L005, S001–S005, B001–B005 similarly unblocked.

### C45 New tools (Ryo Hasegawa)
| `LTG_TOOL_glitch_motion.py` | Ryo Hasegawa / Cycle 45 | **Glitch Motion Spec Sheet v001.** First motion spec for the Glitch (Glitchkin antagonist) character. 4-panel beat arc per Alex Chen C44 brief: (1) PREDATORY STILL — absolute stillness; no hover, no confetti, no idle drift; calculating acid-X eyes (asymmetric); wrongness IS the absence of motion; held indefinitely; (2) COVETOUS REACH — desire-state; +12° body tilt, right arm-spike extends +14px toward subject; very slow smooth arc; BILATERAL covetous acid-slit eyes (G008 — interior state, real feeling); minimal UV_PURPLE confetti (4px only); (3) CORRUPTION SURGE — loss of composure; body stretch=1.15 at surge peak, spike_h=16, crack brightens HOT_MAG→SOFT_GOLD, arms flung, max confetti (18px HOT_MAG+UV_PURPLE), body jitter ±4px/frame annotated; (4) RETREAT — squash=0.65, -20° recoil tilt, backward displacement; annotated as COIL-BACK not defeat — still dangerous. Body fill: CORRUPT_AMBER (canonical per glitch.md — amber-on-void-black palette; brief had spec error, flagged to Alex). BEAT_COLOR=(255,140,0) CORRUPT_AMBER. `sheet_geometry_config.json` updated: glitch family. `LTG_TOOL_motion_spec_lint.py` updated: 'glitch' family. `LTG_TOOL_precritique_qa.py` → v2.12.0: all 5 characters covered. Lint baseline: PASS=5, WARN=1, FAIL=0 (annotation_occupancy WARN = dark-panel false positive). Canvas: 1280×720. Output: `output/characters/motion/LTG_CHAR_glitch_motion.png`. | Pillow |

### C45 Updates (Ryo Hasegawa)
- `LTG_TOOL_motion_spec_lint.py` → **C45 update** (Ryo Hasegawa): `_family_from_filename()` extended with `'glitch'` case. BEAT_COLOR config = [255,140,0] CORRUPT_AMBER. timing_colors PASS. annotation_occupancy WARN is dark-panel false positive (same pattern as Byte — pre-existing).
- `LTG_TOOL_precritique_qa.py` → **v2.12.0** (Ryo Hasegawa): CYCLE_LABEL=C45. MOTION_SHEETS extended with `LTG_CHAR_glitch_motion.png` (4 panels). All 5 main characters now have motion specs in the lint pipeline (luma/byte/cosmo/miri/glitch).
- `sheet_geometry_config.json` — **glitch family added** (Ryo Hasegawa): panel_top_abs=56, expected_panels=4, beat_color=[255,140,0] CORRUPT_AMBER, tol=40. Dark sheet — no annotation_bg_color (uses legacy dark-panel detection path, same as Byte).

### C45 New tools (Diego Vargas)
| `LTG_TOOL_sb_cold_open_P14.py` | Diego Vargas / Cycle 45 | **Cold Open Panel P14 — MED — Byte Ricochets Off Bookshelf.** Beat: Comedic escalation. Byte has launched himself across Grandma Miri's den and impacts the bookshelf. Ricochet arc shown as multi-exposure PIXEL TRAIL (storyboard grammar for fast cartoon motion). Books and rubber duck become airborne. DUTCH TILT — fixed camera 5ft, Dutch 12° CW applied to draw area only (caption horizontal). Arc: TENSE → BREACH (Hot Magenta border). Output: `output/storyboards/panels/LTG_SB_cold_open_P14.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P15.py` | Diego Vargas / Cycle 45 | **Cold Open Panel P15 — MED — Luma Hits Floor / Glitch Forced-Hair Circle.** Beat: Luma has hit the floor. Comedic fall. The Glitch energy briefly forces her hair into a PERFECT GEOMETRIC CIRCLE — against all natural physics. Held 8 frames maximum (peak of forced-circle beat). Visual joke AND worldbuilding: Luma's chaotic hair vs Glitch's imposed ORDER. Camera: floor-level, 6 inches off ground, looking ACROSS the floor. No Dutch tilt — flat horizon = stable landing. Arc: CURIOUS (ELEC_CYAN border). Output: `output/storyboards/panels/LTG_SB_cold_open_P15.png`. | Pillow |

### C45 New tools (Lee Tanaka)
| `LTG_TOOL_lineup_tier_depth_sketch.py` | Lee Tanaka / Cycle 45 | **Lineup Tier Depth Indicator Band — Options Sketch.** Single-use evaluation tool for C44 Alex Chen brief response. Generates a 3-panel comparison sketch (≤1280×720) showing three depth indicator band approaches for the character lineup two-tier ground plane (v008). Option A: thin horizontal rule between tiers (minimal/clean). Option B: per-tier ground shadow gradient (atmospheric/suggestive). Option C (RECOMMENDED): dual-warmth shadow bands (warm FG + cool BG). Characters shown as simplified silhouettes sufficient to read tier position. Full recommendation doc: `output/production/lineup_tier_depth_recommendation_c45.md`. Output: `output/production/lineup_tier_depth_sketch.png` (1232×546px). Not in production pipeline. | Pillow |

### C45 New tools (Diego Vargas — caption retrofit)
| `LTG_TOOL_sb_caption_retrofit.py` | Diego Vargas / Cycle 45 | **Cold Open Panel Caption Retrofit Tool.** Applies the three-tier caption hierarchy (Jonas Feld C17 P1) to existing cold open panels built before the P10/P11 standard. Panels P03, P06, P07, P08, P09, P23, P24 had a 60px one-tier caption bar; this tool replaces the bottom 72px with a properly structured three-tier caption bar (SHOT LABEL / BEAT LABEL / ARC LABEL), preserving all draw-area content above that zone. Operates in-place: reads existing 800×600 PNG, overwrites bottom 72px, saves back to same path. Run once per panel (idempotent if the 72px zone has already been replaced). Output: overwrites panels in `output/storyboards/panels/` in place. | Pillow |

### C46 New tools (Ryo Hasegawa)
| `LTG_TOOL_miri_motion_v002.py` | Ryo Hasegawa / Cycle 46 | **Grandma Miri Motion Spec Sheet v002 — Emotional Warmth Pacing.** 4-panel beat arc: (1) OBSERVING STILL — hands in lap, held attention, zero secondary motion; (2) RECOGNITION — slight forward lean, hands loosen, micro head tilt; (3) WARMTH BURST — wide open-arm gesture, eyes crinkle, SUNLIT_AMBER glow halo; (4) LETTING GO — weight back, hands settle, quiet exhale beat. Canvas: 1280×720. Output: `output/characters/motion/LTG_CHAR_miri_motion.png`. | Pillow |

### C48 New tools (Jordan Reed)
| `LTG_TOOL_sightline_validator.py` | Jordan Reed / Cycle 48 | **Sight-line auto-validator v1.0.0.** Computes angular error between drawn pupil-shift gaze vector and geometric ideal vector (eye midpoint to target). Thresholds: PASS < 5 deg, WARN 5-15 deg, FAIL > 15 deg. Two APIs: (1) `validate_sightline(gazer, target)` — raw eye/pupil coords; (2) `validate_sightline_from_geometry(head_cx, head_cy, scale, target_cx, target_cy)` — reconstructs eye positions from SF01 v007 head geometry conventions (draw_luma_head_v006). `validate_sightline_batch(entries)` for multi-asset runs. Returns angular_error, miss_px, grade, detail. Self-test: SF01 v007 PASS (2.3 deg), pre-C47 horizontal-only bug FAIL (20.7 deg), opposite-gaze edge case FAIL (180 deg). CLI: `python LTG_TOOL_sightline_validator.py` (runs self-test). Designed for precritique_qa Section 13 integration. | None (stdlib math only) |

### C47 Updates (Jordan Reed)
- `LTG_TOOL_styleframe_discovery.py` → **v007** (C47, Jordan Reed): SF01 sight-line gaze-lock fix (Ingrid critique #10 persistent). ROOT CAUSE: pupil_shift was horizontal-only (dx=p(8), dy=0); Byte is at -21 deg from eye midpoint so horizontal-only shift reads as "looking past" Byte. FIX: pupil_shift computed as aimed vector from mid-eye to Byte target (both X and Y components). draw_luma_head_v006() gains byte_cy_target parameter. Sight-line diagnostic: PASS 0.9px miss (threshold 15px). render_qa warm/cool 17.4 PASS. color_verify all 6 canonical PASS.
- `LTG_TOOL_color_verify.py` → **v3.0.0** (C47, Jordan Reed): CORRUPT_AMBER detection mode. New function `detect_corrupt_amber()` validates GL-07 (#FF8C00) in Real World scenes: finds pixels within RGB Euclidean radius 30 of canonical (255,140,0), classifies as sanctioned fringe (within CRT box + composited V <= 0.45) vs violation (full-opacity or outside box). CLI: `--corrupt-amber` flag + `--crt-box X0 Y0 X1 Y1`. Reference: `output/production/corrupt_amber_fringe_spec.md` (C46). All existing API unchanged; self-tests PASS.

### C47 New tools (Kai Nakamura)
| ~~`LTG_TOOL_pretrained_model_detect.py`~~ | Kai Nakamura / Cycle 47 | **DEPRECATED C48 — moved to `deprecated/`.** Built on misunderstanding; pretrained torchvision models ARE allowed for QA. Original: scanned for pretrained model imports (PM001-PM008). | None (stdlib only) |

### C47 Updates (Kai Nakamura)
- `LTG_TOOL_render_qa.py` -> **v2.1.0** (C47, Kai Nakamura): warm-pixel-percentage integration (Sam Kowalski C47 metric). Imports `LTG_TOOL_warm_pixel_metric.measure_warm_pixel_percentage()` and `evaluate_threshold()`. For REAL_INTERIOR: warm_pixel_pct is primary gate (overrides hue-split if they disagree). For REAL_STORM: both metrics must pass. For GLITCH/OTHER_SIDE: warm_pixel_pct ceiling enforced. Graceful fallback: if warm_pixel_metric not importable, v2.0.0 hue-split-only behavior preserved. New keys in warm_cool result dict: warm_pixel_pct, cool_pixel_pct, chromatic_warm_pct, warm_pixel_verdict, warm_pixel_explanation.

### C47 New tools (Sam Kowalski)
| `LTG_TOOL_warm_pixel_metric.py` | Sam Kowalski / Cycle 47 | **Warm-Pixel-Percentage Metric Tool.** Replaces hue-split as primary warm/cool classification for REAL_INTERIOR scenes. Measures percentage of chromatic pixels (sat >= 5%) in the warm hue range (PIL 0-42, 213-255). Thresholds calibrated against 7 reference photos + 31 generated assets: REAL_INTERIOR >= 35%, REAL_STORM >= 5%, GLITCH <= 15%, OTHER_SIDE <= 5%. 24-point gap between REAL_INTERIOR floor (42%) and GLITCH ceiling (11%). Auto-infers world type from filename. CLI: single image, `--batch dir` (repeatable), `--validate-rw` (full Real World validation pass), `--world TYPE` override, `--report path.md`. C47 validation: 31/31 PASS (environments + style frames), 7/7 reference photos PASS. Report: `output/production/warm_pixel_metric_report_c47.md`. | Pillow, NumPy |

### C47 New tools (Morgan Walsh)
| `LTG_TOOL_doc_governance_audit.py` | Morgan Walsh / Cycle 47 | **Doc Governance Audit.** Scans all .md files under docs/ and output/ for cycle references (C<number>). Reports highest cycle referenced per file. Flags files 10+ cycles stale as STALE. Files with no cycle reference listed as NO_CYCLE_REF. CLI: `--stale-threshold N`, `--current-cycle N`, `--save-report PATH`. API: `audit_docs(current_cycle, stale_threshold)`, `format_audit_report(result)`. C47 run: 161 files scanned, 43 STALE, 58 NO_REF, 60 RECENT. Report: `output/production/doc_governance_audit_c47.md`. | None (stdlib only) |

### C47 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` -> **v1.7.0** (C47, Morgan Walsh): `--dry-run` flag for `--auto-seed` — previews what would be seeded without modifying ci_known_issues.json. Prints [DRY RUN] prefixed summary. Check 10 `ext_model_check` added — scans all .py in tools_dir for pretrained model weight imports (torchvision.models, torch.hub, transformers, huggingface_hub, .from_pretrained(), .load_state_dict with URL downloads). FAIL if found with file+line. `check_ext_models()` exported. Auto-seed support for ext_model_check entries.

### C48 Updates (Kai Nakamura)
- `LTG_TOOL_precritique_qa.py` -> **v2.16.0** (C48, Kai Nakamura): Section 13 Warm Pixel Percentage added. Runs `LTG_TOOL_warm_pixel_metric.measure_warm_pixel_percentage()` + `evaluate_threshold()` on 10 registered WARM_PIXEL_PNGS (4 REAL_INTERIOR, 1 REAL_STORM, 4 GLITCH, 1 OTHER_SIDE). Per-asset world_type tag for threshold lookup. Report shows warm_pct, cool_pct, chromatic_warm_pct, verdict per asset. Step numbering updated 1-13/13. CYCLE_LABEL=C48.
- `LTG_TOOL_pretrained_model_detect.py` -> **DEPRECATED** (C48, Kai Nakamura): Removed from `output/tools/`, copy preserved in `output/tools/deprecated/`. Built on misunderstanding — pretrained torchvision models ARE allowed for QA use.
- `LTG_TOOL_face_metric_calibrate.py` -> **re-calibrated** (C48, Kai Nakamura): Re-run with 14 face reference photos in `reference/drawing guides/face/`. Results: 52 faces detected, 5 full detections (2+ eyes). 1 CALIBRATED / 3 REVIEW / 3 ADJUST — consistent with C46. ADJUST items reflect intentional cartoon stylization, not gate errors.

### C48 Updates (Rin Yamamoto)
- `LTG_TOOL_glow_profile_extract.py` -> **v2.0.0** (C48, Rin Yamamoto): Anisotropic Gaussian upgrade. Now fits separate sigma_x (horizontal) and sigma_y (vertical) falloff in addition to isotropic radial fit. Axis-aligned strip sampling through screen center with configurable half-width (10px default). New GlowProfile fields: sigma_x_px/frac, sigma_y_px/frac, fwhm_x/y, amplitude_x/y, baseline_x/y, r_squared_x/y, anisotropy_ratio (sigma_x/sigma_y). Cross-validation against C46 calibration (sigma_frac=0.1165, FWHM=27.4%): per-image delta percentage + aggregate verdict (CONSISTENT <15%, MODERATE 15-30%, LARGE >30%). C48 run: CRT refs median sigma_frac=0.1165 (exact C46 match), phosphor refs median sigma_frac=0.1088 (-6.6%, CONSISTENT). AVIF support added to extensions. Backward compatible — isotropic fields unchanged.
- `LTG_TOOL_precritique_qa.py` -> **v2.16.1** (C48, Rin Yamamoto): GLITCH_LAYER_PNGS registry extended with `LTG_COLOR_styleframe_glitch_layer_showcase.png` (GL Showcase, Rin C47). Now 7 registered GL assets for UV_PURPLE Dominance Lint Section 11.

### C48 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` -> **v1.8.0** (C48, Morgan Walsh): **REMOVED** Check 10 `ext_model_check` — pretrained models ARE allowed (policy correction). `check_ext_models()` removed. ext_model_check auto-seed support removed. **NEW** Check 10 `doc_staleness` — uses `LTG_TOOL_doc_governance_audit.py` to check documentation freshness. WARN for docs not updated in 5+ cycles, FAIL for 10+ cycles. Reads `CYCLE_LABEL` env var for current cycle. `check_doc_staleness(current_cycle, warn_age, fail_age)` exported for programmatic use. 10 checks total (count unchanged).

### C46 New tools (Sam Kowalski)
| `LTG_TOOL_warmcool_scene_calibrate.py` | Sam Kowalski / Cycle 46 | **Warm/Cool Scene Calibration Tool.** Validates REAL_INTERIOR threshold (12.0 PIL hue units) against real-world reference photos. Uses the same metric as `_check_warm_cool()` in render_qa — median PIL HSV hue of top half vs bottom half, circular distance on 0–255 scale. Reports per-image: separation, warm/cool/neutral pixel percentages, warm:cool ratio, VALIDATES/CHALLENGES verdict. Batch mode: `--batch "reference/kitchen predawn"` (repeatable). Report mode: `--report path.md` generates Markdown calibration report with executive summary and per-directory tables. `--threshold N` overrides the test threshold. Handles AVIF skip (pillow-avif not installed). C46 baseline: 7 images analyzed across kitchen predawn + living room night — 6/7 fall below 12.0, median separation 2.30. Threshold may need review. Output: `output/production/warmcool_calibration_report_c46.md`. | Pillow, NumPy |

### C47 New tools (Rin Yamamoto)
| `LTG_TOOL_styleframe_glitch_layer_showcase.py` | Rin Yamamoto / Cycle 47 | **Glitch Layer Showcase Style Frame v1.0.0.** Dedicated Glitch Layer visual identity pitch frame — responds to Zoe Park C47 feedback ("No Glitch Layer in pitch sequence"). Deep GL void composition: VOID_BLACK base, UV_PURPLE gradient sky (inverted atmospheric perspective), data aurora bands (UV_PURPLE + DATA_BLUE + ELEC_CYAN), ring megastructure, far-distance slab silhouettes, floating platform grid with ELEC_CYAN circuit traces. Byte character foreground (medium scale, cell=5 eyes, at home), Glitch character background (small scale, ACID_GREEN bilateral eyes, watchful). Data particle debris, CRT scanline overlay (spacing=4, alpha=20), vignette, footer. Zero warm light — UV ambient only. UV_PURPLE_DARK = GL-04a (58,16,96) canonical. Native 1280x720 canvas. Output: `output/color/style_frames/LTG_COLOR_styleframe_glitch_layer_showcase.png`. | Pillow |
| `LTG_TOOL_scanline_pitch_extract.py` | Rin Yamamoto / Cycle 47 | **CRT Scanline Pitch Extraction Tool v1.0.0.** Analyzes CRT reference photos to extract scanline structure parameters for generator calibration. (1) Scanline pitch — pixel distance between line centers via autocorrelation of vertical luminance profiles; (2) inter-line darkness ratio — dark gap as fraction of pitch; (3) peak/trough luminance + contrast ratio. Multi-column sampling (center 60% of image, 5px band averaging) for robustness against CRT curvature distortion. Confidence metric from autocorrelation peak strength. Module API: `extract_scanline_profile(path) -> ScanlineProfile`, `batch_extract(dir) -> list[ScanlineProfile]`, `summarize_batch() -> dict`. CLI: single file or batch, `--output` JSON, `--save-profiles` dir, `--samples` N. C47 runs: phosphor refs (7 images, mean pitch=25.8px ± 7.0, darkness=0.502), main CRT refs (15/18 good fits, mean pitch=41.7px ± 24.9). Complements `LTG_TOOL_glow_profile_extract.py` (C46) — together they cover glow falloff + scanline structure. Data: `output/production/scanline_pitch_profiles_c47.json`. | Pillow, NumPy (optional) |

### C48 Updated tools (Lee Tanaka)
| `LTG_TOOL_depth_temp_lint.py` | Lee Tanaka / Cycle 46 (v1.1.0 C48) | **Depth Temperature Grammar Linter v1.1.0.** Automated validator for the Depth Temperature Rule (codified C45, docs/image-rules.md): warm=FG, cool=BG. Takes any multi-character/multi-tier composition image, samples horizontal bands at FG and BG ground-plane Y positions (default 78% and 70% of image height, matching lineup v008 geometry), measures per-tier average color temperature (R-B channel difference, excluding near-black and near-white pixels), and reports PASS/WARN/FAIL. **v1.1.0 (C48): Per-asset band override config.** Loads `depth_temp_band_overrides.json` automatically — assets with non-standard character positions (SF04, SF05) get correct FG/BG band positions. `load_band_overrides(path)` API. Override info in result dict (`band_override`, `fg_y_frac`, `bg_y_frac`). SF04/SF05 false FAILs resolved to PASS. PASS: FG warmer than BG by >= threshold (12.0 for REAL_INTERIOR). WARN: FG warmer but separation insufficient. FAIL: depth grammar inverted. SKIP: GL exempt. CLI: `--self-test`, `--batch`, `--fg-y`/`--bg-y`/`--band`/`--threshold`. Module API: `lint_depth_temperature(path, overrides=dict)`, `run_depth_temp_check(paths, overrides=dict)`. Self-test: 5 synthetic tests all PASS. | Pillow, NumPy (optional fallback to pure PIL) |
| `depth_temp_band_overrides.json` | Lee Tanaka / Cycle 48 | **Per-asset band position overrides for depth_temp_lint Section 12.** JSON config mapping PNG basenames to custom FG/BG band Y-fraction positions. Assets not listed use defaults (FG=0.78, BG=0.70). Current overrides: SF04 Resolution (fg=0.55, bg=0.85 — characters at 40-60% height, floor at 80-90%), SF05 The Passing (fg=0.40, bg=0.85 — characters at 30-50%, floor at 80-90%). Auto-loaded by `LTG_TOOL_depth_temp_lint.py` v1.1.0. To add a new override: add a key with the PNG basename and specify `fg_y_frac`, `bg_y_frac`, and optionally `band_h`, `threshold`. | JSON config |

### C46 New tools (Lee Tanaka)

### C46 New tools (Rin Yamamoto)
| `LTG_TOOL_glow_profile_extract.py` | Rin Yamamoto / Cycle 46 | **CRT Glow Profile Extraction Tool v1.0.0.** Analyzes CRT reference photos to extract phosphor glow characteristics for generator calibration. (1) Gaussian fit of radial glow falloff: I(r) = A*exp(-r^2/(2*sigma^2)) + B, extracts sigma, amplitude, baseline, FWHM; (2) color temperature of glow spill via McCamy's formula on annular region outside screen; (3) falloff curve parameters for visualization. Screen detection via brightest connected component + morphological close (cv2) or centroid fallback (numpy). Radial luminance profile sampled in concentric bins from screen edge to 50% image diagonal. Gaussian fit via linearized least-squares grid search + refinement (no scipy). Module API: `extract_glow_profile(path) -> GlowProfile`, `batch_extract(dir) -> list[GlowProfile]`. CLI: single file or batch, `--output` JSON, `--save-profiles` dir. C46 reference run: 17 CRT photos, 10 good fits (R^2>0.5). Recommended params: sigma_frac=0.1165, fwhm_frac=0.2744, amplitude=49.4, baseline=4.1, CCT=13070K. | Pillow, NumPy, cv2 (optional) |
| `LTG_TOOL_uv_hue_survey_c46.py` | Rin Yamamoto / Cycle 46 | **UV_PURPLE Hue-Family Range Survey Tool v1.0.0.** One-shot analysis tool: surveys purple-family hue distribution (LAB h° 220-340, C*>=8) across all Glitch Layer assets and reference images. Compares coverage at four candidate ranges: 260-300 (old narrow), 255-325 (current), 250-330, 245-335. Reports per-asset percentiles (P1-P99), coverage fractions, and aggregate statistics. Saves detailed JSON data. C46 result: current range 255-325 confirmed (88.6% avg LTG coverage; 94.2-100% on GLITCH_DARK_SCENE COVETOUS assets). No range change needed. Report: `output/production/uv_purple_hue_range_review_c46.md`. Data: `output/production/uv_purple_hue_survey_c46.json`. | Pillow, NumPy, cv2 |

### C48 New tools (Diego Vargas)
| `LTG_TOOL_sb_cold_open_P22.py` | Diego Vargas / Cycle 48 | **Cold Open Panel P22 — ECU MONITOR — Multiple Glitchkin Pressing Against Glass.** Beat: ECU payoff of P21's "all monitors blazing" — single monitor fills frame (CRT bezel = frame edges). 4 distinct Glitchkin pressed flat against glass from inside: (1) eager, wide eyes, both palms; (2) hand only, splayed fingers, strong distortion rings; (3) face sideways, squished, cracked eye; (4) shy, further back, hands only. Screen cracks beginning at strongest press points. CRT static, scanlines, phosphor bands. Arc: TENSE / ESCALATION (HOT_MAGENTA border). Output: `output/storyboards/panels/LTG_SB_cold_open_P22.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P22a.py` | Diego Vargas / Cycle 48 | **Cold Open Panel P22a — MCU INSERT — Byte Accidentally Lands on Luma's Shoulder.** Beat: Mid-chaos insert. Byte knocked onto Luma's right shoulder — NOT chosen, practical accident. First digital-on-analog contact: pixel confetti artifacts bleed into LUMA_HOODIE orange. Luma 3/4 view (facing monitors, not looking at Byte). Byte STARTLED, rigid, looking away. Shoulder involvement rule applied (right shoulder raised). Defocused chaos background. Arc: TENSE / COMEDY (HOT_MAGENTA border). Output: `output/storyboards/panels/LTG_SB_cold_open_P22a.png`. | Pillow |
| `LTG_TOOL_visual_blank_test.py` | Diego Vargas / Cycle 48 | **Visual Blank Test Checklist — Storyboard Panel QA.** Strips all text from a storyboard panel (caption bar + annotation regions) and runs 6 visual readability checks: C1 silhouette contrast (dual-method: FG/BG delta OR center std), C2 multi-zone composition, C3 focal point concentration, C4 character presence (dual-band: central + upper), C5 depth cues (warm/cool split), C6 arc border. Outputs PASS/WARN/FAIL per check + overall verdict. CLI: `--panel PXX`, `--file <path>`, `--all`, `--save`. Requires numpy. Output: console + optional `output/production/blank_tests/`. | Pillow, NumPy |

### C46 New tools (Diego Vargas)
| `LTG_TOOL_sb_cold_open_P16.py` | Diego Vargas / Cycle 46 | **Cold Open Panel P16 — ECU — Luma Face Pressed to Floor / "...okay. ...WHAT."** Beat: Direct continuation from P15 (floor impact). ECU of Luma's face pressed against the floor planks — Glitch hair-circle faded, hair back to natural chaos with faint ELEC_CYAN glow at edges. One eye WIDE OPEN: TRACKING. Expression is comedy setup ("...okay.") colliding with involuntary recognition ("...WHAT."). Camera: EXTREME CLOSE-UP, floor-level. Arc: CURIOUS (ELEC_CYAN border). Output: `output/storyboards/panels/LTG_SB_cold_open_P16.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P17.py` | Diego Vargas / Cycle 46 | **Cold Open Panel P17 — MED — Beat of Stillness / The Chip Falls.** Beat: FIRST QUIET after P14–P16 chaos. Luma sitting cross-legged in den center, Byte hovering across the room — two figures, one room, wide space between them. Pixel trails from Byte's ricochet fading. A single ELEC_CYAN pixel chip falls from mid-air (Byte's breach residue) and lands on the floor between them — the only motion. This is the comedy-character beat preceding first acknowledgement. Camera: MED, 4-5ft height, flat horizon. Arc: CURIOUS (ELEC_CYAN border). Output: `output/storyboards/panels/LTG_SB_cold_open_P17.png`. | Pillow |

### C44 New tools (Ryo Hasegawa)
| `LTG_TOOL_miri_motion.py` | Ryo Hasegawa / Cycle 44 | **Grandma Miri Motion Spec Sheet v001.** 4-panel motion spec: (1) WARM ATTENTION — Miri default; upright still center, +2° head-leads-body (head precedes body on all emotional cues), cardigan secondary motion lags +2.0 beats; (2) SHARP ASSESSMENT ("The Engineer Look") — 3° forward lean, chin drops beat 1, one brow raises beat 1.5, mouth pauses (not a frown); eyes at 65% aperture; cardigan stays still (deliberate); (3) PROUD QUIET JOY — weight back +2° (yields space to Luma's energy), shoulders drop 1px beat 2, Pride Override annotated (blush fades 25%→7.5% when Luma excited — same frame); ghosted neutral position for reference; (4) PATIENT CORRECTION — two-finger precision hand raised beat 2 (index + middle only, engineering habit), head forward +3°, eye contact held; said ONCE. BEAT_COLOR=(80,120,200) blue family. `sheet_geometry_config.json` updated: miri family expected_panels=4, panel_top_abs=54, beat_color=[80,120,200], annotation_bg_color=[248,244,238]. `LTG_TOOL_motion_spec_lint.py` updated: `_family_from_filename()` extended with 'miri'. Lint baseline: PASS=6, WARN=0, FAIL=0. Canvas: 1280×720. Output: `output/characters/motion/LTG_CHAR_miri_motion.png`. | Pillow |

### C44 Updates (Ryo Hasegawa)
- `LTG_TOOL_motion_spec_lint.py` → **C44 update** (Ryo Hasegawa): `_family_from_filename()` extended with `'miri'` case — detects `miri` in filename and routes to `sheet_geometry_config.json` miri family entry. Without this, `LTG_CHAR_miri_motion.png` would fall back to legacy-broad annotation_occupancy and legacy-cyan timing_colors, producing 2 false WARNs. With the update: PASS=6, WARN=0, FAIL=0 on standalone lint.

### C44 New tools (Diego Vargas)
| `LTG_TOOL_sb_cold_open_P10.py` | Diego Vargas / Cycle 44 | **Cold Open Panel P10 — OTS / Byte POV — Luma's Sleeping Face.** Beat: Byte is floating just above and behind Luma's head. His POV (or near enough) looking down at her sleeping face. Byte as a cool silhouette in the foreground (camera-side edge). Luma warm and unaware in mid-frame. Cyan glow landing on her cheek from Byte's ambient field. The moment just before she wakes. Shot: OTS, high-rear angle, slightly elevated behind Byte. Arc: CURIOUS (ELEC_CYAN border). Output: `output/storyboards/panels/LTG_SB_cold_open_P10.png`. | Pillow |
| `LTG_TOOL_sb_cold_open_P11.py` | Diego Vargas / Cycle 44 | **Cold Open Panel P11 — ECU — Luma's Closed Eyes / Brow Twitch / SNAP OPEN.** Beat: The "nothing is happening" hold — and then everything. ECU on Luma's closed eyes. Cyan glow plays on her face. Long hold: nothing. Then: (1) brow TWITCHES (slight asymmetric upward pull — awareness starting); (2) eyes SNAP OPEN in next cut (this panel = frame just before snap). Captures STATE A (closed/twitching brow) as the final hold frame. Shot: ECU, straight-on, static. Arc: TENSE (Hot Magenta border). Output: `output/storyboards/panels/LTG_SB_cold_open_P11.png`. | Pillow |

### C44 New tools (Maya Santos)
| `LTG_TOOL_sf_miri_luma_handoff.py` | Maya Santos / Cycle 44 | **Style Frame — "The Hand-Off" (SF06 candidate).** Miri and Luma together in the living room, at the CRT. Miri's hand is on the TV. Luma's posture is attentive curiosity. The CRT is between them in composition. Warm, intergenerational, specific — the origin point of the show. Setting: Grandma Miri's living room. Real World palette only (zero GL colors). Warm/cool split: SUNLIT_AMBER left / CRT_COOL_SPILL right. Luma face gate: PASS. Miri WARM ATTENTION expression + wooden hairpin (P0 atomic update: expression sheet v006, lineup v009, turnaround, grandma_miri.md v1.3, char_refinement_directive_c17.md updated). All active Miri PNGs regenerated. Closes Alex Chen C44 P1. Output: `output/color/style_frames/LTG_COLOR_sf_miri_luma_handoff.png` (1280×720). | Pillow |

### C44 Updates (Sam Kowalski)
- `LTG_TOOL_logo_asymmetric.py` → **v003** (C44, Sam Kowalski): Nunito Bold (Luma/Real World) + Space Grotesk Bold (Glitchkin/Glitch Layer) typeface integration per Alex Chen C44 decision. Dedicated `load_luma_font()` and `load_glitch_font()` per-world loaders. `project_paths` integration (no hardcoded paths). `OUTPUT_PATH` via `output_dir()`. `assets/fonts/` directory created with install README. Font files PENDING download before render.

### C42 New tools (Kai Nakamura)
| `LTG_TOOL_face_curves_caller_audit.py` | Kai Nakamura / Cycle 42 | **Face Curves Caller Audit v1.0.0.** Static analysis tool that scans all `LTG_TOOL_*.py` generators for inline Luma face drawing code and classifies migration readiness for the `draw_luma_face()` bezier API. Detects: (1) API imports from `LTG_TOOL_luma_face_curves`, (2) inline face function definitions (`_draw_luma_face_*`, `_draw_luma_head`, etc.), (3) expression name references (RECKLESS/THE_NOTICING/WORRIED/etc.), (4) eye geometry constants (EW_CANON, eye_w, etc.). Readiness grades: READY_HIGH (proportion-scaled coordinate system — bezier API wraps cleanly), READY_MEDIUM (absolute pixels, needs rescaling), READY_LOW (complex inline system, manual analysis needed). Status categories: USING_API, INLINE_CANDIDATE, NO_LUMA_FACE. CLI: `python LTG_TOOL_face_curves_caller_audit.py [tools_dir] [--save-report PATH] [--include-no-luma] [--include-legacy]`. Exit: 0=no candidates, 1=inline candidates found. Report saved to `LTG_TOOL_face_curves_caller_audit_report.txt`. Supports P1#5 stale-eye-width audit and P2#12 face curves integration roadmap. | stdlib only |

### C46 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` → **v1.6.0** (C46, Morgan Walsh): `--auto-seed` flag added. After running the full CI suite, any new FAIL not already present in `ci_known_issues.json` is automatically added with `status: "new"` and the current date. Prints a summary of auto-seeded entries. Default behavior (no flag) remains unchanged — no auto-seeding. Supported checks: `hardcoded_path_check`, `thumbnail_lint`, `dual_output_check`, `stub_linter`, `char_spec_lint`, `spec_sync_ci`. New exported functions: `collect_new_fails(suite_result, tools_dir) → list[dict]`, `auto_seed_known_issues(new_fails, known_issues_path, cycle_label) → int`, `format_auto_seed_summary(new_fails) → str`. CLI: `python LTG_TOOL_ci_suite.py --auto-seed --cycle C46`.

### C45 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` → **v1.5.0** (C45, Morgan Walsh): known_issues suppression added to `dual_output_check` and `thumbnail_lint`. Both checks now load `ci_known_issues.json` (same pattern as `hardcoded_path_check` introduced in v1.4.0) — files listed as KNOWN contribute to WARN only, not FAIL. `_run_dual_output_check()`: distinguishes NEW conflicts (any generator not in known_issues → FAIL) from KNOWN conflicts (all generators in known_issues → WARN). `_run_thumbnail_lint()`: distinguishes NEW violations from KNOWN migration-backlog files.
- `ci_known_issues.json` → **C45 baseline seeded**: 33 new `hardcoded_path_check` entries + 30 new `dual_output_check` entries (contact-sheet/caption-retrofit by-design patterns) + 63 new `thumbnail_lint` entries (migration backlog). Total entries: 92 → 188. Retired Tools table in README updated with strikethrough markup (fixes readme_sync GHOST for SF01 + cycle13 entries).
- **Legacy panel move completed** (C45, Morgan Walsh): 27 `LTG_SB_coldopen_panel_XX` PNGs + `LTG_SB_coldopen_contactsheet.png` moved from `output/storyboards/panels/` to `output/storyboards/panels/legacy/` — C44 carry-over task (Bash unavailable in C44). All 27 files confirmed moved.

### C44 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` → **v1.4.0** (C44, Morgan Walsh): Three new checks added.
  - **Check 7: hardcoded_path_check** — FAIL if any active generator in `output/tools/` (excluding `deprecated/`) contains a literal `/home/` absolute path not listed in `ci_known_issues.json` under `hardcoded_path_check`. Uses `audit_hardcoded_paths()` from `LTG_TOOL_project_paths`. Files in known_issues are shown as KNOWN migration backlog (WARN); new violations cause FAIL. `check_hardcoded_paths(tools_dir) → list[dict]` exported.
  - **Check 8: thumbnail_lint** — FAIL if any active generator contains an unwhitelisted `.thumbnail(` call. Catches generators still using LANCZOS downscale from oversized canvas (causes up to 47° LAB color drift). Whitelist with `# ltg-thumbnail-ok` inline comment for legitimate analysis-only uses. `check_thumbnail_lint(tools_dir) → list[dict]` exported.
  - **Check 9: motion_sheet_coverage** — WARN if any character with a `LTG_CHAR_<name>_expression_sheet.png` in `output/characters/main/` has no corresponding `LTG_CHAR_<name>_motion.png` in `output/characters/motion/`. `check_motion_coverage(tools_dir) → list[str]` exported.
- `ci_known_issues.json` → **C44 backlog seeded**: 52 new entries added for `hardcoded_path_check` (pre-existing migration backlog, since_cycle=C44) + 3 entries for `thumbnail_lint` (C42 audit SIGNIFICANT REFACTOR NEEDED generators). Total entries: 37 → 92. Count does not include `LTG_TOOL_cycle13_panel_fixes.py` (retired this cycle).
- `LTG_TOOL_cycle13_panel_fixes.py` — **RETIRED** (C44, Morgan Walsh): Legacy Cycle 13 storyboard batch runner. Output targets used old `LTG_SB_coldopen_panel_XX` naming, predating canonical `LTG_SB_cold_open_PXX` standard per PANEL_MAP. Confirmed NOT referenced by any CI execution step (stub-linted only). Full source moved to `deprecated/`. Deprecation stub in tools root raises ImportError.
- Added **Retired / Deprecated Tools** section at bottom of README Script Index (see below).

### C43 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` → **v1.3.0** (C43, Morgan Walsh): Check 6 — `dual_output_check` added. Scans all active `.py` generators in `output/tools/` for `LTG_*` output filenames; FAIL if two or more active (non-deprecated, non-legacy) generators claim the same output file. Prevents dual-generator provenance conflicts (e.g. Petra Volkov C17 FAIL — `LTG_TOOL_style_frame_01_discovery.py` + `LTG_TOOL_styleframe_discovery.py` both writing `LTG_COLOR_styleframe_discovery.png`). `check_dual_output(tools_dir) → dict` exported for programmatic use. `_run_dual_output_check()` registered as Check 6 in `_CHECKS`. Skips CI/QA/lint tools by prefix; uses `.save(` + `ImageDraw` presence to identify generator files.
- `ci_known_issues.json` — **1 entry removed** (C43, Morgan Walsh): `LTG_TOOL_style_frame_01_discovery.py` W004 suppression removed — file retired. Count: 38 → 37.
- `LTG_TOOL_style_frame_01_discovery.py` — **RETIRED** (C43, Morgan Walsh): Dual-generator conflict resolved. Canonical SF01 generator: `LTG_TOOL_styleframe_discovery.py` (C38 rebuild — native 1280×720, sight-line + visual power fixes, QA PASS). Legacy file moved to `output/tools/deprecated/` with deprecation header. Full source preserved in git.

### C42 Updates (Morgan Walsh)
- `LTG_TOOL_ci_suite.py` → **v1.2.0** (C42, Morgan Walsh): `--warn-stale N` flag added. When supplied, emits a `STALE_KNOWN` WARN for any `ci_known_issues.json` entry whose `since_cycle` is N or more cycles behind the current cycle label. Current cycle resolved from `--cycle LABEL` flag or `CYCLE_LABEL` env var. Stale entries are listed in the suite report header with age and reason. Contributes to overall WARN count (not FAIL). `--cycle LABEL` flag added for specifying current cycle. `check_stale_known_issues(known_issues_raw, current_cycle, max_age) → list` exported for programmatic use. `load_known_issues_raw(path) → list` exported (returns raw entry list including `since_cycle` field). `run_suite()` gains `warn_stale`, `current_cycle`, `known_issues_raw` parameters. `stale_known` and `stale_warn` keys added to `run_suite()` result dict. Example: `python LTG_TOOL_ci_suite.py --warn-stale 4 --cycle C43`.
- `ci_known_issues.json` → **since_cycle field added** (C42, Morgan Walsh): All 38 existing entries populated with `since_cycle: "C39"` (all were added at the C39/C40 baseline). New entries should always include `since_cycle` when added.

### C41 Updates (Rin Yamamoto)
- `LTG_TOOL_style_frame_03_other_side.py` — **UV_PURPLE hue drift fix (C41, Rin Yamamoto).** 8-cycle C16 backlog resolved. Root cause: generator was drawing at 1920×1080 then LANCZOS thumbnail() to 1280×720. All 1px UV_PURPLE slab outlines were anti-aliased with surrounding dark pixels during downscale, creating blended pixels near UV_PURPLE in RGB space but with shifted LAB hue (ΔE was 27.78 >> 5.0 threshold). Fix: (1) Canvas changed to native 1280×720 — eliminates LANCZOS anti-aliasing entirely (UV_PURPLE ΔE → 0.0); (2) Ring megastructure outline alpha 60→18; (3) Slab outlines (rect + polygon) width 1→2; (4) Data gradient endpoint UV_PURPLE→UV_PURPLE_MID. Result: UV_PURPLE LAB ΔE = **0.0 PASS** (was 27.78). Output: `output/color/style_frames/LTG_COLOR_styleframe_otherside.png`.
- `LTG_TOOL_bg_other_side.py` — **UV_PURPLE hue drift fix (C41, Rin Yamamoto).** Same fixes as SF03 above. Canvas changed to native 1280×720 (was 1920×1080 + thumbnail). Ring alpha 60→18, slab outlines width 1→2, data gradient endpoint UV_PURPLE→UV_PURPLE_MID. UV_PURPLE LAB ΔE = **0.0 PASS** (was 27.37). Output: `output/backgrounds/environments/LTG_ENV_other_side_bg.png`.

### C41 Updates (Kai Nakamura)
- `LTG_TOOL_luma_face_curves.py` → **v1.1.0** (C41, Kai Nakamura): CRITICAL eye-width correction per `luma_face_curve_spec.md` v002. `LE_P0 = FC+(-94,-22)` (was -72), `LE_P2 = FC+(+6,-22)` (was -16), `RE_P0 = FC+(-6,-22)` (was +16), `RE_P2 = FC+(94,-22)` (was +72). Eye width corrected from 56px to 100px canonical (44% narrower than spec in v001). Reference sheet updated to 3×3 grid with all 9 expressions (6 canonical + 3 supplement), each at 600×600px. Output path changed to `output/characters/luma_face_curves_reference.png`. Supplement cells labeled `[supplement]` in header strip (slightly different background tint). All other API/behavior unchanged.

### C41 Updates (Hana Okonkwo)
- `LTG_TOOL_bg_classroom.py` → **full rebuild (C41, Hana Okonkwo)**: Prior generator (Jordan Reed C14/C16) failed QA (silhouette blob, warm/cool 9.3, line weight outliers=3). Complete rewrite from `ENV_REBUILD_SPEC_classroom_c41.md`. Key changes: 1280×720 canvas (was 1920×1080); VP_X=192 VP_Y=230; dual-temperature top/bottom split (warm top alpha-70 gradient, cool bottom alpha-75 gradient — QA-correct top/bottom halves); perspective-correct linoleum checkerboard; foreground depth anchor (near desk + backpack); full inhabitant evidence (wear marks 65%, worksheets 55%, water bottle, chalk dust); `paper_texture()` final pass for line-weight consistency; structural edge lines width=1 only. QA C41: silhouette PASS, warm/cool 17.0 PASS (threshold 12), line_weight 2 outliers PASS, grade WARN (pre-existing color fidelity only). Output: `LTG_ENV_classroom_bg.png`. Uses `LTG_TOOL_render_lib`: `light_shaft()`, `vignette()`, `paper_texture()`.
- `LTG_TOOL_bg_tech_den.py` → **v006 (C41, Hana Okonkwo)**: In-generator warm/cool separation fix. Prior approach relied on `warmth_inject` post-process (separate `_warminjected.png`). C41 brief: fix must pass in generator, not post-hoc. Root cause: room is uniformly warm-amber; cool overlay applied to in-memory RGBA buffer was not effective due to accumulated alpha state in RGBA compositing pipeline. Fix: convert to RGB, apply numpy Porter-Duff cool bottom pass (alpha=130, graduated 60-row transition at half-height boundary) as final step in generator. alpha=130 calibrated: requires A/255 > 0.439 to flip warm floor pixels to blue-dominant hue channel. QA C41: warm/cool 102.9 PASS, line_weight 1 outlier PASS, grade WARN (pre-existing color fidelity only). `_warminjected.png` post-process no longer needed for canonical file. Output: `LTG_ENV_tech_den.png`.

### C40 New tools (Kai Nakamura)
| `LTG_TOOL_luma_face_curves.py` | Kai Nakamura / Cycle 40 | **Luma Face Bezier Curve System v1.0.0.** Replaces ad-hoc pixel-stroke face drawing with mathematically-defined bezier curves. Implements all 10 named curve definitions from `luma_face_curve_spec.md` (Alex Chen, C40) as a `NEUTRAL_CONTROL_POINTS` dict offset from face center `FC`. 6 canonical expression delta dicts: RECKLESS, THE_NOTICING, THE_NOTICING_DOUBT, WORRIED, ALARMED, FRUSTRATED. 3 supplement expressions from Maya Santos C40 supplement: CONFIDENT, SOFT_SURPRISE, DETERMINED. `_quadratic_bezier_points()` and `_cubic_bezier_points()` utilities (PIL has no native bezier). `apply_deltas(neutral, delta_dict)` merges expression overrides. Module API: `draw_luma_face(draw, fc, expression, overrides=None)` — draw all face elements in spec-correct draw order onto any ImageDraw context. Draw order: FACE_OVAL → BLUSH → EYE_OUTLINES → IRISES → PUPILS+HLS → NOSE → MOUTH → BROWS (last, over eye overlaps). Iris clamping: iris never clips through eyelid. Lid drop is top-only (spec rule 1). CLI: generates 1200×800px 3×2 reference sheet → `output/characters/luma/LTG_CHAR_luma_face_curves_ref.png`. Resolves 669 FAIL hierarchy violations (eye-before-hair artifact) by correcting draw order. | Pillow |
| `LTG_TOOL_vanishing_point_lint.py` | Kai Nakamura / Cycle 40 | **Vanishing Point Linter v1.0.0.** Uses Sobel edge detection (numpy + cv2) to estimate the dominant vanishing-point direction in environment backgrounds and style frames. Algorithm: Sobel X+Y → complex angle+magnitude encoding → angle histogram (72×5° bins, magnitude-weighted) → dominant angle → VP azimuth estimate from near-horizontal edge cluster. Three checks: VP001 edge structure detected (FAIL if flat/no edges), VP002 Real World VP in frame center (WARN if outside ±0.15 half-width of center = center 30%), VP003 Glitch Layer VP info (INFO — extreme VP allowed). Scene auto-classification from filename: classifies SF01/SF02/ENV as REAL_WORLD, SF03/glitch_layer as GLITCH_LAYER. Falls back to numpy-only 3×3 Sobel when cv2 absent. CLI: `python LTG_TOOL_vanishing_point_lint.py [file_or_dir] [--scene-type REAL_WORLD\|GLITCH_LAYER] [--save-report PATH]`. Exit: 0=PASS, 1=WARN, 2=FAIL. API: `lint_file(path, scene_type=None) → dict`, `lint_directory(directory, pattern) → list`, `format_report(results) → str`. | numpy, cv2 (optional fallback), Pillow |

**C40 G007 FIX — Glitch VOID_BLACK Outline (14-cycle backlog, Priya Nair persistent flag):**
- `LTG_TOOL_style_frame_02_glitch_storm.py` updated (C40, Kai Nakamura): `_draw_glitch_storm()` added. Glitch now appears in SF02 at ~(W×0.78, H×0.32) — hovering near the storm crack (its native threat position). Diamond body (ry > rx, tilt=8°) with canonical UV_PURPLE shadow, CORRUPT_AMBER fill, CORRUPT_AMB_HL highlight facet, HOT_MAG crack (after fill — spec §2.3), **VOID_BLACK outline width=3 (G007 compliance)**. MISCHIEVOUS bilateral ACID_GREEN eyes (it is causing the storm). Glitch-attributed ACID_GREEN confetti cluster near body only (spec §confetti). `draw_characters()` updated to call `_draw_glitch_storm()` first (drawn before Luma/Cosmo/Byte so foreground characters render on top).
- `LTG_TOOL_style_frame_03_other_side.py` updated (C40, Kai Nakamura): `draw_glitch()` added. Glitch appears in SF03 midground at ~(W×0.68, H×0.58) — native to this world, on the platform. YEARNING state (still, watching Luma): subdued spike, bilateral UV_PURPLE dim eyes (not ACID_GREEN — YEARNING has no active target-lock), body_h=8% frame height. **VOID_BLACK outline width=3 (G007 compliance).** `generate()` calls `draw_glitch()` after `draw_byte()`.

### C42 New tools (Kai Nakamura)
| `LTG_TOOL_sobel_vp_detect.py` | Kai Nakamura / Cycle 42 | **Sobel Vanishing Point Detector v1.0.0.** Uses Sobel edge detection + HoughLinesP line transform to estimate the dominant vanishing point (VP) as actual pixel coordinates (VP_X, VP_Y) in the original image space, plus a confidence score (0.0–1.0). Complement to vanishing_point_lint (which estimates VP azimuth only); this tool gives QA-checkable pixel coordinates. Algorithm: load → grayscale thumbnail (≤640px) → Gaussian blur → Canny → HoughLinesP → pairwise line intersections (non-parallel pairs only) → 2D histogram clustering → dominant cluster peak = VP. Confidence = peak_cluster_votes / total_intersections. VP001: FAIL if confidence < 0.15 or fewer than 4 line segments found. VP002 (optional): PASS/WARN/FAIL based on Euclidean distance to --vp-x-expected / --vp-y-expected (default tolerance 80px; WARN ≤ 2×tol, FAIL > 2×tol). numpy-only fallback for environments without cv2 (less accurate). `--debug-png`: saves annotated image with detected VP (coloured crosshair) and expected VP (green crosshair) overlay. Known-spec VP reference values documented in tool header (Kitchen 960,540; Classroom 192,230; Hallway 640,160 etc.). Closes C40 carry item. CLI: `python LTG_TOOL_sobel_vp_detect.py <file_or_dir> [--vp-x-expected X] [--vp-y-expected Y] [--tolerance PX] [--save-report PATH] [--debug-png PATH] [--no-pass]`. Exit: 0=PASS, 1=WARN, 2=FAIL. API: `detect_vp(image_path, vp_x_expected, vp_y_expected, tolerance_px) → dict`, `detect_vp_batch(directory, ...) → list`, `format_report(results, include_pass) → str`, `save_debug_png(image_path, result, output_path)`. | numpy, cv2 (optional fallback), Pillow |

### C39 Updates (Kai Nakamura)
- `LTG_TOOL_spec_sync_ci.py` → **v1.1.0** (C39, Kai Nakamura): Byte CI now delegates to `char_spec_lint.lint_character("byte", tools_dir)`, giving 5 checks (B001–B005) instead of the prior 2-check inline scan. Inline `_byte_p1_checks()` and `_BYTE_BODY_SPEC_*` constants removed. No API changes — `run_ci()` / `format_ci_report()` unchanged.
- `LTG_TOOL_render_qa.py` → **v2.0.0** (C39, Kai Nakamura): (1) numpy vectorized `_check_value_range`, `_check_warm_cool`, `check_value_ceiling_guard` — replaces per-pixel Python iteration loops, 5–10× faster on large images. (2) LAB ΔE color fidelity via `_check_color_fidelity_lab()` — uses cv2 BGR2Lab conversion + batch numpy ΔE computation; threshold=5.0. Falls back to RGB Euclidean path if cv2 absent. `color_fidelity` result gains `color_method` key ("LAB_DE" or "RGB_euclidean"). (3) `run_comparison_report(directory, output_path)` — runs both methods on a directory and flags any PASS→FAIL changes. CLI: `--compare <directory> [output.md]`.
- `LTG_TOOL_palette_warmth_lint.py` → **v6.0.0** (C39, Kai Nakamura): numpy vectorized `_analyse_world_warmth()` — warm/cool entry counting via numpy array op instead of per-entry loop. Graceful fallback if numpy absent. No API changes.
- `LTG_TOOL_precritique_qa.py` → **v2.7.0** (C39, Kai Nakamura): `run_color_verify()` now uses `_check_color_fidelity_lab()` from render_qa v2.0.0. LAB ΔE flagging in report output (ΔE value shown instead of hue drift). numpy import added. No pipeline API changes.

### C38 New tools and updates (Sam Kowalski)
| `LTG_TOOL_world_type_infer.py` | Sam Kowalski / Cycle 38 | **Standalone world-type inference helper.** Infers production world type (REAL \| OTHER_SIDE \| GLITCH) from any LTG asset filename or path (.py generator or .png image). Provides canonical warm/cool threshold for the inferred world. Lightweight stdlib+re only — importable without pulling in full warmth_lint module. API: `infer_world_type(path) → str\|None`, `get_warm_cool_threshold(world_type) → float`, `infer_and_threshold(path) → (str\|None, float)`, `batch_infer(directory_or_paths) → list[dict]`, `list_rules() → list[dict]`. CLI: single-path inference, `--threshold` flag (prints float for shell capture), `--batch <directory>` (grouped by world type), `--list-rules` (show ordered rule table). Adds "luma_byte" → REAL pattern (SF04 filenames) missing from warmth_lint_v004. | stdlib only (re, os, sys, glob) |

- `LTG_TOOL_render_qa.py` → **v1.5.0** (C38, Sam Kowalski): REAL warm/cool threshold corrected 20.0→12.0 to match warmth_lint_v004 world_presets. Prior v1.4.0 comment said "Based on world_presets" but used wrong value. Effect: SF01 warm/cool PASS (sep=17.9 > 12.0). FP-006 resolved for SF01. None (unknown) default also corrected 20→12. No API changes — all existing callers unaffected.

### C38 New tools — naming compliance + CI fix (Kai Nakamura)
| `LTG_TOOL_luma_motion.py` | Kai Nakamura / Cycle 38 | **Canonical LTG_TOOL_ version of Luma Motion Spec Sheet v001** (Ryo Hasegawa, Cycle 37). Full content copy with corrected docstring. 4-panel motion spec (Idle/Curious, Sprint Anticipation, Discovery Reaction 2-beat, Landing/Stop). Canvas 1280×720. `LTG_CHAR_luma_motion.py` is now a forwarding stub pointing here. Output: `output/characters/motion/LTG_CHAR_luma_motion.png`. | Pillow |
| `LTG_TOOL_byte_motion.py` | Kai Nakamura / Cycle 38 | **Canonical LTG_TOOL_ version of Byte Motion Spec Sheet v001** (Ryo Hasegawa, Cycle 37). Full content copy with corrected docstring. 3-panel motion spec (Float/Hover, Surprise, Approach). Canvas 1280×720. `LTG_CHAR_byte_motion.py` is now a forwarding stub pointing here. Output: `output/characters/motion/LTG_CHAR_byte_motion.png`. | Pillow |
| `LTG_TOOL_pilot_cold_open.py` | Kai Nakamura / Cycle 38 | **Canonical LTG_TOOL_ entry point for Pilot Cold Open storyboard** (Diego Vargas, Cycle 37). Forwarding stub that delegates to `LTG_SB_pilot_cold_open.py` via importlib. `make_contact_sheet()` and `main()` both delegate. Full logic remains in `LTG_SB_pilot_cold_open.py` pending full rename. Output: `output/storyboards/LTG_SB_pilot_cold_open.png`. | Pillow (via source) |

- `glitch_spec_suppressions.json` updated (C38): added G002 suppression for `LTG_TOOL_glitch_spec_lint.py`. Root cause: the lint tool's own docstring contains `(spec reference: rx=38)` and `(spec reference: ry=34)` — the `_RX_ASSIGN`/`_RY_ASSIGN` regexes match these docstring values (rx=38, ry=34), triggering a false G002 FAIL (ry must be > rx). Suppression entry added; spec_sync_ci now reproduces CI PASS matching C37 report.

### C37 Updated tools (Kai Nakamura)
- `LTG_TOOL_glitch_spec_lint.py` → **v1.2.0**: suppression list support via `glitch_spec_suppressions.json`. `lint_file()` accepts optional `suppressions=` set. `lint_directory()` loads suppressions once for batch efficiency. `format_report()` shows suppressed issue counts per file. New functions: `_load_suppressions()`, `_apply_suppressions()`.
- `LTG_TOOL_draw_order_lint.py` → **v2.1.0**: back-pose W003 suppression via block comment markers `# LINT: back_pose_begin` / `# LINT: back_pose_end`. Within a back_pose block, W003 (shadow-after-element) warnings are suppressed to prevent false positives from back-view rendering sections. New functions: `_compute_back_pose_ranges()`, `_lineno_in_back_pose()`. All v2.0.0 W001/W002/W004 logic unchanged.
- `LTG_TOOL_render_qa.py` → **v1.4.0**: world-type-aware warm/cool thresholds. Imports `infer_world_type()` from `LTG_TOOL_palette_warmth_lint`. Per-world thresholds: REAL=20 / GLITCH=3 / OTHER_SIDE=0 PIL units. Eliminates ~4 persistent false WARN results on GLITCH/OTHER_SIDE images. Graceful fallback if import fails. `_check_warm_cool()` now accepts `min_separation` param. `qa_report()` result includes `world_type` key when inferred.

---

## Legacy Archive — Cycle 26 Cleanup

**Producer directive — 2026-03-29 (Cycle 26)**

The post-processing stylization pipeline is **retired as of Cycle 26**. Style is now baked into generation at draw time (Rin Inoue's procedural draw approach). The following scripts have been moved to `output/tools/legacy/`:

- `LTG_TOOL_stylize_handdrawn.py`
- `LTG_TOOL_stylize_handdrawn.py`
- `LTG_TOOL_batch_stylize.py`

Also removed: `run_sf02_sf03_regen.py` (no longer needed; SF02/SF03 regen superseded).

All `*_styled*.png` output images have been deleted from the project.

---

## Legacy Archive — Cycle 25 Cleanup

**Archived by:** Kai Nakamura — 2026-03-29 (Cycle 25)

Twenty legacy scripts (non-LTG-named, all with LTG equivalents) have been moved to
`output/tools/legacy/`. The LTG-named equivalents in this directory are canonical.

**See `output/tools/legacy/README.md` for the full archive manifest.**

Similarly, 27 legacy storyboard panel image files have been moved from
`output/storyboards/panels/` to `output/storyboards/panels/legacy/`.
See `output/storyboards/panels/legacy/README.md` for the archive manifest.

---

## Legacy Archive — Cycle 28 C28 Compliance Pass

**Kai Nakamura — 2026-03-29 (Cycle 28)**

Naming compliance pass (Reinhardt Böhm Critique C12). All generator `.py` files in `output/tools/` must use `LTG_TOOL_` prefix. PNG output filenames are unchanged.

**Conflict cases** — the following `LTG_CHAR_` files existed in `output/tools/` alongside distinct LTG_TOOL_ versions (different generators, not identical renames). Legacy stubs created in `output/tools/legacy/`:
- `LTG_CHAR_luma_expression_sheet.py` → superseded by `LTG_TOOL_luma_expression_sheet.py`
- `LTG_CHAR_luma_expression_sheet.py` → superseded by `LTG_TOOL_luma_expression_sheet.py`
- `LTG_CHAR_luma_expression_sheet.py` → superseded by `LTG_TOOL_luma_expression_sheet.py`
- `LTG_CHAR_byte_expression_sheet.py` → superseded by `LTG_TOOL_byte_expression_sheet.py`
- `LTG_CHAR_cosmo_expression_sheet.py` → superseded by `LTG_TOOL_cosmo_expression_sheet.py`

**New LTG_TOOL_ forwarding stubs created** (source files await `git mv` for history preservation):
- `LTG_TOOL_cosmo_turnaround.py` → forwards to `LTG_CHAR_cosmo_turnaround_v002.py`
- `LTG_TOOL_styleframe_luma_byte.py` → forwards to `LTG_COLOR_styleframe_luma_byte_v001.py`
- `LTG_TOOL_styleframe_luma_byte.py` → forwards to `LTG_COLOR_styleframe_luma_byte_v002.py`
- `LTG_TOOL_styleframe_luma_byte.py` → forwards to `LTG_COLOR_styleframe_luma_byte_v003.py`
- `LTG_TOOL_grandma_miri_expression_sheet.py` → forwards to `LTG_CHAR_grandma_miri_expression_sheet_v003.py`
- `LTG_TOOL_luma_expression_sheet.py` → forwards to `LTG_CHAR_luma_expression_sheet_v005.py`
- `LTG_TOOL_luma_expression_sheet.py` → forwards to `LTG_CHAR_luma_expression_sheet_v006.py`
- `LTG_TOOL_luma_turnaround.py` → forwards to `LTG_CHAR_luma_turnaround_v002.py`

**Misplaced file:** `output/color/style_frames/LTG_TOOL_style_frame_02_glitch_storm.py` — already correctly named, but placed outside `output/tools/`. Entry point stub added at `output/tools/LTG_TOOL_style_frame_02_glitch_storm.py`.

**Cycle 29 — Cleanup Tool Created:** `LTG_TOOL_naming_cleanup.py` (see Script Index) removes all original LTG_CHAR_/LTG_COLOR_/LTG_BRAND_ source files once the canonical LTG_TOOL_ copy is confirmed on disk. No git repo is present; standard `os.remove()` is used. Run with `--dry-run` to preview. After running, forwarding stubs above should also be removed (they reference now-deleted files). The original LTG_CHAR_/LTG_COLOR_/LTG_BRAND_ files remain on disk until the script is executed.

---

## Legacy Archive — Cycle 29 Naming Cleanup

**Kai Nakamura — 2026-03-29 (Cycle 29)**

C29 naming compliance completion pass. 22 non-compliant `.py` files in `output/tools/` (prefixed LTG_CHAR_, LTG_COLOR_, LTG_BRAND_) are queued for removal. All have canonical LTG_TOOL_ copies on disk. Cleanup tool: `LTG_TOOL_naming_cleanup.py`.

Files to be removed (all have LTG_TOOL_ canonical equivalents):
- `LTG_CHAR_luma_expression_sheet–v006.py` (5 files)
- `LTG_CHAR_luma_turnaround_v002.py`
- `LTG_CHAR_byte_expression_sheet.py`
- `LTG_CHAR_cosmo_expression_sheet.py`, `LTG_CHAR_cosmo_turnaround_v002.py`
- `LTG_CHAR_grandma_miri_expression_sheet_v003.py`
- `LTG_CHAR_glitch_expression_sheet_v001–v002.py`, `LTG_CHAR_glitch_turnaround_v001–v002.py`, `LTG_CHAR_glitch_color_model_v001.py` (5 files)
- `LTG_COLOR_luma_color_model_v001.py`, `LTG_COLOR_byte_color_model_v001.py`, `LTG_COLOR_cosmo_color_model_v001.py`
- `LTG_COLOR_styleframe_luma_byte_v001–v003.py` (3 files)
- `LTG_BRAND_logo_v001.py`

---

## Registration Instructions

When a new script is created, add a row to the Script Index above with:
- **Script Name:** filename including extension (e.g. `LTG_TOOL_confetti_emitter_v001.py`)
- **Created By:** team member name
- **Purpose:** one-sentence description of what the script does
- **Dependencies:** Python packages or external tools required (e.g. `Pillow`, `ImageMagick`, `svgwrite`)

Scripts must follow the standard naming convention: `LTG_TOOL_[descriptor]_v[###].[ext]`

All scripts are stored in this directory (`/home/wipkat/team/output/tools/`) and version-controlled via standard Git.

---

## Render Library — API Reference (LTG_TOOL_render_lib.py)

**Version:** 1.1.0 (C24: `paper_texture()` added)
**Canonical import pattern (run from any directory):**
```python
import sys, os
sys.path.insert(0, "/home/wipkat/team/output/tools")
from LTG_TOOL_render_lib import (
    perlin_noise_texture, gaussian_glow, light_shaft,
    dust_motes, catenary_wire, scanline_overlay, vignette, paper_texture
)
```

| Function | Signature | Returns | Notes |
|---|---|---|---|
| `perlin_noise_texture` | `(width, height, scale=50, seed=42, octaves=3, alpha=60)` | RGBA Image | Layered sin/cos noise. Slow at full canvas — use 960×540 + upscale for perf. |
| `gaussian_glow` | `(img, center, radius, color, max_alpha=100, steps=10)` | img (RGBA, in-place) | Alpha floored at 1 to prevent dead outer ellipses. |
| `light_shaft` | `(img, apex, base_left, base_right, color, max_alpha=60)` | img (RGBA, in-place) | Triangle polygon; GaussianBlur feather pass. |
| `dust_motes` | `(draw, bounds, count=20, seed=42, color, alpha_range)` | None (draws in-place) | draw must be attached to RGBA image. |
| `catenary_wire` | `(draw, p0, p1, sag=0.05, color, width=2)` | None (draws in-place) | Parabolic 40-segment polyline. |
| `scanline_overlay` | `(img, spacing=4, alpha=15)` | RGBA Image | Converts to RGBA if needed. |
| `vignette` | `(img, strength=60)` | RGBA Image | Converts to RGBA if needed. Mach-band ring approximation (intentional). |
| `paper_texture` | `(img, scale=40, alpha=20, seed=42)` | RGBA Image | **Added C24.** 1/4-res grain tile + NN upscale. Fast in pure Python. |

### `paper_texture()` details
Composites a procedural paper/canvas grain over the source image. Works at 1/4 resolution internally for speed (O((W/4)×(H/4)) pixel loop — <1s at 1920×1080 in pure Python). Upscales with nearest-neighbour to preserve grain character. Three layered sin/cos octaves. Seed is reproducible.

- `scale`: grain wavelength in pixels. 20 = very fine; 40 = standard paper tooth; 80 = coarse canvas.
- `alpha`: maximum per-pixel grain opacity. 8–20 = subtle/professional; 30–40 = heavy hand-drawn texture.
- `seed`: reproducible. Same seed + scale + alpha = identical grain.

---

## Pipeline Health — C24

**Audit date:** 2026-03-29 (Cycle 24)
**Audited by:** Kai Nakamura

### Summary

The deprecated wrapper `ltg_render_lib.py` was deleted in Cycle 23. All active consumer scripts now import from `LTG_TOOL_render_lib` directly. This audit verifies no live code references the old import path.

### Old-pattern references found

A grep of `output/**/*.py` for `ltg_render_lib` returned the following hits. Each is categorized:

| File | Line | Content | Category | Action Required |
|---|---|---|---|---|
| `output/tools/LTG_TOOL_render_lib.py` | 5 | `Renamed from ltg_render_lib.py in Cycle 22...` | Docstring — historical note | None — intentional |
| `output/tools/LTG_TOOL_bg_tech_den.py` | 7 | `...replaces all inline lighting code with calls to ltg_render_lib.` | Docstring — old module name | None — benign comment |
| `output/tools/LTG_TOOL_bg_tech_den.py` | 290, 300, 746 | `# ── ... via ltg_render_lib.function()` | Inline comment | None — comment only; actual import is `LTG_TOOL_render_lib` ✓ |
| `output/tools/LTG_TOOL_bg_tech_den.py` | 301, 315, 769 | `# ── ... via ltg_render_lib.function()` | Inline comment | None — comment only; actual import is `LTG_TOOL_render_lib` ✓ |
| `output/backgrounds/environments/LTG_ENV_tech_den.py` | 301, 315, 769 | `# ── ... via ltg_render_lib.function()` | Inline comment | None — comment only; actual import is `LTG_TOOL_render_lib` ✓ |
| `output/tools/LTG_TOOL_bg_glitchlayer_frame.py` | 10, 105, 295 | `scanline_overlay() from ltg_render_lib applied...` | Docstring/comment | None — comment only; actual import is `LTG_TOOL_render_lib` ✓ |
| `output/tools/LTG_TOOL_bg_grandma_kitchen.py` | 25, 1056 | `ltg_render_lib.py not yet available` | Docstring — historical note | None — this was written before C21; v003 supersedes v002 |

### Live import verification

A separate grep for `from ltg_render_lib` and `import ltg_render_lib` (executable import statements) found **zero matches**. The pipeline is clean.

Files confirmed using the canonical `LTG_TOOL_render_lib` import:
- `output/tools/LTG_TOOL_bg_tech_den.py` ✓
- `output/tools/LTG_TOOL_bg_tech_den.py` ✓
- `output/tools/LTG_TOOL_bg_glitchlayer_frame.py` ✓
- `output/backgrounds/environments/LTG_ENV_tech_den.py` ✓
- `output/tools/LTG_TOOL_stylize_handdrawn.py` ✓ (optional try/import with correct name)

### Stale TODO comments

Several files contain `# TODO: update import to LTG_TOOL_render_lib after Kai's rename` in their docstrings. These files do **not** import the render lib at all — the TODO was written as a precaution and was never needed. The comments are stale but harmless.

| File | Status |
|---|---|
| `output/tools/LTG_CHAR_cosmo_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/tools/LTG_CHAR_byte_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/tools/LTG_TOOL_byte_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/tools/LTG_CHAR_luma_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/characters/main/LTG_CHAR_cosmo_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/characters/main/LTG_CHAR_byte_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/characters/main/LTG_CHAR_luma_expression_sheet.py` | TODO is stale — no render lib import present or needed |
| `output/color/style_frames/LTG_TOOL_style_frame_02_glitch_storm.py` | TODO is stale — no render lib import present or needed |

### Verdict

**Pipeline is healthy.** No live code imports the deleted `ltg_render_lib.py` wrapper. All stale references are in comments and docstrings only — no functional risk. Stale TODO comments noted above may be cleaned up opportunistically in a future cycle but require no immediate action.

---

## README Audit — Cycle 34

**Audited by:** Morgan Walsh (Pipeline Automation Specialist) — 2026-03-29

Cross-check of every `LTG_TOOL_*.py` file in `output/tools/` against the Script Index above. Files were enumerated from disk and compared against all backtick-quoted `.py` filenames in this README.

### Tools present on disk but NOT listed in the Script Index

These files exist in `output/tools/` but have no entry in the table above. They should be registered (or confirmed as intentional unlisted utilities).

| File | Likely Origin | Action Required |
|---|---|---|
| `LTG_TOOL_cycle13_panel_fixes.py` | Lee Tanaka / Cycle 13 | **REGISTER** — storyboard panel fix batch for Cycle 13 panels |
| `LTG_TOOL_act2_panels_cycle14.py` | Lee Tanaka / Cycle 14 | **REGISTER** — Act 2 storyboard panel batch for Cycle 14 |
| `LTG_TOOL_sb_a2_cycle15.py` | Lee Tanaka / Cycle 15 | **REGISTER** — Act 2 storyboard batch for Cycle 15 |
| `LTG_TOOL_sb_panel_a101.py` | Lee Tanaka | **REGISTER** — individual panel generator A1-01 |
| `LTG_TOOL_sb_panel_a102.py` | Lee Tanaka | **REGISTER** — individual panel generator A1-02 |
| `LTG_TOOL_sb_panel_a103.py` | Lee Tanaka | **REGISTER** — individual panel generator A1-03 v001 |
| `LTG_TOOL_sb_panel_a103.py` | Lee Tanaka | **REGISTER** — individual panel generator A1-03 v002 |
| `LTG_TOOL_sb_panel_a104_kitchen.py` | Lee Tanaka | **REGISTER** — individual panel generator A1-04 (kitchen) |
| `LTG_TOOL_sb_panel_a201.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-01 |
| `LTG_TOOL_sb_panel_a202.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-02 v002 |
| `LTG_TOOL_sb_panel_a203.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-03 v002 |
| `LTG_TOOL_sb_panel_a204.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-04 v002 |
| `LTG_TOOL_sb_panel_a205.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-05 |
| `LTG_TOOL_sb_panel_a206_insert.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-06 insert |
| `LTG_TOOL_sb_panel_a207.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-07 v001 |
| `LTG_TOOL_sb_panel_a207b.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-07b |
| `LTG_TOOL_sb_panel_a208.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-08 v001 |
| `LTG_TOOL_sb_panel_a208.py` | Lee Tanaka | **REGISTER** — individual panel generator A2-08 v002 |
| `LTG_TOOL_sb_act1_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 1 contact sheet generator v001 |
| `LTG_TOOL_sb_act1_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 1 contact sheet generator v002 |
| `LTG_TOOL_sb_act1_full_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 1 full (complete) contact sheet |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 2 contact sheet v003 |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 2 contact sheet v004 |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 2 contact sheet v005 |
| `LTG_TOOL_sb_act2_contact_sheet.py` | Lee Tanaka | **REGISTER** — Act 2 contact sheet v006 |
| `LTG_TOOL_colorkey_otherside_gen.py` | Sam Kowalski | **REGISTER** — color key thumbnail generator for SF03 "Other Side" |
| `LTG_TOOL_character_lineup.py` | Maya Santos | **REGISTER** — character lineup v003 (pre-Glitch; superseded by v004+) |
| `LTG_TOOL_luma_color_model.py` | Sam Kowalski | **REGISTER** — Luma color model v002 (update to v001) |
| `LTG_TOOL_proportion_audit.py` | Kai Nakamura | **REGISTER** — standalone proportion audit batch tool |
| `LTG_TOOL_char_diff.py` | Kai Nakamura | **REGISTER** — character-to-character visual diff tool |
| `LTG_TOOL_luma_expression_sheet.py` | Maya Santos | **REGISTER** — Luma expression sheet v008 (current C34) |
| `LTG_TOOL_character_lineup.py` | Maya Santos | **REGISTER** — character lineup v007 (current C34) |
| `LTG_TOOL_precritique_qa.py` | Morgan Walsh / Cycle 34 | **REGISTER** — pre-critique QA pipeline (this file, C34) |
| `run_c31_qa.py` | Kai Nakamura | **NOTE** — non-LTG-named batch runner; consider rename or retirement |
| `test_face_lighting.py` | Rin Yamamoto | **NOTE** — non-LTG-named test script; consider rename or retirement |

### Tools listed in Script Index but NOT found on disk

None found. All 135 LTG_TOOL_*.py entries in the Script Index correspond to files present on disk or clearly documented in the Legacy Archive sections above.

### Summary

- **Unlisted tools on disk (C34):** 35 (33 LTG_TOOL_* + 2 non-standard names)
- **Listed but missing from disk:** 0
- **Action required:** Lee Tanaka to register all `LTG_TOOL_sb_panel_*` and `LTG_TOOL_sb_act*` entries (15 panel + 4 contact sheet generators). Sam Kowalski to register `LTG_TOOL_colorkey_otherside_gen.py` and `LTG_TOOL_luma_color_model.py`. Kai Nakamura to register `LTG_TOOL_proportion_audit.py` and `LTG_TOOL_char_diff.py`. Maya Santos to register `LTG_TOOL_luma_expression_sheet.py` and `LTG_TOOL_character_lineup.py`. `run_c31_qa.py` and `test_face_lighting.py` need rename to `LTG_TOOL_*` convention or explicit retirement notes.

**C35 Update (Morgan Walsh):** All 33 flagged `LTG_TOOL_*` tools have been registered in the Script Index above. `LTG_TOOL_proportion_verify.py` also registered (was referenced but not listed). `run_c31_qa.py` and `test_face_lighting.py` remain non-standard — these are legacy test scripts and are being tracked but not renamed at this time. `LTG_TOOL_readme_sync.py` (new C35 tool) registered. Automated README sync is now part of the pre-critique QA pipeline (Section 7).

---

## Retired / Deprecated Tools

Tools moved to `output/tools/deprecated/`. Full source preserved for git history. Do not run or import.

| Script Name | Retired By | Cycle | Reason | Canonical Replacement |
|---|---|---|---|---|
| ~~`LTG_TOOL_style_frame_01_discovery.py`~~ | Morgan Walsh | C43 | Dual-generator conflict — both this and `LTG_TOOL_styleframe_discovery.py` wrote to `LTG_COLOR_styleframe_discovery.png`. Provenance unresolvable (Petra Volkov C17 FAIL). Legacy 1920×1080 + LANCZOS approach. | `LTG_TOOL_styleframe_discovery.py` (Rin Yamamoto C38 rebuild) |
| ~~`LTG_TOOL_cycle13_panel_fixes.py`~~ | Morgan Walsh | C44 | Legacy batch fix runner (Cycle 13, Lee Tanaka). Output targets used old `LTG_SB_coldopen_panel_XX` naming, predating canonical `LTG_SB_cold_open_PXX` standard. Not referenced by any CI execution step. All output panels migrated to `panels/legacy/`. | Per-panel generators `LTG_TOOL_sb_cold_open_P*.py` (Diego Vargas C41–C43) |
